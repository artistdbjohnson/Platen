commit cbc1bf22bb367d9c5aa548af0b2acbe24de8f134
Author: artistdbjohnson <fakedoingstuff@gmail.com>
Date:   Sun Feb 22 01:50:02 2026 +0000

    Feature: Add 'Set Seed' button to regenerate specific artwork

diff --git a/larkspur.html b/larkspur.html
index e7dca56..a4e94ed 100644
--- a/larkspur.html
+++ b/larkspur.html
@@ -349,6 +349,7 @@
             <button id="btn-theme" onclick="toggleTheme()" title="Toggle Light/Dark Mode">☀︎</button>
             <button id="btn-randomize" onclick="randomize()">&#9632; Randomize</button>
             <button id="btn-regen" onclick="regenerate()">&#8635; Regenerate</button>
+            <button id="btn-set-seed" onclick="setSeed()" title="Enter a specific seed"># Set Seed</button>
             <button id="btn-panel" class="active" onclick="setLayout('panel')" disabled>Panel 9:16</button>
             <button id="btn-banner" onclick="setLayout('banner')" disabled>Banner 16:9</button>
             <button id="btn-out-svg" onclick="saveSVG()" title="Download SVG" disabled>&#8595; SVG</button>
@@ -503,6 +504,19 @@
             renderAll();
         }
 
+        function setSeed() {
+            var input = prompt("Enter a seed number to regenerate:", RENDER_SEED);
+            if (input !== null) {
+                var newSeed = parseInt(input, 10);
+                if (!isNaN(newSeed)) {
+                    RENDER_SEED = newSeed;
+                    renderAll();
+                } else {
+                    alert("Invalid seed. Please enter a number.");
+                }
+            }
+        }
+
 
         function populateSelect(id, opts, val) {
             var sel = document.getElementById(id);

commit 58604bdd5f1a080a996f63d857db356c70332531
Author: artistdbjohnson <fakedoingstuff@gmail.com>
Date:   Sun Feb 22 01:41:44 2026 +0000

    UI: Auto-render artwork on layout change (9:16 vs 16:9)

diff --git a/larkspur.html b/larkspur.html
index c6fd1bd..e7dca56 100644
--- a/larkspur.html
+++ b/larkspur.html
@@ -479,8 +479,8 @@
                 btnPanel.classList.remove('active');
                 btnBanner.classList.add('active');
             }
-            // Auto-render disabled: only render on randomize/regenerate
-            // renderAll();
+            // Auto-render on layout change
+            renderAll();
         }
 
         function randomize() {

commit 0f66b8d741780d9fc4361597c9a76402b34052d8
Author: artistdbjohnson <fakedoingstuff@gmail.com>
Date:   Sun Feb 22 01:40:46 2026 +0000

    UI: Trigger randomize on boot for immediate rendering

diff --git a/larkspur.html b/larkspur.html
index 2e3f6ac..c6fd1bd 100644
--- a/larkspur.html
+++ b/larkspur.html
@@ -1404,7 +1404,8 @@
         })();
 
         // ── Boot ──────────────────────────────────────────────────────────────────────
-        // Initial render disabled per user request: setTimeout(renderAll, 100);
+        // Initial render: start immediately with random art
+        setTimeout(randomize, 100);
     </script>
 </body>
 

commit 54ea9c3e1b752972bfc476e291476a150e0b09f9
Author: artistdbjohnson <fakedoingstuff@gmail.com>
Date:   Sun Feb 22 01:38:14 2026 +0000

    UI: Add traits readout to status bar upon render completion

diff --git a/larkspur.html b/larkspur.html
index 673e433..2e3f6ac 100644
--- a/larkspur.html
+++ b/larkspur.html
@@ -553,22 +553,24 @@
                         var totalTime = (performance.now() - t0).toFixed(0);
                         console.log('Total render time: ' + totalTime + 'ms');
                         console.groupEnd();
-                        document.getElementById('st-info').textContent = 'ready (' + totalTime + 'ms)  ·  #' + RENDER_SEED;
-                        if (infos.length > 0) document.getElementById('st-info').textContent = infos.join('  ·  ') + '  ·  #' + RENDER_SEED;
+
+                        var traitStr = "sym: " + runTraits.symmetry + " · space: " + runTraits.space + " · chrome: " + runTraits.chromes + " · engine: " + runTraits.engine + " · canvas: " + runTraits.canvas;
+                        var prefix = infos.length > 0 ? infos.join('  ·  ') : 'ready (' + totalTime + 'ms)';
+                        document.getElementById('st-info').textContent = prefix + '  ·  #' + RENDER_SEED + '  |  ' + traitStr;
                         return;
                     }
 
                     var id = panels[pi];
                     // Render one panel asynchronously
                     var t1 = performance.now();
-                    renderLarkspur('s' + id, id, runTraits).then(function(r) {
+                    renderLarkspur('s' + id, id, runTraits).then(function (r) {
                         var dt = (performance.now() - t1).toFixed(0);
                         console.log('  v' + id + ': ' + r.flowers + ' flowers, ' + r.attempts + ' att, ' + dt + 'ms');
                         infos.push('v' + id + ': ' + r.flowers + ' flowers');
                         pi++;
                         // Yield to main thread
                         setTimeout(renderNext, 20);
-                    }).catch(function(e) {
+                    }).catch(function (e) {
                         console.error('Error rendering panel ' + id, e);
                         pi++;
                         setTimeout(renderNext, 20);

commit 0b7101f155d69f931d131ecefcd2689691820736
Author: artistdbjohnson <fakedoingstuff@gmail.com>
Date:   Sun Feb 22 01:34:50 2026 +0000

    Refactor: Extract modules and implement Web Worker rendering loop

diff --git a/African Textiles/Asante.png b/African Textiles/Asante.png
new file mode 100644
index 0000000..a10dd36
Binary files /dev/null and b/African Textiles/Asante.png differ
diff --git a/African Textiles/Bamana people of Mali.png b/African Textiles/Bamana people of Mali.png
new file mode 100644
index 0000000..c59082c
Binary files /dev/null and b/African Textiles/Bamana people of Mali.png differ
diff --git a/African Textiles/Ewe.png b/African Textiles/Ewe.png
new file mode 100644
index 0000000..51aab49
Binary files /dev/null and b/African Textiles/Ewe.png differ
diff --git a/African Textiles/Ganda people of Uganda.png b/African Textiles/Ganda people of Uganda.png
new file mode 100644
index 0000000..1e48220
Binary files /dev/null and b/African Textiles/Ganda people of Uganda.png differ
diff --git a/African Textiles/Ghana.png b/African Textiles/Ghana.png
new file mode 100644
index 0000000..368e288
Binary files /dev/null and b/African Textiles/Ghana.png differ
diff --git a/African Textiles/Hausa (2).png b/African Textiles/Hausa (2).png
new file mode 100644
index 0000000..093735d
Binary files /dev/null and b/African Textiles/Hausa (2).png differ
diff --git a/African Textiles/Hausa (3).png b/African Textiles/Hausa (3).png
new file mode 100644
index 0000000..d5a2241
Binary files /dev/null and b/African Textiles/Hausa (3).png differ
diff --git a/African Textiles/Hausa.png b/African Textiles/Hausa.png
new file mode 100644
index 0000000..fa80d38
Binary files /dev/null and b/African Textiles/Hausa.png differ
diff --git a/African Textiles/Hausa3.png b/African Textiles/Hausa3.png
new file mode 100644
index 0000000..653ab5c
Binary files /dev/null and b/African Textiles/Hausa3.png differ
diff --git a/African Textiles/Kpokpo_Vai Sierra LEone.png b/African Textiles/Kpokpo_Vai Sierra LEone.png
new file mode 100644
index 0000000..63fb68b
Binary files /dev/null and b/African Textiles/Kpokpo_Vai Sierra LEone.png differ
diff --git a/African Textiles/Malagasy Lamba_Betsileo people.png b/African Textiles/Malagasy Lamba_Betsileo people.png
new file mode 100644
index 0000000..487c032
Binary files /dev/null and b/African Textiles/Malagasy Lamba_Betsileo people.png differ
diff --git a/African Textiles/Malagasy Merina Madagascar.png b/African Textiles/Malagasy Merina Madagascar.png
new file mode 100644
index 0000000..5f156b4
Binary files /dev/null and b/African Textiles/Malagasy Merina Madagascar.png differ
diff --git a/African Textiles/Malagasy Merina people of Madagascar.png b/African Textiles/Malagasy Merina people of Madagascar.png
new file mode 100644
index 0000000..bf13ca6
Binary files /dev/null and b/African Textiles/Malagasy Merina people of Madagascar.png differ
diff --git a/African Textiles/Mende Chequer board cloth.png b/African Textiles/Mende Chequer board cloth.png
new file mode 100644
index 0000000..0fcadf8
Binary files /dev/null and b/African Textiles/Mende Chequer board cloth.png differ
diff --git a/African Textiles/Ngongo Raffia.png b/African Textiles/Ngongo Raffia.png
new file mode 100644
index 0000000..f3f1fd4
Binary files /dev/null and b/African Textiles/Ngongo Raffia.png differ
diff --git a/African Textiles/Ngongo.png b/African Textiles/Ngongo.png
new file mode 100644
index 0000000..77d07e2
Binary files /dev/null and b/African Textiles/Ngongo.png differ
diff --git a/African Textiles/Screenshot 2026-02-21 234850.png b/African Textiles/Screenshot 2026-02-21 234850.png
new file mode 100644
index 0000000..c4109b7
Binary files /dev/null and b/African Textiles/Screenshot 2026-02-21 234850.png differ
diff --git a/African Textiles/Screenshot 2026-02-21 234906.png b/African Textiles/Screenshot 2026-02-21 234906.png
new file mode 100644
index 0000000..ecb593b
Binary files /dev/null and b/African Textiles/Screenshot 2026-02-21 234906.png differ
diff --git a/African Textiles/Screenshot 2026-02-21 234919.png b/African Textiles/Screenshot 2026-02-21 234919.png
new file mode 100644
index 0000000..6de39e8
Binary files /dev/null and b/African Textiles/Screenshot 2026-02-21 234919.png differ
diff --git a/African Textiles/Screenshot 2026-02-21 234930.png b/African Textiles/Screenshot 2026-02-21 234930.png
new file mode 100644
index 0000000..995ee45
Binary files /dev/null and b/African Textiles/Screenshot 2026-02-21 234930.png differ
diff --git a/African Textiles/Tunisia.png b/African Textiles/Tunisia.png
new file mode 100644
index 0000000..14f6c8a
Binary files /dev/null and b/African Textiles/Tunisia.png differ
diff --git a/African Textiles/Yoruba Resit DYed Adire Cloth.png b/African Textiles/Yoruba Resit DYed Adire Cloth.png
new file mode 100644
index 0000000..4f8dfed
Binary files /dev/null and b/African Textiles/Yoruba Resit DYed Adire Cloth.png differ
diff --git a/African Textiles/akwete cotton clothb.png b/African Textiles/akwete cotton clothb.png
new file mode 100644
index 0000000..e01ea7d
Binary files /dev/null and b/African Textiles/akwete cotton clothb.png differ
diff --git a/African Textiles/cameroon.png b/African Textiles/cameroon.png
new file mode 100644
index 0000000..824db74
Binary files /dev/null and b/African Textiles/cameroon.png differ
diff --git a/African Textiles/ghana or togo.png b/African Textiles/ghana or togo.png
new file mode 100644
index 0000000..8dfbfff
Binary files /dev/null and b/African Textiles/ghana or togo.png differ
diff --git a/African Textiles/guinea bissau.png b/African Textiles/guinea bissau.png
new file mode 100644
index 0000000..47490e9
Binary files /dev/null and b/African Textiles/guinea bissau.png differ
diff --git a/African Textiles/mandingo weavers guinea.png b/African Textiles/mandingo weavers guinea.png
new file mode 100644
index 0000000..b574b22
Binary files /dev/null and b/African Textiles/mandingo weavers guinea.png differ
diff --git a/Japanese Waves/29775127757_522c854d25_o.jpg b/Japanese Waves/29775127757_522c854d25_o.jpg
new file mode 100644
index 0000000..57b6c9b
Binary files /dev/null and b/Japanese Waves/29775127757_522c854d25_o.jpg differ
diff --git a/Japanese Waves/29775129927_40e4cb8d4d_o.jpg b/Japanese Waves/29775129927_40e4cb8d4d_o.jpg
new file mode 100644
index 0000000..49ac1bd
Binary files /dev/null and b/Japanese Waves/29775129927_40e4cb8d4d_o.jpg differ
diff --git a/Japanese Waves/30841397038_8d2b24474a_o.jpg b/Japanese Waves/30841397038_8d2b24474a_o.jpg
new file mode 100644
index 0000000..1df5671
Binary files /dev/null and b/Japanese Waves/30841397038_8d2b24474a_o.jpg differ
diff --git a/Japanese Waves/30841400178_dd94816c44_o.jpg b/Japanese Waves/30841400178_dd94816c44_o.jpg
new file mode 100644
index 0000000..c02594f
Binary files /dev/null and b/Japanese Waves/30841400178_dd94816c44_o.jpg differ
diff --git a/Japanese Waves/30841404218_a93139bd5d_o.jpg b/Japanese Waves/30841404218_a93139bd5d_o.jpg
new file mode 100644
index 0000000..aa41b5c
Binary files /dev/null and b/Japanese Waves/30841404218_a93139bd5d_o.jpg differ
diff --git a/Japanese Waves/30841407478_2eb1a4a858_o.jpg b/Japanese Waves/30841407478_2eb1a4a858_o.jpg
new file mode 100644
index 0000000..4be7333
Binary files /dev/null and b/Japanese Waves/30841407478_2eb1a4a858_o.jpg differ
diff --git a/Japanese Waves/30841414138_7072a459fd_o.jpg b/Japanese Waves/30841414138_7072a459fd_o.jpg
new file mode 100644
index 0000000..b6ca13a
Binary files /dev/null and b/Japanese Waves/30841414138_7072a459fd_o.jpg differ
diff --git a/Japanese Waves/35861583063_8d24dee78a_o.jpg b/Japanese Waves/35861583063_8d24dee78a_o.jpg
new file mode 100644
index 0000000..ff42ea7
Binary files /dev/null and b/Japanese Waves/35861583063_8d24dee78a_o.jpg differ
diff --git a/Japanese Waves/43803023395_0adaf94c16_h.jpg b/Japanese Waves/43803023395_0adaf94c16_h.jpg
new file mode 100644
index 0000000..50968b3
Binary files /dev/null and b/Japanese Waves/43803023395_0adaf94c16_h.jpg differ
diff --git a/Japanese Waves/43803031265_c5c79f217e_o.jpg b/Japanese Waves/43803031265_c5c79f217e_o.jpg
new file mode 100644
index 0000000..270131f
Binary files /dev/null and b/Japanese Waves/43803031265_c5c79f217e_o.jpg differ
diff --git a/Japanese Waves/43803034805_153d7379fa_o.jpg b/Japanese Waves/43803034805_153d7379fa_o.jpg
new file mode 100644
index 0000000..8903de7
Binary files /dev/null and b/Japanese Waves/43803034805_153d7379fa_o.jpg differ
diff --git a/Japanese Waves/43803036165_4c1b32ec91_o.jpg b/Japanese Waves/43803036165_4c1b32ec91_o.jpg
new file mode 100644
index 0000000..73578a2
Binary files /dev/null and b/Japanese Waves/43803036165_4c1b32ec91_o.jpg differ
diff --git a/Japanese Waves/43803038685_4ff14826cc_o.jpg b/Japanese Waves/43803038685_4ff14826cc_o.jpg
new file mode 100644
index 0000000..cb4c9b5
Binary files /dev/null and b/Japanese Waves/43803038685_4ff14826cc_o.jpg differ
diff --git a/Japanese Waves/43803040365_cd1fea78fd_o.jpg b/Japanese Waves/43803040365_cd1fea78fd_o.jpg
new file mode 100644
index 0000000..207cddd
Binary files /dev/null and b/Japanese Waves/43803040365_cd1fea78fd_o.jpg differ
diff --git a/Japanese Waves/43994120514_9cf3df4d11_o.jpg b/Japanese Waves/43994120514_9cf3df4d11_o.jpg
new file mode 100644
index 0000000..e0d7a7c
Binary files /dev/null and b/Japanese Waves/43994120514_9cf3df4d11_o.jpg differ
diff --git a/Japanese Waves/43994123754_b84e5a89b0_o.jpg b/Japanese Waves/43994123754_b84e5a89b0_o.jpg
new file mode 100644
index 0000000..4130400
Binary files /dev/null and b/Japanese Waves/43994123754_b84e5a89b0_o.jpg differ
diff --git a/Japanese Waves/43994125164_f5915fca91_o.jpg b/Japanese Waves/43994125164_f5915fca91_o.jpg
new file mode 100644
index 0000000..b8fa4d5
Binary files /dev/null and b/Japanese Waves/43994125164_f5915fca91_o.jpg differ
diff --git a/Japanese Waves/43994126084_c017a133e0_o.jpg b/Japanese Waves/43994126084_c017a133e0_o.jpg
new file mode 100644
index 0000000..7c13f73
Binary files /dev/null and b/Japanese Waves/43994126084_c017a133e0_o.jpg differ
diff --git a/Japanese Waves/44662899742_2ac90f2057_o.jpg b/Japanese Waves/44662899742_2ac90f2057_o.jpg
new file mode 100644
index 0000000..5c2b224
Binary files /dev/null and b/Japanese Waves/44662899742_2ac90f2057_o.jpg differ
diff --git a/Japanese Waves/44662901212_b09371bee1_o.jpg b/Japanese Waves/44662901212_b09371bee1_o.jpg
new file mode 100644
index 0000000..4b75aeb
Binary files /dev/null and b/Japanese Waves/44662901212_b09371bee1_o.jpg differ
diff --git a/Japanese Waves/44662902712_5a6a54f0de_o.jpg b/Japanese Waves/44662902712_5a6a54f0de_o.jpg
new file mode 100644
index 0000000..90b9b93
Binary files /dev/null and b/Japanese Waves/44662902712_5a6a54f0de_o.jpg differ
diff --git a/Japanese Waves/44711800391_f06d0dfa25_o.jpg b/Japanese Waves/44711800391_f06d0dfa25_o.jpg
new file mode 100644
index 0000000..3d081ff
Binary files /dev/null and b/Japanese Waves/44711800391_f06d0dfa25_o.jpg differ
diff --git a/Snow Flakes/Screenshot 2026-02-21 224959.png b/Snow Flakes/Screenshot 2026-02-21 224959.png
new file mode 100644
index 0000000..4d71ff5
Binary files /dev/null and b/Snow Flakes/Screenshot 2026-02-21 224959.png differ
diff --git a/Snow Flakes/Screenshot 2026-02-21 225016.png b/Snow Flakes/Screenshot 2026-02-21 225016.png
new file mode 100644
index 0000000..86ceba1
Binary files /dev/null and b/Snow Flakes/Screenshot 2026-02-21 225016.png differ
diff --git a/Snow Flakes/Screenshot 2026-02-21 225039.png b/Snow Flakes/Screenshot 2026-02-21 225039.png
new file mode 100644
index 0000000..1b07d1a
Binary files /dev/null and b/Snow Flakes/Screenshot 2026-02-21 225039.png differ
diff --git a/Snow Flakes/Screenshot 2026-02-21 225115.png b/Snow Flakes/Screenshot 2026-02-21 225115.png
new file mode 100644
index 0000000..44a7f84
Binary files /dev/null and b/Snow Flakes/Screenshot 2026-02-21 225115.png differ
diff --git a/Snow Flakes/Screenshot 2026-02-21 225133.png b/Snow Flakes/Screenshot 2026-02-21 225133.png
new file mode 100644
index 0000000..d0b8294
Binary files /dev/null and b/Snow Flakes/Screenshot 2026-02-21 225133.png differ
diff --git a/Snow Flakes/Screenshot 2026-02-21 225142.png b/Snow Flakes/Screenshot 2026-02-21 225142.png
new file mode 100644
index 0000000..0c888d9
Binary files /dev/null and b/Snow Flakes/Screenshot 2026-02-21 225142.png differ
diff --git a/Snow Flakes/Screenshot 2026-02-21 225149.png b/Snow Flakes/Screenshot 2026-02-21 225149.png
new file mode 100644
index 0000000..0948996
Binary files /dev/null and b/Snow Flakes/Screenshot 2026-02-21 225149.png differ
diff --git a/Snow Flakes/Screenshot 2026-02-21 225155.png b/Snow Flakes/Screenshot 2026-02-21 225155.png
new file mode 100644
index 0000000..07d4aa5
Binary files /dev/null and b/Snow Flakes/Screenshot 2026-02-21 225155.png differ
diff --git a/Snow Flakes/Screenshot 2026-02-21 225204.png b/Snow Flakes/Screenshot 2026-02-21 225204.png
new file mode 100644
index 0000000..fd54352
Binary files /dev/null and b/Snow Flakes/Screenshot 2026-02-21 225204.png differ
diff --git a/Snow Flakes/Screenshot 2026-02-21 225210.png b/Snow Flakes/Screenshot 2026-02-21 225210.png
new file mode 100644
index 0000000..8e2530e
Binary files /dev/null and b/Snow Flakes/Screenshot 2026-02-21 225210.png differ
diff --git a/Snow Flakes/Screenshot 2026-02-21 225218.png b/Snow Flakes/Screenshot 2026-02-21 225218.png
new file mode 100644
index 0000000..940cea5
Binary files /dev/null and b/Snow Flakes/Screenshot 2026-02-21 225218.png differ
diff --git a/Snow Flakes/Screenshot 2026-02-21 225225.png b/Snow Flakes/Screenshot 2026-02-21 225225.png
new file mode 100644
index 0000000..53fff06
Binary files /dev/null and b/Snow Flakes/Screenshot 2026-02-21 225225.png differ
diff --git a/axidraw_pen_mapping.md b/axidraw_pen_mapping.md
new file mode 100644
index 0000000..0189da3
--- /dev/null
+++ b/axidraw_pen_mapping.md
@@ -0,0 +1,217 @@
+# Axidraw Pen Mapping for Larkspur
+
+This document maps the digital hex codes used in the Larkspur generative palettes to specific archival physical pens suitable for Axidraw plotting.
+
+## Recommended Pen Sets
+- **Faber-Castell Pitt Artist Pens**: Excellent archival India ink, consistent flow, wide color gamut (60+ colors).
+- **Sakura Pigma Micron**: The industry standard for archival ink, but limited color palette. Very precise nibs.
+
+## Master Color Mapping List
+
+| Hex Code | Closest Physical Pen |
+|---|---|
+| `#008080` | 153 Cobalt Turquoise |
+| `#010650` | 247 Indanthrene Blue |
+| `#06FFA5` | 174 Chromium Green Opaque |
+| `#0F1316` | 199 Black |
+| `#101820` | 161 Phthalo Green |
+| `#101840` | 247 Indanthrene Blue |
+| `#111416` | 199 Black |
+| `#121212` | 199 Black |
+| `#121417` | 199 Black |
+| `#171718` | 161 Phthalo Green |
+| `#1B998B` | 153 Cobalt Turquoise |
+| `#1E2126` | 175 Dark Sepia |
+| `#1F2529` | 175 Dark Sepia |
+| `#201810` | 175 Dark Sepia |
+| `#201F25` | 175 Dark Sepia |
+| `#203828` | 161 Phthalo Green |
+| `#20B2AA` | 153 Cobalt Turquoise |
+| `#264653` | Micron Hunter Green |
+| `#2A1C17` | 175 Dark Sepia |
+| `#2A1F1A` | 175 Dark Sepia |
+| `#2A2A2A` | 175 Dark Sepia |
+| `#2A2D34` | 175 Dark Sepia |
+| `#2A9D8F` | 174 Chromium Green Opaque |
+| `#2D3047` | 175 Dark Sepia |
+| `#2E2E2E` | 175 Dark Sepia |
+| `#2E363B` | 175 Dark Sepia |
+| `#2F363D` | 175 Dark Sepia |
+| `#2F4F4F` | Micron Hunter Green |
+| `#303028` | 175 Dark Sepia |
+| `#306038` | Micron Hunter Green |
+| `#353B41` | 175 Dark Sepia |
+| `#387050` | Micron Hunter Green |
+| `#3A2F2C` | 175 Dark Sepia |
+| `#3A3A3C` | 175 Dark Sepia |
+| `#3A5F3B` | Micron Hunter Green |
+| `#3E2F23` | 175 Dark Sepia |
+| `#3E3E3E` | 177 Walnut Brown |
+| `#3F5C57` | Micron Hunter Green |
+| `#45B698` | 174 Chromium Green Opaque |
+| `#480810` | 157 Dark Indigo |
+| `#49545B` | Micron Hunter Green |
+| `#4A2F23` | 175 Dark Sepia |
+| `#4A7C6B` | 235 Cold Grey VI |
+| `#4B555F` | 235 Cold Grey VI |
+| `#4B6C70` | 235 Cold Grey VI |
+| `#4C516D` | 235 Cold Grey VI |
+| `#4ECDC4` | 146 Sky Blue |
+| `#4FC3F7` | 146 Sky Blue |
+| `#505878` | 235 Cold Grey VI |
+| `#5386A6` | 233 Cold Grey IV |
+| `#53B36A` | 174 Chromium Green Opaque |
+| `#556270` | 235 Cold Grey VI |
+| `#584020` | 177 Walnut Brown |
+| `#585048` | 177 Walnut Brown |
+| `#5B85AA` | 233 Cold Grey IV |
+| `#5C3A2E` | 177 Walnut Brown |
+| `#5C8D62` | 170 May Green |
+| `#603F3A` | 177 Walnut Brown |
+| `#60C890` | 174 Chromium Green Opaque |
+| `#681818` | Micron Burgundy |
+| `#686858` | 235 Cold Grey VI |
+| `#6A0572` | Micron Purple |
+| `#6A7177` | 235 Cold Grey VI |
+| `#6A8D73` | 233 Cold Grey IV |
+| `#6B8E7A` | 233 Cold Grey IV |
+| `#6C4E3C` | 177 Walnut Brown |
+| `#6C584C` | 235 Cold Grey VI |
+| `#6C8A7A` | 233 Cold Grey IV |
+| `#6D7880` | 233 Cold Grey IV |
+| `#6E6E71` | 235 Cold Grey VI |
+| `#6E7882` | 233 Cold Grey IV |
+| `#6E7C8F` | 233 Cold Grey IV |
+| `#707088` | 233 Cold Grey IV |
+| `#708238` | 167 Permanent Green Olive |
+| `#7A4A33` | 177 Walnut Brown |
+| `#7DA5B3` | 232 Cold Grey III |
+| `#7E4A5E` | 235 Cold Grey VI |
+| `#7E5A9B` | 233 Cold Grey IV |
+| `#848582` | 233 Cold Grey IV |
+| `#86AB2E` | 268 Green Gold |
+| `#87CEEB` | 146 Sky Blue |
+| `#883028` | Micron Brown |
+| `#886830` | 180 Raw Umber |
+| `#8890A0` | 233 Cold Grey IV |
+| `#8A3324` | Micron Brown |
+| `#8B5A47` | 180 Raw Umber |
+| `#8C7A66` | 180 Raw Umber |
+| `#908878` | 233 Cold Grey IV |
+| `#90D8B0` | 171 Light Green |
+| `#98A1A8` | 232 Cold Grey III |
+| `#98D8C8` | 146 Sky Blue |
+| `#9AA2AA` | 232 Cold Grey III |
+| `#A0A088` | 232 Cold Grey III |
+| `#A3C4BC` | 232 Cold Grey III |
+| `#A46F38` | 180 Raw Umber |
+| `#A57C58` | 180 Raw Umber |
+| `#A58F6F` | 180 Raw Umber |
+| `#A65D7B` | 180 Raw Umber |
+| `#A7ADB2` | 232 Cold Grey III |
+| `#A7C4A0` | 232 Cold Grey III |
+| `#A7C6ED` | 146 Sky Blue |
+| `#A8613F` | 180 Raw Umber |
+| `#A86B3D` | 180 Raw Umber |
+| `#B02040` | 134 Crimson |
+| `#B07060` | 180 Raw Umber |
+| `#B3595C` | 192 Indian Red |
+| `#B84848` | 192 Indian Red |
+| `#B87A65` | 192 Indian Red |
+| `#B8B8B9` | 232 Cold Grey III |
+| `#B8E8C8` | 272 Warm Grey III |
+| `#BB8FCE` | 125 Middle Purple Pink |
+| `#C0B890` | 232 Cold Grey III |
+| `#C1D37F` | 171 Light Green |
+| `#C36B4C` | 192 Indian Red |
+| `#C58B5B` | 186 Terracotta |
+| `#C6543F` | 192 Indian Red |
+| `#C6CCD1` | 272 Warm Grey III |
+| `#C6E2FF` | 154 Light Cobalt Turquoise |
+| `#C7F464` | 171 Light Green |
+| `#C82828` | 121 Pale Geranium Lake |
+| `#C84C7A` | 192 Indian Red |
+| `#C8A060` | 268 Green Gold |
+| `#C8B779` | 232 Cold Grey III |
+| `#C8E0A0` | 272 Warm Grey III |
+| `#CE3E3E` | 121 Pale Geranium Lake |
+| `#CF7B4A` | 186 Terracotta |
+| `#D07870` | 186 Terracotta |
+| `#D3B99A` | 232 Cold Grey III |
+| `#D4A58F` | 232 Cold Grey III |
+| `#D4D8DC` | 272 Warm Grey III |
+| `#D6F0BA` | 272 Warm Grey III |
+| `#D86C82` | 186 Terracotta |
+| `#D89890` | 232 Cold Grey III |
+| `#D8DCDF` | 272 Warm Grey III |
+| `#D94F3D` | 192 Indian Red |
+| `#D96B5F` | 186 Terracotta |
+| `#D9B382` | 232 Cold Grey III |
+| `#D9E4DD` | 272 Warm Grey III |
+| `#DCC48E` | 232 Cold Grey III |
+| `#E0F5E0` | 104 Light Yellow Glaze |
+| `#E56459` | 186 Terracotta |
+| `#E63946` | 121 Pale Geranium Lake |
+| `#E69FAF` | Micron Rose |
+| `#E7F5BE` | 104 Light Yellow Glaze |
+| `#E89A5C` | 186 Terracotta |
+| `#E8C4B0` | Micron Rose |
+| `#E8E2CF` | 272 Warm Grey III |
+| `#E9967A` | 186 Terracotta |
+| `#E9F1E9` | 103 Ivory |
+| `#EAEDEF` | 103 Ivory |
+| `#ED6BA7` | 127 Pink Carmine |
+| `#F05858` | 186 Terracotta |
+| `#F05A28` | 121 Pale Geranium Lake |
+| `#F0B898` | Micron Rose |
+| `#F0CA00` | 109 Dark Chrome Yellow |
+| `#F0E880` | 104 Light Yellow Glaze |
+| `#F0EAD6` | 104 Light Yellow Glaze |
+| `#F1FAEE` | 103 Ivory |
+| `#F27649` | 186 Terracotta |
+| `#F28F3B` | 186 Terracotta |
+| `#F2C14E` | 268 Green Gold |
+| `#F2C28A` | Micron Rose |
+| `#F2E394` | Micron Rose |
+| `#F2E8C6` | 104 Light Yellow Glaze |
+| `#F2EFEA` | 103 Ivory |
+| `#F38E82` | 186 Terracotta |
+| `#F3E9D2` | 104 Light Yellow Glaze |
+| `#F3F5F6` | 103 Ivory |
+| `#F4836A` | 186 Terracotta |
+| `#F4A261` | 186 Terracotta |
+| `#F4E1D2` | 104 Light Yellow Glaze |
+| `#F5E6D3` | 104 Light Yellow Glaze |
+| `#F6F3E8` | 103 Ivory |
+| `#F7DC6F` | 268 Green Gold |
+| `#F83038` | 121 Pale Geranium Lake |
+| `#F8BBD0` | Micron Rose |
+| `#F8E0A0` | Micron Rose |
+| `#F8E3C4` | 104 Light Yellow Glaze |
+| `#F8E8D0` | 104 Light Yellow Glaze |
+| `#F8F4E3` | 104 Light Yellow Glaze |
+| `#F8F4ED` | 103 Ivory |
+| `#F9B1C8` | Micron Rose |
+| `#F9BCB8` | Micron Rose |
+| `#F9C6C9` | Micron Rose |
+| `#F9D949` | 107 Cadmium Yellow |
+| `#FA9A86` | 186 Terracotta |
+| `#FAF8F3` | 103 Ivory |
+| `#FEDBDD` | Micron Rose |
+| `#FF69B4` | 127 Pink Carmine |
+| `#FF6B57` | 186 Terracotta |
+| `#FF6B6B` | 186 Terracotta |
+| `#FF7F51` | 186 Terracotta |
+| `#FF9F1C` | 113 Orange Glaze |
+| `#FFB400` | 109 Dark Chrome Yellow |
+| `#FFB692` | Micron Rose |
+| `#FFB857` | 186 Terracotta |
+| `#FFD166` | 268 Green Gold |
+| `#FFD6A8` | Micron Rose |
+| `#FFE4B5` | 104 Light Yellow Glaze |
+| `#FFE5D4` | 104 Light Yellow Glaze |
+| `#FFE6A7` | 104 Light Yellow Glaze |
+| `#FFE7E0` | 104 Light Yellow Glaze |
+| `#FFF0B6` | 104 Light Yellow Glaze |
+| `#FFF0F5` | 103 Ivory |
+| `#FFFCF2` | 103 Ivory |
diff --git a/extra patterns/070314-24a.jpg b/extra patterns/070314-24a.jpg
new file mode 100644
index 0000000..8c944ce
Binary files /dev/null and b/extra patterns/070314-24a.jpg differ
diff --git a/extra patterns/101004-08d.JPG b/extra patterns/101004-08d.JPG
new file mode 100644
index 0000000..f3f9d99
Binary files /dev/null and b/extra patterns/101004-08d.JPG differ
diff --git a/extra patterns/140225-01c.JPG b/extra patterns/140225-01c.JPG
new file mode 100644
index 0000000..c11bc62
Binary files /dev/null and b/extra patterns/140225-01c.JPG differ
diff --git a/extra patterns/140225-01p.JPG b/extra patterns/140225-01p.JPG
new file mode 100644
index 0000000..87d57f4
Binary files /dev/null and b/extra patterns/140225-01p.JPG differ
diff --git a/extra patterns/2084-01a.JPG b/extra patterns/2084-01a.JPG
new file mode 100644
index 0000000..bfcddcb
Binary files /dev/null and b/extra patterns/2084-01a.JPG differ
diff --git a/extra patterns/2084-01c.JPG b/extra patterns/2084-01c.JPG
new file mode 100644
index 0000000..fc887e8
Binary files /dev/null and b/extra patterns/2084-01c.JPG differ
diff --git a/extra patterns/Screenshot_20260220_215142_Chrome.jpg b/extra patterns/Screenshot_20260220_215142_Chrome.jpg
new file mode 100644
index 0000000..03dd412
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215142_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215202_Chrome.jpg b/extra patterns/Screenshot_20260220_215202_Chrome.jpg
new file mode 100644
index 0000000..622ac38
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215202_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215212_Chrome.jpg b/extra patterns/Screenshot_20260220_215212_Chrome.jpg
new file mode 100644
index 0000000..3f965ca
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215212_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215225_Chrome.jpg b/extra patterns/Screenshot_20260220_215225_Chrome.jpg
new file mode 100644
index 0000000..5178630
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215225_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215233_Chrome.jpg b/extra patterns/Screenshot_20260220_215233_Chrome.jpg
new file mode 100644
index 0000000..11b42f6
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215233_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215243_Chrome.jpg b/extra patterns/Screenshot_20260220_215243_Chrome.jpg
new file mode 100644
index 0000000..d58d7f0
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215243_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215256_Chrome.jpg b/extra patterns/Screenshot_20260220_215256_Chrome.jpg
new file mode 100644
index 0000000..a72c7d3
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215256_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215312_Chrome.jpg b/extra patterns/Screenshot_20260220_215312_Chrome.jpg
new file mode 100644
index 0000000..8b5667c
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215312_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215321_Chrome.jpg b/extra patterns/Screenshot_20260220_215321_Chrome.jpg
new file mode 100644
index 0000000..bc42850
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215321_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215328_Chrome.jpg b/extra patterns/Screenshot_20260220_215328_Chrome.jpg
new file mode 100644
index 0000000..0cc59ad
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215328_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215342_Chrome.jpg b/extra patterns/Screenshot_20260220_215342_Chrome.jpg
new file mode 100644
index 0000000..6c3339a
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215342_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215356_Chrome.jpg b/extra patterns/Screenshot_20260220_215356_Chrome.jpg
new file mode 100644
index 0000000..e1f4a36
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215356_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215404_Chrome.jpg b/extra patterns/Screenshot_20260220_215404_Chrome.jpg
new file mode 100644
index 0000000..65d059f
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215404_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215419_Chrome.jpg b/extra patterns/Screenshot_20260220_215419_Chrome.jpg
new file mode 100644
index 0000000..14efee6
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215419_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215427_Chrome.jpg b/extra patterns/Screenshot_20260220_215427_Chrome.jpg
new file mode 100644
index 0000000..8928ace
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215427_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215435_Chrome(1).jpg b/extra patterns/Screenshot_20260220_215435_Chrome(1).jpg
new file mode 100644
index 0000000..4add9cf
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215435_Chrome(1).jpg differ
diff --git a/extra patterns/Screenshot_20260220_215435_Chrome.jpg b/extra patterns/Screenshot_20260220_215435_Chrome.jpg
new file mode 100644
index 0000000..a128044
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215435_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215446_Chrome.jpg b/extra patterns/Screenshot_20260220_215446_Chrome.jpg
new file mode 100644
index 0000000..9405f6f
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215446_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215456_Chrome.jpg b/extra patterns/Screenshot_20260220_215456_Chrome.jpg
new file mode 100644
index 0000000..5e5d4b8
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215456_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215514_Chrome.jpg b/extra patterns/Screenshot_20260220_215514_Chrome.jpg
new file mode 100644
index 0000000..bbd7807
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215514_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215527_Chrome.jpg b/extra patterns/Screenshot_20260220_215527_Chrome.jpg
new file mode 100644
index 0000000..447e871
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215527_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215618_Chrome.jpg b/extra patterns/Screenshot_20260220_215618_Chrome.jpg
new file mode 100644
index 0000000..cb7e415
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215618_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215636_Chrome.jpg b/extra patterns/Screenshot_20260220_215636_Chrome.jpg
new file mode 100644
index 0000000..3f9591c
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215636_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215644_Chrome.jpg b/extra patterns/Screenshot_20260220_215644_Chrome.jpg
new file mode 100644
index 0000000..2ed905b
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215644_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215657_Chrome.jpg b/extra patterns/Screenshot_20260220_215657_Chrome.jpg
new file mode 100644
index 0000000..921665c
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215657_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215715_Chrome.jpg b/extra patterns/Screenshot_20260220_215715_Chrome.jpg
new file mode 100644
index 0000000..d62e1e6
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215715_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215730_Chrome.jpg b/extra patterns/Screenshot_20260220_215730_Chrome.jpg
new file mode 100644
index 0000000..3a7ecb2
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215730_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215751_Chrome(1).jpg b/extra patterns/Screenshot_20260220_215751_Chrome(1).jpg
new file mode 100644
index 0000000..fc614dc
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215751_Chrome(1).jpg differ
diff --git a/extra patterns/Screenshot_20260220_215751_Chrome.jpg b/extra patterns/Screenshot_20260220_215751_Chrome.jpg
new file mode 100644
index 0000000..85eccc1
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215751_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215807_Chrome.jpg b/extra patterns/Screenshot_20260220_215807_Chrome.jpg
new file mode 100644
index 0000000..d1eb164
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215807_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_215814_Chrome.jpg b/extra patterns/Screenshot_20260220_215814_Chrome.jpg
new file mode 100644
index 0000000..879c3ac
Binary files /dev/null and b/extra patterns/Screenshot_20260220_215814_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_220032_Chrome.jpg b/extra patterns/Screenshot_20260220_220032_Chrome.jpg
new file mode 100644
index 0000000..3c553c6
Binary files /dev/null and b/extra patterns/Screenshot_20260220_220032_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231134_Chrome(1).jpg b/extra patterns/Screenshot_20260220_231134_Chrome(1).jpg
new file mode 100644
index 0000000..17657d6
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231134_Chrome(1).jpg differ
diff --git a/extra patterns/Screenshot_20260220_231134_Chrome.jpg b/extra patterns/Screenshot_20260220_231134_Chrome.jpg
new file mode 100644
index 0000000..a93f541
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231134_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231248_Chrome.jpg b/extra patterns/Screenshot_20260220_231248_Chrome.jpg
new file mode 100644
index 0000000..e1bb742
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231248_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231313_Chrome.jpg b/extra patterns/Screenshot_20260220_231313_Chrome.jpg
new file mode 100644
index 0000000..807f2bc
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231313_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231322_Chrome.jpg b/extra patterns/Screenshot_20260220_231322_Chrome.jpg
new file mode 100644
index 0000000..348eb73
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231322_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231332_Chrome.jpg b/extra patterns/Screenshot_20260220_231332_Chrome.jpg
new file mode 100644
index 0000000..06081e8
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231332_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231349_Chrome.jpg b/extra patterns/Screenshot_20260220_231349_Chrome.jpg
new file mode 100644
index 0000000..89577a9
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231349_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231427_Chrome.jpg b/extra patterns/Screenshot_20260220_231427_Chrome.jpg
new file mode 100644
index 0000000..4324968
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231427_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231435_Chrome.jpg b/extra patterns/Screenshot_20260220_231435_Chrome.jpg
new file mode 100644
index 0000000..26e1650
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231435_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231445_Chrome.jpg b/extra patterns/Screenshot_20260220_231445_Chrome.jpg
new file mode 100644
index 0000000..743327b
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231445_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231456_Chrome.jpg b/extra patterns/Screenshot_20260220_231456_Chrome.jpg
new file mode 100644
index 0000000..9b4902e
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231456_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231512_Chrome.jpg b/extra patterns/Screenshot_20260220_231512_Chrome.jpg
new file mode 100644
index 0000000..a5cbeae
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231512_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231529_Chrome.jpg b/extra patterns/Screenshot_20260220_231529_Chrome.jpg
new file mode 100644
index 0000000..9efa5c8
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231529_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231637_Chrome.jpg b/extra patterns/Screenshot_20260220_231637_Chrome.jpg
new file mode 100644
index 0000000..54bc293
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231637_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231644_Chrome.jpg b/extra patterns/Screenshot_20260220_231644_Chrome.jpg
new file mode 100644
index 0000000..cd48d6d
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231644_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231648_Chrome.jpg b/extra patterns/Screenshot_20260220_231648_Chrome.jpg
new file mode 100644
index 0000000..363119d
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231648_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231710_Chrome.jpg b/extra patterns/Screenshot_20260220_231710_Chrome.jpg
new file mode 100644
index 0000000..a2d31cf
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231710_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231739_Chrome.jpg b/extra patterns/Screenshot_20260220_231739_Chrome.jpg
new file mode 100644
index 0000000..8223c8d
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231739_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231752_Chrome.jpg b/extra patterns/Screenshot_20260220_231752_Chrome.jpg
new file mode 100644
index 0000000..6745197
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231752_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231809_Chrome.jpg b/extra patterns/Screenshot_20260220_231809_Chrome.jpg
new file mode 100644
index 0000000..24d5fea
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231809_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231821_Chrome.jpg b/extra patterns/Screenshot_20260220_231821_Chrome.jpg
new file mode 100644
index 0000000..5daf6e2
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231821_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231843_Chrome.jpg b/extra patterns/Screenshot_20260220_231843_Chrome.jpg
new file mode 100644
index 0000000..5a2ad71
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231843_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231903_Chrome.jpg b/extra patterns/Screenshot_20260220_231903_Chrome.jpg
new file mode 100644
index 0000000..45bf449
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231903_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231911_Chrome.jpg b/extra patterns/Screenshot_20260220_231911_Chrome.jpg
new file mode 100644
index 0000000..04725ec
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231911_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231924_Chrome.jpg b/extra patterns/Screenshot_20260220_231924_Chrome.jpg
new file mode 100644
index 0000000..8dd666c
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231924_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231940_Chrome.jpg b/extra patterns/Screenshot_20260220_231940_Chrome.jpg
new file mode 100644
index 0000000..9ff9246
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231940_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_231952_Chrome.jpg b/extra patterns/Screenshot_20260220_231952_Chrome.jpg
new file mode 100644
index 0000000..097b79e
Binary files /dev/null and b/extra patterns/Screenshot_20260220_231952_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_232003_Chrome.jpg b/extra patterns/Screenshot_20260220_232003_Chrome.jpg
new file mode 100644
index 0000000..6b9fb7b
Binary files /dev/null and b/extra patterns/Screenshot_20260220_232003_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_232019_Chrome.jpg b/extra patterns/Screenshot_20260220_232019_Chrome.jpg
new file mode 100644
index 0000000..152465a
Binary files /dev/null and b/extra patterns/Screenshot_20260220_232019_Chrome.jpg differ
diff --git a/extra patterns/Screenshot_20260220_232032_Chrome.jpg b/extra patterns/Screenshot_20260220_232032_Chrome.jpg
new file mode 100644
index 0000000..fdf9f64
Binary files /dev/null and b/extra patterns/Screenshot_20260220_232032_Chrome.jpg differ
diff --git a/extra patterns/frenchtextiles00unse_0024.jpg b/extra patterns/frenchtextiles00unse_0024.jpg
new file mode 100644
index 0000000..69c7361
Binary files /dev/null and b/extra patterns/frenchtextiles00unse_0024.jpg differ
diff --git a/extra patterns/frenchtextiles00unse_0027.jpg b/extra patterns/frenchtextiles00unse_0027.jpg
new file mode 100644
index 0000000..2351ed4
Binary files /dev/null and b/extra patterns/frenchtextiles00unse_0027.jpg differ
diff --git a/extra patterns/frenchtextiles00unse_0028.jpg b/extra patterns/frenchtextiles00unse_0028.jpg
new file mode 100644
index 0000000..ba3ffc2
Binary files /dev/null and b/extra patterns/frenchtextiles00unse_0028.jpg differ
diff --git a/extra patterns/frenchtextiles00unse_0031.jpg b/extra patterns/frenchtextiles00unse_0031.jpg
new file mode 100644
index 0000000..6a82fe3
Binary files /dev/null and b/extra patterns/frenchtextiles00unse_0031.jpg differ
diff --git a/extra patterns/frenchtextiles00unse_0038.jpg b/extra patterns/frenchtextiles00unse_0038.jpg
new file mode 100644
index 0000000..5022552
Binary files /dev/null and b/extra patterns/frenchtextiles00unse_0038.jpg differ
diff --git a/extra patterns/frenchtextiles00unse_0039.jpg b/extra patterns/frenchtextiles00unse_0039.jpg
new file mode 100644
index 0000000..91521cb
Binary files /dev/null and b/extra patterns/frenchtextiles00unse_0039.jpg differ
diff --git a/extra patterns/frenchtextiles00unse_0040.jpg b/extra patterns/frenchtextiles00unse_0040.jpg
new file mode 100644
index 0000000..4491cd8
Binary files /dev/null and b/extra patterns/frenchtextiles00unse_0040.jpg differ
diff --git a/extra patterns/frenchtextiles00unse_0041.jpg b/extra patterns/frenchtextiles00unse_0041.jpg
new file mode 100644
index 0000000..ccec19a
Binary files /dev/null and b/extra patterns/frenchtextiles00unse_0041.jpg differ
diff --git a/extra patterns/frenchtextiles00unse_0043.jpg b/extra patterns/frenchtextiles00unse_0043.jpg
new file mode 100644
index 0000000..c675ab0
Binary files /dev/null and b/extra patterns/frenchtextiles00unse_0043.jpg differ
diff --git a/extra patterns/frenchtextiles00unse_0106.jpg b/extra patterns/frenchtextiles00unse_0106.jpg
new file mode 100644
index 0000000..afb43c1
Binary files /dev/null and b/extra patterns/frenchtextiles00unse_0106.jpg differ
diff --git a/extra patterns/frenchtextiles00unse_0118.jpg b/extra patterns/frenchtextiles00unse_0118.jpg
new file mode 100644
index 0000000..f8d73dd
Binary files /dev/null and b/extra patterns/frenchtextiles00unse_0118.jpg differ
diff --git a/extra patterns/frenchtextiles00unse_0125.jpg b/extra patterns/frenchtextiles00unse_0125.jpg
new file mode 100644
index 0000000..c776453
Binary files /dev/null and b/extra patterns/frenchtextiles00unse_0125.jpg differ
diff --git a/extra patterns/frenchtextiles00unse_0131.jpg b/extra patterns/frenchtextiles00unse_0131.jpg
new file mode 100644
index 0000000..ab96dda
Binary files /dev/null and b/extra patterns/frenchtextiles00unse_0131.jpg differ
diff --git a/extra patterns/frenchtextiles00unse_0136.jpg b/extra patterns/frenchtextiles00unse_0136.jpg
new file mode 100644
index 0000000..14e490c
Binary files /dev/null and b/extra patterns/frenchtextiles00unse_0136.jpg differ
diff --git a/extra patterns/frenchtextiles00unse_0152.jpg b/extra patterns/frenchtextiles00unse_0152.jpg
new file mode 100644
index 0000000..4e38041
Binary files /dev/null and b/extra patterns/frenchtextiles00unse_0152.jpg differ
diff --git a/extra patterns/frenchtextiles00unse_0159.jpg b/extra patterns/frenchtextiles00unse_0159.jpg
new file mode 100644
index 0000000..4334835
Binary files /dev/null and b/extra patterns/frenchtextiles00unse_0159.jpg differ
diff --git a/extra patterns/frenchtextiles00unse_0187.jpg b/extra patterns/frenchtextiles00unse_0187.jpg
new file mode 100644
index 0000000..ee1fc9b
Binary files /dev/null and b/extra patterns/frenchtextiles00unse_0187.jpg differ
diff --git a/extra patterns/wari-textile-1.jpg b/extra patterns/wari-textile-1.jpg
new file mode 100644
index 0000000..9304314
Binary files /dev/null and b/extra patterns/wari-textile-1.jpg differ
diff --git a/extra patterns/wari-textile-10.jpg b/extra patterns/wari-textile-10.jpg
new file mode 100644
index 0000000..f484634
Binary files /dev/null and b/extra patterns/wari-textile-10.jpg differ
diff --git a/extra patterns/wari-textile-12.jpg b/extra patterns/wari-textile-12.jpg
new file mode 100644
index 0000000..b989da2
Binary files /dev/null and b/extra patterns/wari-textile-12.jpg differ
diff --git a/extra patterns/wari-textile-13.jpg b/extra patterns/wari-textile-13.jpg
new file mode 100644
index 0000000..a15a08f
Binary files /dev/null and b/extra patterns/wari-textile-13.jpg differ
diff --git a/extra patterns/wari-textile-6.jpg b/extra patterns/wari-textile-6.jpg
new file mode 100644
index 0000000..0af7047
Binary files /dev/null and b/extra patterns/wari-textile-6.jpg differ
diff --git a/extra patterns/wari-textile-9.jpg b/extra patterns/wari-textile-9.jpg
new file mode 100644
index 0000000..f8955cf
Binary files /dev/null and b/extra patterns/wari-textile-9.jpg differ
diff --git a/extract_colors.py b/extract_colors.py
new file mode 100644
index 0000000..a5620a8
--- /dev/null
+++ b/extract_colors.py
@@ -0,0 +1,34 @@
+import os
+import sys
+from PIL import Image
+from collections import Counter
+import json
+
+def rgb_to_hex(rgb):
+    return '#{:02x}{:02x}{:02x}'.format(rgb[0], rgb[1], rgb[2])
+
+def get_dominant_colors(image_path, num_colors=5):
+    try:
+        img = Image.open(image_path)
+        img = img.convert('RGB')
+        img.thumbnail((100, 100)) # Resize for speed
+        pixels = list(img.getdata())
+        
+        # Simple frequency count
+        counter = Counter(pixels)
+        dominant = counter.most_common(num_colors)
+        
+        return [rgb_to_hex(color) for color, count in dominant]
+    except Exception as e:
+        return []
+
+directory = r"C:\Work\Douglxss\Douglxss Projects\Antigravity\Larkspurs\African Textiles"
+results = {}
+
+for filename in os.listdir(directory):
+    if filename.endswith(".png") or filename.endswith(".jpg"):
+        path = os.path.join(directory, filename)
+        colors = get_dominant_colors(path, 6)
+        results[filename] = colors
+
+print(json.dumps(results, indent=2))
diff --git a/extract_colors_kmeans.py b/extract_colors_kmeans.py
new file mode 100644
index 0000000..b2121b5
--- /dev/null
+++ b/extract_colors_kmeans.py
@@ -0,0 +1,47 @@
+import os
+import sys
+import json
+from PIL import Image
+try:
+    from sklearn.cluster import KMeans
+    import numpy as np
+except ImportError:
+    import subprocess
+    subprocess.check_call([sys.executable, "-m", "pip", "install", "scikit-learn", "numpy"])
+    from sklearn.cluster import KMeans
+    import numpy as np
+
+def rgb_to_hex(rgb):
+    return '#{:02x}{:02x}{:02x}'.format(int(rgb[0]), int(rgb[1]), int(rgb[2]))
+
+def get_kmeans_colors(image_path, num_colors=4):
+    try:
+        img = Image.open(image_path)
+        img = img.convert('RGB')
+        img.thumbnail((150, 150))
+        img_array = np.array(img)
+        pixels = img_array.reshape(-1, 3)
+        
+        # Filter out extreme blacks/whites to find the actual dyes
+        filtered_pixels = [p for p in pixels if not (p[0]<20 and p[1]<20 and p[2]<20) and not (p[0]>240 and p[1]>240 and p[2]>240)]
+        if len(filtered_pixels) < num_colors:
+            filtered_pixels = pixels
+            
+        kmeans = KMeans(n_clusters=num_colors, random_state=42, n_init=10)
+        kmeans.fit(filtered_pixels)
+        colors = kmeans.cluster_centers_
+        
+        return [rgb_to_hex(c) for c in colors]
+    except Exception as e:
+        return [str(e)]
+
+directory = r"C:\Work\Douglxss\Douglxss Projects\Antigravity\Larkspurs\African Textiles"
+results = {}
+
+for filename in os.listdir(directory):
+    if filename.endswith(".png") or filename.endswith(".jpg"):
+        path = os.path.join(directory, filename)
+        colors = get_kmeans_colors(path, 5)
+        results[filename] = colors
+
+print(json.dumps(results, indent=2))
diff --git a/js/larkspur-core.js b/js/larkspur-core.js
new file mode 100644
index 0000000..72c6592
--- /dev/null
+++ b/js/larkspur-core.js
@@ -0,0 +1,541 @@
+// ── CORE DATA & CONFIG ────────────────────────────────────────────────────────
+// Centralized configuration and math helpers used by workers and UI
+
+// ── Chromes (cohesive families from catalog color plates + Near Eastern dyes) ──
+// All hex values extracted from gallery color plates via pixel sampling.
+// r = relative weight (higher = more frequent), v = variant accent flag.
+var PALETTES = {
+    panar: [  // hemp canvas — warm parchment family
+        { c: "#f8e8d0", r: 2 },       // lightest parchment
+        { c: "#f8e0a0", r: 2 },       // golden parchment
+        { c: "#c8a060", r: 3 },       // warm gold
+        { c: "#886830", r: 2 },       // golden brown
+        { c: "#584020", r: 2 },       // dark umber
+        { c: "#883028", r: 1, v: 1 }, // sienna accent
+        { c: "#201810", r: 3 }        // near-black
+    ],
+    rutsya: [  // red thread — the dominant embroidery reds
+        { c: "#c82828", r: 3 },       // vermillion
+        { c: "#883028", r: 2 },       // dark sienna
+        { c: "#b84848", r: 2 },       // brick red
+        { c: "#f05858", r: 1 },       // coral highlight
+        { c: "#f8e0a0", r: 1, v: 1 }, // parchment accent
+        { c: "#681818", r: 2 },       // blood dark
+        { c: "#480810", r: 2 },       // near-black crimson
+        { c: "#201810", r: 2 }        // deep dark
+    ],
+    tekhelet: [  // indigo — Judean desert blue dye family
+        { c: "#505878", r: 3 },       // slate blue
+        { c: "#101840", r: 3 },       // midnight blue
+        { c: "#707088", r: 2 },       // blue-gray
+        { c: "#8890a0", r: 2 },       // cool steel
+        { c: "#c8a060", r: 1, v: 1 }, // warm gold accent
+        { c: "#303028", r: 2 },       // dark olive
+        { c: "#201810", r: 2 }        // deep dark
+    ],
+    argaman: [  // Tyrian purple — murex shell dye, the most precious
+        { c: "#b02040", r: 3 },       // wine/crimson
+        { c: "#505878", r: 2 },       // slate blue
+        { c: "#883028", r: 2 },       // dark sienna
+        { c: "#f05858", r: 1, v: 1 }, // coral accent
+        { c: "#d89890", r: 1 },       // dusty rose
+        { c: "#101840", r: 3 },       // midnight
+        { c: "#480810", r: 2 }        // near-black
+    ],
+    karkom: [  // saffron gold — crocus dye, warm ceremonial
+        { c: "#c8a060", r: 3 },       // warm gold
+        { c: "#886830", r: 3 },       // golden brown
+        { c: "#f8e0a0", r: 2 },       // parchment light
+        { c: "#b07060", r: 2 },       // terracotta
+        { c: "#f83038", r: 1, v: 1 }, // scarlet accent
+        { c: "#584020", r: 2 },       // umber
+        { c: "#303028", r: 2 }        // dark
+    ],
+    madder: [  // rubia root — Samarian red earth dye
+        { c: "#b84848", r: 3 },       // brick red
+        { c: "#d07870", r: 2 },       // dusty rose
+        { c: "#b07060", r: 2 },       // terracotta
+        { c: "#f0b898", r: 1 },       // peach
+        { c: "#f8e0a0", r: 1, v: 1 }, // parchment accent
+        { c: "#584020", r: 2 },       // dark umber
+        { c: "#681818", r: 2 },       // dark blood
+        { c: "#201810", r: 2 }        // near-black
+    ],
+    murex: [  // royal sea snail — crimson + midnight deep
+        { c: "#b02040", r: 3 },       // wine
+        { c: "#c82828", r: 2 },       // vermillion
+        { c: "#101840", r: 3 },       // midnight blue
+        { c: "#505878", r: 2 },       // slate
+        { c: "#c8a060", r: 1, v: 1 }, // gold accent
+        { c: "#480810", r: 3 },       // near-black crimson
+        { c: "#201810", r: 2 }        // deep dark
+    ],
+    kelme: [  // ink + parchment — the catalog page itself
+        { c: "#303028", r: 3 },       // dark ink
+        { c: "#585048", r: 2 },       // warm gray
+        { c: "#f8e8d0", r: 3 },       // light parchment
+        { c: "#c0b890", r: 2 },       // warm sage
+        { c: "#c82828", r: 1, v: 1 }, // red accent
+        { c: "#908878", r: 2 },       // mid gray
+        { c: "#201810", r: 2 }        // near-black
+    ],
+    viride: [  // green earth — forest dye from the color plates
+        { c: "#306038", r: 3 },       // forest green
+        { c: "#686858", r: 2 },       // olive gray
+        { c: "#886830", r: 2 },       // golden brown
+        { c: "#a0a088", r: 2 },       // sage
+        { c: "#f05858", r: 1, v: 1 }, // coral accent
+        { c: "#584020", r: 2 },       // umber
+        { c: "#201810", r: 3 }        // deep dark
+    ],
+    kokkos: [  // scarlet worm — kermes insect dye (Greek: kokkos)
+        { c: "#f83038", r: 3 },       // bright scarlet
+        { c: "#f05858", r: 2 },       // coral
+        { c: "#b02040", r: 2 },       // wine
+        { c: "#d89890", r: 1 },       // dusty rose
+        { c: "#c8a060", r: 1, v: 1 }, // gold accent
+        { c: "#883028", r: 2 },       // dark sienna
+        { c: "#303028", r: 3 }        // dark
+    ],
+    shahar: [  // dawn (Hebrew) — Salmon dominant
+        { c: "#f38e82", r: 3 },       // salmon
+        { c: "#e9967a", r: 2 },       // dark salmon
+        { c: "#f5e6d3", r: 2 },       // cream
+        { c: "#d89890", r: 2 },       // dusty rose
+        { c: "#45b698", r: 1, v: 1 }, // teal accent
+        { c: "#883028", r: 2 },       // sienna
+        { c: "#303028", r: 3 }        // dark
+    ],
+    gehelet: [  // ember (Hebrew) — Burnt Umber dominant
+        { c: "#8a3324", r: 3 },       // burnt umber
+        { c: "#5c3a2e", r: 2 },       // dark brown
+        { c: "#c58b5b", r: 2 },       // golden brown
+        { c: "#a8613f", r: 2 },       // rust
+        { c: "#f2c28a", r: 1, v: 1 }, // gold accent
+        { c: "#4a2f23", r: 2 },       // deep brown
+        { c: "#201810", r: 3 }        // near-black
+    ],
+    tarshish: [  // beryl/sea-stone (Hebrew) — Aqua dominant
+        { c: "#45b698", r: 3 },       // aqua / teal
+        { c: "#20b2aa", r: 2 },       // light sea green
+        { c: "#008080", r: 2 },       // teal
+        { c: "#6c8a7a", r: 2 },       // sage
+        { c: "#f05858", r: 1, v: 1 }, // coral accent
+        { c: "#2f4f4f", r: 2 },       // dark slate
+        { c: "#101820", r: 3 }        // dark
+    ],
+
+    // ── HyperLoom Origins palettes ──────────────────────────────────────────
+
+    pepel: [  // ash (Erzya) — White Mono
+        { c: "#F3F5F6", r: 2 },       // off-white
+        { c: "#D8DCDF", r: 2 },       // very light gray
+        { c: "#A7ADB2", r: 3 },       // light gray
+        { c: "#6A7177", r: 2 },       // medium gray
+        { c: "#353B41", r: 2 },       // dark gray
+        { c: "#0F1316", r: 3 }        // near-black
+    ],
+    katan: [  // fine linen (Hebrew) — White on Cream
+        { c: "#FAF8F3", r: 2 },       // pure cream
+        { c: "#F3F5F6", r: 2 },       // off-white
+        { c: "#D8DCDF", r: 2 },       // light gray
+        { c: "#A7ADB2", r: 2 },       // medium gray
+        { c: "#6A7177", r: 2 },       // steel
+        { c: "#C6543F", r: 1, v: 1 }, // coral accent
+        { c: "#0F1316", r: 3 }        // near-black
+    ],
+    wari: [  // Andean patchwork (Peru)
+        { c: "#102848", r: 3 },       // indigo/navy background
+        { c: "#883028", r: 3 },       // rust/terracotta red
+        { c: "#606838", r: 2 },       // olive/moss green
+        { c: "#D8B048", r: 2 },       // golden/ochre yellow
+        { c: "#E8D8B8", r: 1 },       // off-white/cream
+        { c: "#B84830", r: 1, v: 1 }, // brighter brick red
+        { c: "#201818", r: 2 }        // near black
+    ],
+    navajo: [  // Southwestern rug bands
+        { c: "#101838", r: 3 },       // deep midnight navy
+        { c: "#A01820", r: 3 },       // madder red / cardinal
+        { c: "#C8A880", r: 2 },       // sand/tan
+        { c: "#F0E8D8", r: 1 },       // pale gray/white
+        { c: "#B03038", r: 1, v: 1 }, // brick accent
+        { c: "#282828", r: 3 }        // charcoal/black
+    ],
+    tsemer: [  // soft wool (Hebrew) — Sorbet Grid
+        { c: "#FFE7E0", r: 2 },       // peach
+        { c: "#FFD6A8", r: 2 },       // apricot
+        { c: "#FFF0B6", r: 2 },       // light yellow
+        { c: "#D6F0BA", r: 2 },       // light green
+        { c: "#E7F5BE", r: 2 },       // mint cream
+        { c: "#F2E8C6", r: 2 },       // cream
+        { c: "#F9B1C8", r: 1 },       // pink
+        { c: "#F4836A", r: 1, v: 1 }, // coral accent
+        { c: "#6E7C8F", r: 2 },       // slate
+        { c: "#201F25", r: 3 }        // dark
+    ],
+    porphyra: [  // purple-shell (Greek) — Shell and Coral
+        { c: "#F8F4ED", r: 2 },       // very light shell
+        { c: "#F2EFEA", r: 2 },       // shell
+        { c: "#B8B8B9", r: 2 },       // light gray
+        { c: "#FA9A86", r: 1 },       // coral pink
+        { c: "#E56459", r: 1, v: 1 }, // coral red accent
+        { c: "#C84C7A", r: 1 },       // pink
+        { c: "#C8B779", r: 1 },       // tan
+        { c: "#6E6E71", r: 2 },       // medium gray
+        { c: "#3A3A3C", r: 2 },       // dark gray
+        { c: "#171718", r: 3 }        // near-black
+    ],
+    kaspi: [  // silver (Erzya) — Slate Fog
+        { c: "#EAEDEF", r: 2 },       // off-white
+        { c: "#C6CCD1", r: 2 },       // light gray
+        { c: "#98A1A8", r: 2 },       // cool gray
+        { c: "#6D7880", r: 2 },       // medium
+        { c: "#49545B", r: 2 },       // dark medium
+        { c: "#F2C14E", r: 1, v: 1 }, // yellow accent
+        { c: "#D96B5F", r: 1 },       // coral
+        { c: "#2E363B", r: 2 },       // charcoal
+        { c: "#1F2529", r: 2 },       // dark
+        { c: "#111416", r: 3 }        // near-black
+    ],
+    tukmo: [  // brick/earthen (Erzya) — Baked
+        { c: "#F8E3C4", r: 1 },       // cream
+        { c: "#F2C28A", r: 2 },       // sand
+        { c: "#E89A5C", r: 2 },       // light terracotta
+        { c: "#CF7B4A", r: 3 },       // terracotta
+        { c: "#A8613F", r: 2 },       // rust
+        { c: "#7A4A33", r: 2 },       // brown
+        { c: "#6C8A7A", r: 1, v: 1 }, // sage accent
+        { c: "#3F5C57", r: 1 },       // dark teal
+        { c: "#4A2F23", r: 2 },       // dark brown
+        { c: "#2A1C17", r: 3 }        // near-black
+    ],
+    barzel: [  // iron (Hebrew) — Politique
+        { c: "#F3F5F6", r: 1 },       // off-white
+        { c: "#D4D8DC", r: 2 },       // light gray
+        { c: "#9AA2AA", r: 2 },       // cool gray
+        { c: "#6E7882", r: 2 },       // medium
+        { c: "#4B555F", r: 2 },       // dark medium
+        { c: "#CE3E3E", r: 1, v: 1 }, // red accent
+        { c: "#A46F38", r: 1 },       // ochre
+        { c: "#2F363D", r: 2 },       // charcoal
+        { c: "#1E2126", r: 2 },       // dark
+        { c: "#121417", r: 3 }        // near-black
+    ],
+    adama: [  // earth/ground (Hebrew) — Southwest
+        { c: "#F5E6D3", r: 1 },       // cream
+        { c: "#E8C4B0", r: 2 },       // sand
+        { c: "#D4A58F", r: 2 },       // dusty rose
+        { c: "#B87A65", r: 2 },       // terra
+        { c: "#8B5A47", r: 3 },       // brown
+        { c: "#C58B5B", r: 1 },       // golden brown
+        { c: "#A86B3D", r: 1 },       // rust
+        { c: "#6B8E7A", r: 1, v: 1 }, // sage accent
+        { c: "#4A7C6B", r: 1 },       // teal
+        { c: "#5C3A2E", r: 2 },       // dark
+        { c: "#2A1F1A", r: 3 }        // near-black
+    ],
+    shemen: [  // olive oil (Hebrew) — Alentejo
+        { c: "#F0EAD6", r: 2 },       // warm cream
+        { c: "#F2E394", r: 2 },       // golden yellow
+        { c: "#D3B99A", r: 2 },       // sand
+        { c: "#C1D37F", r: 2 },       // spring green
+        { c: "#A57C58", r: 2 },       // earth
+        { c: "#708238", r: 2, v: 1 }, // olive accent
+        { c: "#6C4E3C", r: 2 },       // dark brown
+        { c: "#3E2F23", r: 3 }        // near-black
+    ],
+    yam: [  // sea (Hebrew) — St. Croix
+        { c: "#FEDBDD", r: 2 },       // light pink
+        { c: "#F9BCB8", r: 3 },       // salmon
+        { c: "#45B698", r: 2, v: 1 }, // teal accent
+        { c: "#848582", r: 2 },       // gray
+        { c: "#010650", r: 3 }        // deep navy
+    ],
+    erev: [  // evening (Hebrew) — Monterey Drive
+        { c: "#F4E1D2", r: 1 },       // cream
+        { c: "#D9B382", r: 2 },       // sand
+        { c: "#A7C6ED", r: 2 },       // sky blue
+        { c: "#5386A6", r: 2 },       // ocean
+        { c: "#7E5A9B", r: 2 },       // purple
+        { c: "#E63946", r: 1, v: 1 }, // red accent
+        { c: "#F28F3B", r: 1 },       // orange
+        { c: "#2A2D34", r: 3 }        // dark
+    ],
+    alon: [  // oak (Hebrew) — Monticello
+        { c: "#F8F4E3", r: 1 },       // cream
+        { c: "#DCC48E", r: 2 },       // golden
+        { c: "#A58F6F", r: 3 },       // warm brown
+        { c: "#7DA5B3", r: 2 },       // blue-gray
+        { c: "#4B6C70", r: 2 },       // teal
+        { c: "#B3595C", r: 1, v: 1 }, // brick red accent
+        { c: "#6C584C", r: 2 },       // dark brown
+        { c: "#3A2F2C", r: 3 }        // near-black
+    ],
+    afarsek: [  // peach (Hebrew) — Peachtree
+        { c: "#FFE5D4", r: 1 },       // lightest peach
+        { c: "#FFB692", r: 2 },       // peach
+        { c: "#A3C4BC", r: 2 },       // sage
+        { c: "#F9D949", r: 1 },       // yellow
+        { c: "#FF7F51", r: 2 },       // orange
+        { c: "#6A8D73", r: 2 },       // forest
+        { c: "#E63946", r: 1, v: 1 }, // red accent
+        { c: "#3E3E3E", r: 3 }        // dark
+    ],
+    shoshan: [  // lily/rose (Hebrew) — Dogwood Bloom
+        { c: "#FFF0F5", r: 1 },       // lavender blush
+        { c: "#F9C6C9", r: 2 },       // light pink
+        { c: "#C6E2FF", r: 2 },       // light blue
+        { c: "#E69FAF", r: 2 },       // pink
+        { c: "#D86C82", r: 2 },       // rose
+        { c: "#A65D7B", r: 2 },       // mauve
+        { c: "#7E4A5E", r: 2, v: 1 }, // dark mauve accent
+        { c: "#4C516D", r: 3 }        // slate
+    ],
+    neshef: [  // twilight (Hebrew) — Baja Sur
+        { c: "#FFFCF2", r: 1 },       // near-white
+        { c: "#FFE6A7", r: 2 },       // light gold
+        { c: "#FFB857", r: 2 },       // amber
+        { c: "#F27649", r: 2 },       // orange
+        { c: "#D94F3D", r: 2 },       // red
+        { c: "#1B998B", r: 1, v: 1 }, // teal accent
+        { c: "#6A0572", r: 2 },       // deep purple
+        { c: "#2D3047", r: 3 }        // dark
+    ],
+    bahir: [  // luminous (Hebrew) — Miracle Mile
+        { c: "#F3E9D2", r: 1 },       // cream
+        { c: "#FFB400", r: 1 },       // gold
+        { c: "#FF9F1C", r: 1 },       // amber
+        { c: "#C7F464", r: 1 },       // lime
+        { c: "#4ECDC4", r: 2 },       // teal
+        { c: "#FF6B6B", r: 1, v: 1 }, // coral accent
+        { c: "#556270", r: 2 },       // slate
+        { c: "#2A2A2A", r: 3 }        // dark
+    ],
+    gan: [  // garden (Hebrew) — Belvedere Park
+        { c: "#E9F1E9", r: 1 },       // pale green
+        { c: "#F4E1D2", r: 1 },       // cream
+        { c: "#D9E4DD", r: 2 },       // soft sage
+        { c: "#A7C4A0", r: 2 },       // green
+        { c: "#8C7A66", r: 2 },       // warm gray
+        { c: "#5C8D62", r: 2, v: 1 }, // forest accent
+        { c: "#3A5F3B", r: 2 },       // dark green
+        { c: "#2E2E2E", r: 3 }        // dark
+    ],
+    zahav: [  // gold (Hebrew) — Luxe
+        { c: "#F1FAEE", r: 1 },       // near-white
+        { c: "#FFD166", r: 2 },       // gold
+        { c: "#F4A261", r: 2 },       // apricot
+        { c: "#4ECDC4", r: 2 },       // teal
+        { c: "#06FFA5", r: 1, v: 1 }, // neon green accent
+        { c: "#2A9D8F", r: 2 },       // dark teal
+        { c: "#FF6B57", r: 1 },       // coral
+        { c: "#5B85AA", r: 2 },       // steel blue
+        { c: "#E63946", r: 1 },       // red
+        { c: "#264653", r: 3 }        // dark teal
+    ],
+    simcha: [  // joy/festival (Hebrew) — Happy Socks
+        { c: "#F6F3E8", r: 1 },       // warm white
+        { c: "#E8E2CF", r: 2 },       // cream
+        { c: "#86AB2E", r: 2 },       // lime green
+        { c: "#F05A28", r: 2 },       // orange
+        { c: "#F0CA00", r: 2, v: 1 }, // yellow accent
+        { c: "#53B36A", r: 2 },       // green
+        { c: "#ED6BA7", r: 1 },       // pink
+        { c: "#C36B4C", r: 2 },       // brown
+        { c: "#603F3A", r: 2 },       // dark brown
+        { c: "#121212", r: 3 }        // near-black
+    ],
+    ruach: [  // wind/spirit (Hebrew) — Bay City Breeze
+        { c: "#87CEEB", r: 3 },       // sky blue
+        { c: "#FFE4B5", r: 2 },       // moccasin
+        { c: "#98D8C8", r: 2 },       // mint
+        { c: "#F7DC6F", r: 2 },       // yellow
+        { c: "#4FC3F7", r: 2 },       // light blue
+        { c: "#FF69B4", r: 1, v: 1 }, // hot pink accent
+        { c: "#BB8FCE", r: 2 },       // lavender
+        { c: "#F8BBD0", r: 2 }        // light pink
+    ],
+    nana: [  // mint (Hebrew/Arabic) — Mint (Genesis)
+        { c: "#E0F5E0", r: 2 },       // lightest mint
+        { c: "#B8E8C8", r: 3 },       // mint green
+        { c: "#90D8B0", r: 2 },       // medium mint
+        { c: "#60C890", r: 2 },       // green
+        { c: "#F0E880", r: 1, v: 1 }, // yellow accent
+        { c: "#C8E0A0", r: 2 },       // lime
+        { c: "#387050", r: 2 },       // dark green
+        { c: "#203828", r: 3 }        // near-black
+    ],
+
+    // ── Japanese Traditional Palettes ─────────────────────────────────────────
+
+    kinjiki: [  // Imperial Reds & Purples (Forbidden Colors)
+        { c: "#FF4E20", r: 1 },       // Otan / Orange
+        { c: "#C91F37", r: 2 },       // Karakurenai / Crimson
+        { c: "#7E2639", r: 1 },       // Su'o / Sappanwood
+        { c: "#2B2028", r: 3 },       // Shikon / Blue-violet
+        { c: "#5D3F6A", r: 4 },       // Kikyo / Bellflower (Boosted)
+        { c: "#DB5A6B", r: 1, v: 1 }, // Kobai / Red plum accent
+        { c: "#F5E6D3", r: 1 }        // Cream contrast
+    ],
+    shibui: [  // Earthy & Astringent (Permissible Colors)
+        { c: "#264348", r: 3 },       // Ai-iro / Indigo
+        { c: "#656255", r: 2 },       // Rikyu-nezumi / Greyish dark green
+        { c: "#A13D2D", r: 2 },       // Edocha / Red-brown
+        { c: "#593A27", r: 2 },       // Susutake / Weathered bamboo
+        { c: "#8B7D3A", r: 1 },       // Koke / Moss accent
+        { c: "#B9A193", r: 2 },       // Shironezumi / White mouse
+        { c: "#F8F8F8", r: 1, v: 1 }  // Bright White (Larkspur accent)
+    ],
+    seigaiha: [  // The Sea & Waves
+        { c: "#003171", r: 3 },       // Konjo / Prussian blue
+        { c: "#1B294B", r: 3 },       // Rurikon / Lapis dark blue
+        { c: "#48929B", r: 2 },       // Asagi / Light blue
+        { c: "#5D8CAE", r: 2 },       // Gunjo / Ultramarine
+        { c: "#A5BA93", r: 1 },       // Byakuroku / Whitish green accent
+        { c: "#FFDDCA", r: 1 },       // Shironeri / Unbleached silk
+        { c: "#FFFFFF", r: 1, v: 1 }  // Pure White (Larkspur foam accent)
+    ],
+
+    // ── African Textiles ──────────────────────────────────────────────────────
+
+    bogolan: [  // Bamana Mudcloth
+        { c: "#272013", r: 3 },       // Dark Mud
+        { c: "#a9a599", r: 2 },       // Bleached Cotton
+        { c: "#8c7c57", r: 2 },       // Ochre Mud
+        { c: "#4b3f23", r: 2 },       // Wet Earth
+        { c: "#392f1b", r: 3 }        // Deep Brown
+    ],
+    adire: [  // Yoruba Indigo Resist Dye
+        { c: "#1b1c1d", r: 3 },       // Deep Indigo Black
+        { c: "#333f44", r: 3 },       // Dark Slate Blue
+        { c: "#4a5b61", r: 2 },       // Faded Indigo
+        { c: "#6f7471", r: 2 },       // Greyed Blue
+        { c: "#928d82", r: 1, v: 1 }  // White Resist Dye Accent
+    ],
+    kuba: [  // Ngongo Raffia
+        { c: "#aaa59d", r: 2 },       // Natural Light Raffia
+        { c: "#816a5b", r: 2 },       // Warm Woven Tan
+        { c: "#713417", r: 2 },       // Terracotta Dye
+        { c: "#502716", r: 2 },       // Russet Brown
+        { c: "#311a12", r: 3 }        // Dark Woven Brown
+    ],
+    kente: [  // Asante / Ewe Woven Colors
+        { c: "#272018", r: 3 },       // Dark Woven Bark
+        { c: "#a29a8b", r: 2 },       // Light Woven Thread
+        { c: "#8b4513", r: 2, v: 1 }, // Vibrant Rust/Ochre Accent
+        { c: "#683c1c", r: 2 },       // Warm Earth Red
+        { c: "#4b351b", r: 2 }        // Deep Brown
+    ]
+};
+
+var PEN_MAP = {
+    "#008080": "153 Cobalt Turquoise", "#010650": "247 Indanthrene Blue", "#06FFA5": "174 Chromium Green Opaque",
+    "#0F1316": "199 Black", "#101820": "161 Phthalo Green", "#101840": "247 Indanthrene Blue", "#111416": "199 Black",
+    "#121212": "199 Black", "#121417": "199 Black", "#171718": "161 Phthalo Green", "#1B998B": "153 Cobalt Turquoise",
+    "#1E2126": "175 Dark Sepia", "#1F2529": "175 Dark Sepia", "#201810": "175 Dark Sepia", "#201F25": "175 Dark Sepia",
+    "#203828": "161 Phthalo Green", "#20B2AA": "153 Cobalt Turquoise", "#264653": "Micron Hunter Green",
+    "#2A1C17": "175 Dark Sepia", "#2A1F1A": "175 Dark Sepia", "#2A2A2A": "175 Dark Sepia", "#2A2D34": "175 Dark Sepia",
+    "#2A9D8F": "174 Chromium Green Opaque", "#2D3047": "175 Dark Sepia", "#2E2E2E": "175 Dark Sepia",
+    "#2E363B": "175 Dark Sepia", "#2F363D": "175 Dark Sepia", "#2F4F4F": "Micron Hunter Green", "#303028": "175 Dark Sepia",
+    "#306038": "Micron Hunter Green", "#353B41": "175 Dark Sepia", "#387050": "Micron Hunter Green",
+    "#3A2F2C": "175 Dark Sepia", "#3A3A3C": "175 Dark Sepia", "#3A5F3B": "Micron Hunter Green", "#3E2F23": "175 Dark Sepia",
+    "#3E3E3E": "177 Walnut Brown", "#3F5C57": "Micron Hunter Green", "#45B698": "174 Chromium Green Opaque",
+    "#480810": "157 Dark Indigo", "#49545B": "Micron Hunter Green", "#4A2F23": "175 Dark Sepia",
+    "#4A7C6B": "235 Cold Grey VI", "#4B555F": "235 Cold Grey VI", "#4B6C70": "235 Cold Grey VI",
+    "#4C516D": "235 Cold Grey VI", "#4ECDC4": "146 Sky Blue", "#4FC3F7": "146 Sky Blue", "#505878": "235 Cold Grey VI",
+    "#5386A6": "233 Cold Grey IV", "#53B36A": "174 Chromium Green Opaque", "#556270": "235 Cold Grey VI",
+    "#584020": "177 Walnut Brown", "#585048": "177 Walnut Brown", "#5B85AA": "233 Cold Grey IV",
+    "#5C3A2E": "177 Walnut Brown", "#5C8D62": "170 May Green", "#603F3A": "177 Walnut Brown",
+    "#60C890": "174 Chromium Green Opaque", "#681818": "Micron Burgundy", "#686858": "235 Cold Grey VI",
+    "#6A0572": "Micron Purple", "#6A7177": "235 Cold Grey VI", "#6A8D73": "233 Cold Grey IV", "#6B8E7A": "233 Cold Grey IV",
+    "#6C4E3C": "177 Walnut Brown", "#6C584C": "235 Cold Grey VI", "#6C8A7A": "233 Cold Grey IV",
+    "#6D7880": "233 Cold Grey IV", "#6E6E71": "235 Cold Grey VI", "#6E7882": "233 Cold Grey IV",
+    "#6E7C8F": "233 Cold Grey IV", "#707088": "233 Cold Grey IV", "#708238": "167 Permanent Green Olive",
+    "#7A4A33": "177 Walnut Brown", "#7DA5B3": "232 Cold Grey III", "#7E4A5E": "235 Cold Grey VI",
+    "#7E5A9B": "233 Cold Grey IV", "#848582": "233 Cold Grey IV", "#86AB2E": "268 Green Gold",
+    "#87CEEB": "146 Sky Blue", "#883028": "Micron Brown", "#886830": "180 Raw Umber", "#8890A0": "233 Cold Grey IV",
+    "#8A3324": "Micron Brown", "#8B5A47": "180 Raw Umber", "#8C7A66": "180 Raw Umber", "#908878": "233 Cold Grey IV",
+    "#90D8B0": "171 Light Green", "#98A1A8": "232 Cold Grey III", "#98D8C8": "146 Sky Blue", "#9AA2AA": "232 Cold Grey III",
+    "#A0A088": "232 Cold Grey III", "#A3C4BC": "232 Cold Grey III", "#A46F38": "180 Raw Umber",
+    "#A57C58": "180 Raw Umber", "#A58F6F": "180 Raw Umber", "#A65D7B": "180 Raw Umber", "#A7ADB2": "232 Cold Grey III",
+    "#A7C4A0": "232 Cold Grey III", "#A7C6ED": "146 Sky Blue", "#A8613F": "180 Raw Umber", "#A86B3D": "180 Raw Umber",
+    "#B02040": "134 Crimson", "#B07060": "180 Raw Umber", "#B3595C": "192 Indian Red", "#B84848": "192 Indian Red",
+    "#B87A65": "192 Indian Red", "#B8B8B9": "232 Cold Grey III", "#B8E8C8": "272 Warm Grey III",
+    "#BB8FCE": "125 Middle Purple Pink", "#C0B890": "232 Cold Grey III", "#C1D37F": "171 Light Green",
+    "#C36B4C": "192 Indian Red", "#C58B5B": "186 Terracotta", "#C6543F": "192 Indian Red", "#C6CCD1": "272 Warm Grey III",
+    "#C6E2FF": "154 Light Cobalt Turquoise", "#C7F464": "171 Light Green", "#C82828": "121 Pale Geranium Lake",
+    "#C84C7A": "192 Indian Red", "#C8A060": "268 Green Gold", "#C8B779": "232 Cold Grey III",
+    "#C8E0A0": "272 Warm Grey III", "#CE3E3E": "121 Pale Geranium Lake", "#CF7B4A": "186 Terracotta",
+    "#D07870": "186 Terracotta", "#D3B99A": "232 Cold Grey III", "#D4A58F": "232 Cold Grey III",
+    "#D4D8DC": "272 Warm Grey III", "#D6F0BA": "272 Warm Grey III", "#D86C82": "186 Terracotta",
+    "#D89890": "232 Cold Grey III", "#D8DCDF": "272 Warm Grey III", "#D94F3D": "192 Indian Red",
+    "#D96B5F": "186 Terracotta", "#D9B382": "232 Cold Grey III", "#D9E4DD": "272 Warm Grey III",
+    "#DCC48E": "232 Cold Grey III", "#E0F5E0": "104 Light Yellow Glaze", "#E56459": "186 Terracotta",
+    "#E63946": "121 Pale Geranium Lake", "#E69FAF": "Micron Rose", "#E7F5BE": "104 Light Yellow Glaze",
+    "#E89A5C": "186 Terracotta", "#E8C4B0": "Micron Rose", "#E8E2CF": "272 Warm Grey III", "#E9967A": "186 Terracotta",
+    "#E9F1E9": "103 Ivory", "#EAEDEF": "103 Ivory", "#ED6BA7": "127 Pink Carmine", "#F05858": "186 Terracotta",
+    "#F05A28": "121 Pale Geranium Lake", "#F0B898": "Micron Rose", "#F0CA00": "109 Dark Chrome Yellow",
+    "#F0E880": "104 Light Yellow Glaze", "#F0EAD6": "104 Light Yellow Glaze", "#F1FAEE": "103 Ivory",
+    "#F27649": "186 Terracotta", "#F28F3B": "186 Terracotta", "#F2C14E": "268 Green Gold", "#F2C28A": "Micron Rose",
+    "#F2E394": "Micron Rose", "#F2E8C6": "104 Light Yellow Glaze", "#F2EFEA": "103 Ivory", "#F38E82": "186 Terracotta",
+    "#F3E9D2": "104 Light Yellow Glaze", "#F3F5F6": "103 Ivory", "#F4836A": "186 Terracotta", "#F4A261": "186 Terracotta",
+    "#F4E1D2": "104 Light Yellow Glaze", "#F5E6D3": "104 Light Yellow Glaze", "#F6F3E8": "103 Ivory",
+    "#F7DC6F": "268 Green Gold", "#F83038": "121 Pale Geranium Lake", "#F8BBD0": "Micron Rose", "#F8E0A0": "Micron Rose",
+    "#F8E3C4": "104 Light Yellow Glaze", "#F8E8D0": "104 Light Yellow Glaze", "#F8F4E3": "104 Light Yellow Glaze",
+    "#F8F4ED": "103 Ivory", "#F9B1C8": "Micron Rose", "#F9BCB8": "Micron Rose", "#F9C6C9": "Micron Rose",
+    "#F9D949": "107 Cadmium Yellow", "#FA9A86": "186 Terracotta", "#FAF8F3": "103 Ivory", "#FEDBDD": "Micron Rose",
+    "#FF69B4": "127 Pink Carmine", "#FF6B57": "186 Terracotta", "#FF6B6B": "186 Terracotta", "#FF7F51": "186 Terracotta",
+    "#FF9F1C": "113 Orange Glaze", "#FFB400": "109 Dark Chrome Yellow", "#FFB692": "Micron Rose",
+    "#FFB857": "186 Terracotta", "#FFD166": "268 Green Gold", "#FFD6A8": "Micron Rose",
+    "#FFE4B5": "104 Light Yellow Glaze", "#FFE5D4": "104 Light Yellow Glaze", "#FFE6A7": "104 Light Yellow Glaze",
+    "#FFE7E0": "104 Light Yellow Glaze", "#FFF0B6": "104 Light Yellow Glaze", "#FFF0F5": "103 Ivory",
+    "#FFFCF2": "103 Ivory"
+};
+
+var SYMMETRY_OPTS = ['random', 'none', 'vertical', 'horizontal', 'both', 'diagonal', 'rotational', 'quad', 'kaleidoscope', 'glide', 'radial'];
+var SPACE_OPTS = ['euclidean', 'noneuclidean'];
+var CHROME_OPTS = ['random', 'kokkos', 'murex', 'tukmo', 'rutsya', 'zahav', 'karkom', 'viride', 'gan', 'tarshish', 'tekhelet', 'yam', 'porphyra', 'shoshan', 'pepel', 'panar', 'katan', 'kaspi', 'barzel', 'wari', 'navajo', 'kinjiki', 'shibui', 'seigaiha', 'bogolan', 'adire', 'kuba', 'kente'];
+var ENGINE_OPTS = ['random', 'adama', 'adire', 'argyle', 'bogolan', 'chipaz', 'interlace', 'kente', 'kepe', 'kishtima', 'kolya', 'kshtir', 'kuba', 'kudo', 'mastor', 'narmuny', 'navajo', 'pakshats', 'panks', 'pulay', 'seigaiha', 'sermat', 'sermat-kudo', 'tol', 'ved', 'virma', 'wari'];
+var CANVAS_OPTS = ['black', 'tan', 'olive', 'white'];
+
+var CHROME_DESCS = {
+    'random': 'Random', 'adama': 'Earth / Southwest', 'afarsek': 'Peach', 'alon': 'Oak',
+    'argaman': 'Crimson', 'bahir': 'Luminous', 'barzel': 'Iron', 'erev': 'Evening',
+    'gan': 'Garden / Green', 'gehelet': 'Ember / Umber', 'karkom': 'Saffron / Amber',
+    'kaspi': 'Silver / Slate', 'katan': 'Linen / Cream', 'kelme': 'Ink & Parchment',
+    'kokkos': 'Scarlet', 'madder': 'Madder Root', 'murex': 'Midnight', 'nana': 'Mint',
+    'navajo': 'Southwest Rug', 'neshef': 'Twilight', 'panar': 'Hemp / Parchment',
+    'pepel': 'Ash / White Mono', 'porphyra': 'Purple Shell / Coral', 'ruach': 'Wind / Breeze',
+    'rutsya': 'Rust', 'shahar': 'Dawn / Salmon', 'shemen': 'Olive Oil', 'shoshan': 'Lily / Rose',
+    'simcha': 'Joy / Festival', 'tarshish': 'Aqua / Beryl', 'tekhelet': 'Azure Blue',
+    'tsemer': 'Soft Wool', 'tukmo': 'Brick / Earthen', 'viride': 'Green Earth / Forest',
+    'wari': 'Andean Patchwork', 'yam': 'Sea / Navy', 'zahav': 'Gold / Luxe',
+    'kinjiki': 'Imperial Reds & Purples', 'shibui': 'Astringent / Earthy', 'seigaiha': 'The Sea & Waves',
+    'bogolan': 'Mali Mudcloth', 'adire': 'Yoruba Indigo', 'kuba': 'Ngongo Raffia', 'kente': 'Asante Woven'
+};
+
+var ENGINE_DESCS = {
+    'random': 'Random', 'adama': 'Earth / Southwest', 'adire': 'Indigo Resist Dye',
+    'argyle': 'Lace & Lattice', 'bogolan': 'Mudcloth Strips', 'chipaz': 'Sunburst',
+    'interlace': 'Islamic Strapwork', 'kente': 'Staggered Weave', 'kepe': 'Crown Peaks',
+    'kishtima': 'Tessellation', 'kolya': 'Corner Meander', 'kshtir': 'Rotational Pinwheel',
+    'kuba': 'Raffia Patches', 'kudo': 'Brick Masonry', 'mastor': 'Diamonds & Diagonals',
+    'narmuny': 'Decorative Strips', 'navajo': 'Stepped Bands', 'pakshats': 'Interlocking Meander',
+    'panks': 'Floral Rosettes', 'pulay': 'Varied Grid Bands', 'seigaiha': 'Japanese Waves',
+    'sermat': 'Tiled Crosses', 'sermat-kudo': 'Blended Patterns', 'tol': 'Chevron Flames',
+    'ved': 'Interlocking S-Curves', 'virma': 'Stepped Forest Frame', 'wari': 'Patchwork Checks'
+};
+
+// ── PRNG (Park-Miller LCG) ────────────────────────────────────────────────────
+function makePRNG(s) {
+    var P = 2147483647;
+    s = ((s + 1590398727) % P);
+    if (s <= 0) s += P - 1;
+    var t = s;
+    function x() { t = (t * 16807) % P; return t; }
+    x(); // warm up
+    return {
+        rfl: function (a, b) { return (x() - 1) / (P - 1) * (b - a) + a; },
+        rin: function (a, b) { return ((x() - 1) % (b - a + 1)) + a; }
+    };
+}
diff --git a/js/larkspur-engines.js b/js/larkspur-engines.js
new file mode 100644
index 0000000..4292948
--- /dev/null
+++ b/js/larkspur-engines.js
@@ -0,0 +1,810 @@
+// ── ENGINE WEIGHT CALCULATOR ─────────────────────────────────────────────────
+// Returns total motif weight for cell (x,y) based on engine type + params.
+function calcMotifWeight(x, y, w, h, engine, params) {
+    var wt = 0;
+    switch (engine) {
+        case 'adire':
+            // Yoruba indigo resist dye: organic, tied circles, stenciled bleeding grids
+            var adireType = (Math.floor(x / params.cellSize) + Math.floor(y / params.cellSize)) % 3;
+
+            if (adireType === 0) {
+                // organic tie-dye sunburst circles
+                var cx = Math.floor(x / params.cellSize) * params.cellSize + params.cellSize / 2;
+                var cy = Math.floor(y / params.cellSize) * params.cellSize + params.cellSize / 2;
+                wt += motifTieDyeRing(x, y, cx, cy, params.ringR, params.ringT, params.fuzz) * params.ringWt;
+            } else if (adireType === 1) {
+                // freehand cascading waves/whorls (using a warped sine)
+                var wave = Math.sin((x + Math.sin(y / 20) * 15) / params.waveP);
+                if (Math.abs(wave) < params.waveT) wt += params.waveWt;
+            } else {
+                // sloppy stenciled grid (imperfect)
+                wt += motifLattice(x + Math.sin(y / 10) * 5, y + Math.cos(x / 10) * 5, params.lP, params.lT) * params.lWt;
+            }
+
+            // Global random dye bleed/splash
+            var noiseX = (x * 13 + y * 17) % params.bleed;
+            var noiseY = (x * 19 + y * 23) % params.bleed;
+            if (noiseX < 2 && noiseY < 2) wt += params.bleedWt;
+            break;
+
+        case 'kente':
+            // Asante staggered sequence blocks
+            var stripIdx = Math.floor(x / params.stripW);
+            var stripOffset = stripIdx * (params.blockH / 3); // Stagger strips vertically
+            var adjustedY = y + stripOffset;
+
+            var stripPhaseX = x % params.stripW;
+
+            // Thick seams separating strips
+            if (stripPhaseX < params.seamW || stripPhaseX > params.stripW - params.seamW) {
+                if (stripPhaseX % 2 === 0) wt += params.seamWt; // dashed/stitch effect
+            } else {
+                // Inside the strip - sequence blocks
+                var blockIdx = Math.floor(adjustedY / params.blockH);
+                var motifType = (stripIdx + blockIdx) % 4; // Alternate 4 patterns
+
+                var localX = stripPhaseX - params.stripW / 2;
+                var localY = Math.abs((adjustedY % params.blockH) - params.blockH / 2);
+
+                if (motifType === 0) {
+                    // tightly packed checkered
+                    wt += motifLattice(localX, localY, params.cP, params.cT) * params.cWt;
+                } else if (motifType === 1) {
+                    // dense parallel weft lines
+                    if (localY % params.wP < params.wT) wt += params.wWt;
+                } else if (motifType === 2) {
+                    // stepped block steps
+                    wt += motifStepped(localX, localY, params.sP, params.sT, 0) * params.sWt;
+                } else {
+                    // solid block
+                    if (Math.abs(localX - params.stripW / 2) < params.stripW * 0.4 && Math.abs(localY - params.blockH / 2) < params.blockH * 0.4) {
+                        wt += params.bWt;
+                    }
+                }
+            }
+            break;
+
+        case 'bogolan':
+            // Mudcloth - distinct horizontal or vertical strips containing geometric repetitions
+            var isVert = params.orientation === 1;
+            var primaryCoord = isVert ? x : y;
+            var secondaryCoord = isVert ? y : x;
+
+            var stripIdx2 = Math.floor(primaryCoord / params.stripW);
+            var stripPhase = primaryCoord % params.stripW;
+
+            var bogolanMotifType = stripIdx2 % params.motifs.length;
+            var mo = params.motifs[bogolanMotifType];
+
+            if (stripPhase < params.borderT || stripPhase > params.stripW - params.borderT) {
+                wt += params.borderWt;
+            } else {
+                if (mo.type === 0) {
+                    wt += motifChevron(secondaryCoord, stripPhase, params.stripW, mo.p1, mo.p2) * mo.wt;
+                } else if (mo.type === 1) {
+                    var dcx = (secondaryCoord % mo.p1) - mo.p1 / 2;
+                    var dcy = stripPhase - params.stripW / 2;
+                    if (Math.abs(dcx) + Math.abs(dcy) < mo.p2) wt += mo.wt;
+                } else if (mo.type === 2) {
+                    var ccx = (secondaryCoord % mo.p1) - mo.p1 / 2;
+                    var ccy = stripPhase - params.stripW / 2;
+                    if (Math.abs(ccx) < mo.p2 || Math.abs(ccy) < mo.p2) wt += mo.wt;
+                } else {
+                    if ((secondaryCoord % mo.p1) < mo.p2) wt += mo.wt;
+                }
+            }
+            break;
+
+        case 'kuba':
+            // Raffia weave - dense, irregular orthogonal patches
+            var pcx = Math.floor(x / params.patchSize);
+            var pcy = Math.floor(y / params.patchSize);
+            var pType = (pcx * 13 + pcy * 29) % 5;
+            var ix = x % params.patchSize;
+            var iy = y % params.patchSize;
+
+            if (pType === 0) wt += motifMeander(x, y, params.mP, params.mT, 1) * params.mWt;
+            else if (pType === 1) wt += motifMeander(y, x, params.mP, params.mT, 1) * params.mWt;
+            else if (pType === 2) wt += motifStepped(x, y, params.sP, params.sT, 0) * params.sWt;
+            else if (pType === 3) wt += motifStepped(x, y, params.sP, params.sT, 1) * params.sWt;
+            else wt += motifLattice(x, y, params.mP, params.mT) * params.mWt * 0.7;
+
+            if (ix < params.bT || iy < params.bT) wt += params.bWt;
+            break;
+
+        case 'wari':
+            // Patchwork checks + tie-dye rings
+            var px = Math.floor(x / params.patchW);
+            var py = Math.floor(y / params.patchH);
+            var patchType = (px * 3 + py * 7) % 3; // pseudo-random deterministic
+
+            if (patchType === 0) {
+                // Tie-dye center ring
+                var cx2 = (px + 0.5) * params.patchW;
+                var cy2 = (py + 0.5) * params.patchH;
+                wt += motifTieDyeRing(x, y, cx2, cy2, params.ringR, params.ringT, params.fuzz) * params.ringWt;
+            } else if (patchType === 1) {
+                // Diagonal steps
+                wt += motifStepped(x, y, params.step, params.stepT, 0) * params.stepWt;
+            } else {
+                // Checkerboard / grid
+                wt += motifLattice(x, y, params.gridP, params.gridT) * params.gridWt;
+            }
+
+            // Seams
+            if (x % params.patchW < params.seamT || y % params.patchH < params.seamT) {
+                wt += params.seamWt;
+            }
+            break;
+
+        case 'navajo':
+            // Horizontal bands with stepped diamonds
+            var bandY = y % params.bandH;
+            var bIdx = Math.floor(y / params.bandH) % params.bands.length;
+            var band = params.bands[bIdx];
+
+            if (band.type === 0 && Math.abs(bandY - params.bandH / 2) < band.diaR) {
+                // Central Stepped Diamond
+                wt += motifSteppedDiamond(x, y, w / 2, y, band.diaR, band.stepSz) * band.wt;
+                // Flanking side diamonds
+                wt += motifSteppedDiamond(x, y, w * 0.15, y, band.diaR * 0.5, band.stepSz) * band.wt * 0.8;
+                wt += motifSteppedDiamond(x, y, w * 0.85, y, band.diaR * 0.5, band.stepSz) * band.wt * 0.8;
+            } else if (band.type === 1) {
+                // Serrated line
+                wt += motifSerratedBand(x, y, y, band.amp, band.period) * band.wt;
+            } else {
+                // Chevrons (water)
+                wt += motifChevron(x, y, w, band.period, band.amp) * band.wt;
+            }
+
+            // Demarcation stripes
+            if (bandY < params.stripeT) wt += params.stripeWt;
+            break;
+
+        case 'argyle':
+            // French lace borders and thick multi-lattice center
+            var isBorder = x < params.borderW || x > (w - params.borderW);
+
+            if (isBorder) {
+                // Scalloped edging + inner diamond dots
+                if (x < params.borderW) {
+                    wt += motifScallopEdge(x, y, params.borderW, params.scallopR, params.scallopP) * params.laceWt;
+                } else {
+                    wt += motifScallopEdge(w - x, y, params.borderW, params.scallopR, params.scallopP) * params.laceWt;
+                }
+            } else {
+                // Central Argyle field
+                wt += motifMultiLattice(x, y, params.latP, params.latT, params.latL, params.latSpc) * params.argyleWt;
+                // Accent dots inside argyle diamonds
+                var dCx = (x % params.latP) - params.latP / 2;
+                var dCy = (y % params.latP) - params.latP / 2;
+                if (Math.abs(dCx) < params.latP / 4 && Math.abs(dCy) < params.latP / 4) {
+                    wt += motifDiamond(x, y, x - dCx, y - dCy, params.dotR, 0) * params.dotWt;
+                }
+            }
+            break;
+
+        case 'interlace':
+            // Islamic overlapping strapwork stars
+            wt += motifStarGrid(x, y, params.starP, params.starR, params.starT) * params.starWt;
+            break;
+
+        case 'seigaiha':
+            // Japanese concentric overlapping waves
+            wt += motifSeigaiha(x, y, params.period, params.amp, params.rowSpacing, params.stripeSpacing, params.thickness) * params.waveWt;
+            break;
+
+        case 'sermat':
+            // Tiled crosses — family identity marks
+            for (var ci = 0; ci < params.centers.length; ci++) {
+                var ctr = params.centers[ci];
+                wt += motifCross(x, y, ctr[0], ctr[1], params.armW, params.armL) * params.weights[ci % params.weights.length];
+            }
+            wt += motifDiamond(x, y, w / 2, h / 2, params.ringR, params.ringW) * params.ringWt;
+            break;
+
+        case 'tol':
+            // Fire — chevron flames climbing upward
+            for (var chi = 0; chi < params.layers.length; chi++) {
+                var ly = params.layers[chi];
+                wt += motifChevron(x, y, w, ly[0], ly[1]) * ly[2];
+            }
+            break;
+
+        case 'mastor':
+            // Land — concentric diamond rings + diagonal fill
+            for (var di = 0; di < params.diamonds.length; di++) {
+                var dm = params.diamonds[di];
+                wt += motifDiamond(x, y, dm[0], dm[1], dm[2], dm[3]) * dm[4];
+            }
+            wt += motifDiagonal(x, y, params.diagP, params.diagT) * params.diagWt;
+            wt += motifAntiDiag(x, y, params.adiagP, params.adiagT) * params.adiagWt;
+            break;
+
+        case 'virma':
+            // Forest frame — stepped border with interior growth
+            var bx = Math.min(x, w - 1 - x);
+            var by = Math.min(y, h - 1 - y);
+            var borderDist = Math.min(bx, by);
+            if (borderDist < params.borderW) {
+                wt += motifStepped(x, y, params.step, params.stepT, 0) * params.stepWt;
+                wt += motifStepped(x, y, params.step2, params.stepT2, 1) * params.stepWt2;
+            }
+            if (borderDist >= params.borderW && borderDist < params.borderW + params.innerW) {
+                wt += motifChevron(x, y, w, params.innerP, params.innerA) * params.innerWt;
+            }
+            if (borderDist >= params.borderW + params.innerW) {
+                wt += motifDiamond(x, y, w / 2, h / 2, params.centralR, params.centralRing) * params.centralWt;
+            }
+            break;
+
+        case 'ved':
+            // Water — interlocking S-curves
+            for (var si = 0; si < params.scrolls.length; si++) {
+                var sc = params.scrolls[si];
+                wt += motifScroll(x, y, w, sc[0], sc[1]) * sc[2];
+            }
+            wt += motifStepped(x, y, params.bandStep, params.bandT, 0) * params.bandWt;
+            break;
+
+        case 'chipaz':
+            // Sun god — radial sunburst + concentric rings + lattice glow
+            for (var chi2 = 0; chi2 < params.rays.length; chi2++) {
+                var ray = params.rays[chi2];
+                wt += motifCross(x, y, ray[0], ray[1], ray[2], ray[3]) * ray[4];
+            }
+            wt += motifDiamond(x, y, w / 2, h / 2, params.sunR, params.sunRing) * params.sunWt;
+            wt += motifLattice(x, y, params.glowP, params.glowT) * params.glowWt;
+            break;
+
+        case 'kudo':
+            // House — brick masonry + rectangular partitions
+            wt += motifBrick(x, y, params.brickW, params.brickH) * params.brickWt;
+            for (var ki = 0; ki < params.rooms.length; ki++) {
+                var rm = params.rooms[ki];
+                if (x >= rm[0] && x < rm[0] + rm[2] && y >= rm[1] && y < rm[1] + rm[3]) {
+                    wt += rm[4];
+                }
+            }
+            wt += motifStepped(x, y, params.frameStep, params.frameT, 0) * params.frameWt;
+            break;
+
+        case 'pulay':
+            // Waist belt — horizontal bands with different motifs per band
+            var bandIdx = Math.floor(y / params.bandH) % params.bands.length;
+            var b = params.bands[bandIdx];
+            if (b.type === 0) wt += motifDiagonal(x, y, b.p1, b.p2) * b.wt;
+            else if (b.type === 1) wt += motifChevron(x, y, w, b.p1, b.p2) * b.wt;
+            else if (b.type === 2) wt += motifPolyline(x, y, b.p1, b.p2, b.p3) * b.wt;
+            else if (b.type === 3) wt += motifLattice(x, y, b.p1, b.p2) * b.wt;
+            else wt += motifStepped(x, y, b.p1, b.p2, 0) * b.wt;
+            // Duckfoot accents between bands
+            for (var pi2 = 0; pi2 < params.feet.length; pi2++) {
+                var ft = params.feet[pi2];
+                wt += motifDuckfoot(x, y, ft[0], ft[1], ft[2], ft[3]) * ft[4];
+            }
+            break;
+
+        case 'sermat-kudo':
+            // Pattern-house — blend 2-3 engines
+            for (var mi = 0; mi < params.mix.length; mi++) {
+                var sub = params.mix[mi];
+                wt += calcMotifWeight(x, y, w, h, sub.engine, sub.params) * sub.weight;
+            }
+            break;
+
+        case 'kepe':
+            // Crown/headdress — sawtooth peaks + stepped fill (catalog p122, p158-162)
+            var peakY = (y % params.peakH);
+            var peakX = (x % params.peakW);
+            var peakSlope = peakY < params.peakH / 2 ? peakX < peakY * params.peakW / params.peakH : peakX < (params.peakH - peakY) * params.peakW / params.peakH;
+            if (peakSlope) wt += params.peakWt;
+            wt += motifStepped(x, y, params.fillStep, params.fillT, 0) * params.fillWt;
+            wt += motifDiamond(x, y, w / 2, h / 2, params.crownR, params.crownRing) * params.crownWt;
+            break;
+
+        case 'panks':
+            // Petal/blossom — floral rosettes (catalog p107)
+            for (var pki = 0; pki < params.flowers.length; pki++) {
+                var fl = params.flowers[pki];
+                wt += motifRosette(x, y, fl[0], fl[1], fl[2], fl[3], fl[4]) * fl[5];
+            }
+            wt += motifDiamond(x, y, w / 2, h / 2, params.stemR, params.stemRing) * params.stemWt;
+            break;
+
+        case 'kishtima':
+            // Pattern/ornament — all-over field tessellation (catalog p168, p172)
+            if (params.tileType === 0) {
+                wt += motifLattice(x, y, params.tileP, params.tileT) * params.tileWt;
+            } else {
+                wt += motifHexLattice(x, y, params.tileP, params.tileT) * params.tileWt;
+            }
+            // Node accents at intersections
+            var nodeX = ((x + params.nodeOff) % params.nodeP) < params.nodeSz;
+            var nodeY = ((y + params.nodeOff) % params.nodeP) < params.nodeSz;
+            if (nodeX && nodeY) wt += params.nodeWt;
+            wt += motifDiamond(x, y, w / 2, h / 2, params.fieldR, params.fieldRing) * params.fieldWt;
+            break;
+
+        case 'kshtir':
+            // Spindle/whorl — rotational pinwheel (catalog p132)
+            var kdx = x - w / 2, kdy = y - h / 2;
+            var kAngle = Math.atan2(kdy, kdx);
+            var kDist = Math.sqrt(kdx * kdx + kdy * kdy);
+            // Spiral arms
+            for (var kai = 0; kai < params.armCount; kai++) {
+                var armAngle = kAngle - kai * Math.PI * 2 / params.armCount;
+                var spiralPhase = ((armAngle + kDist * params.spiralTwist) % (Math.PI * 2) + Math.PI * 2) % (Math.PI * 2);
+                if (spiralPhase < params.armWidth && kDist < params.armLen) {
+                    wt += params.armWt;
+                }
+            }
+            wt += motifMeander(x, y, params.bgP, params.bgW, params.bgD) * params.bgWt;
+            break;
+
+        case 'narmuny':
+            // Border/edge — decorative horizontal band strips (catalog p152-155)
+            var zoneY = y % params.zoneH;
+            var zoneIdx = Math.floor(y / params.zoneH) % params.zones.length;
+            var zone = params.zones[zoneIdx];
+            if (zone.motif === 0) wt += motifChevron(x, y, w, zone.p1, zone.p2) * zone.wt;
+            else if (zone.motif === 1) wt += motifDiagonal(x, y, zone.p1, zone.p2) * zone.wt;
+            else if (zone.motif === 2) wt += motifLattice(x, y, zone.p1, zone.p2) * zone.wt;
+            else if (zone.motif === 3) wt += motifDiamond(x, y, w / 2, y, zone.p1, zone.p2) * zone.wt;
+            else wt += motifStepped(x, y, zone.p1, zone.p2, 0) * zone.wt;
+            // Separator lines between zones
+            if (zoneY < params.sepT) wt += params.sepWt;
+            break;
+
+        case 'pakshats':
+            // Interlocking — Greek key meander at multiple scales (catalog p115, p135)
+            for (var psi = 0; psi < params.layers.length; psi++) {
+                var ml = params.layers[psi];
+                wt += motifMeander(x, y, ml[0], ml[1], ml[2]) * ml[3];
+            }
+            wt += motifStepped(x, y, params.accentStep, params.accentT, 1) * params.accentWt;
+            break;
+
+        case 'kolya':
+            // Corner/angle — triangular meander-filled V-shapes (catalog p162)
+            var cornerDist = Math.min(x, y, w - 1 - x, h - 1 - y);
+            var inCorner = cornerDist < params.cornerSize;
+            if (inCorner) {
+                wt += motifMeander(x, y, params.fillP, params.fillW, params.fillD) * params.cornerWt;
+                wt += motifChevron(x, y, w, params.chevP, params.chevA) * params.chevWt;
+            }
+            // Edge frame
+            if (cornerDist < params.edgeW) wt += params.edgeWt;
+            break;
+    }
+    return wt;
+}
+
+// ── ENGINE PARAMETER GENERATOR ───────────────────────────────────────────────
+// Uses PRNG to create parameters for each Mordvinian engine.
+function generatePatternParams(engine, prng, w, h) {
+    var rfl = prng.rfl, rin = prng.rin;
+    var params = {};
+
+    switch (engine) {
+        case 'bogolan':
+            params.orientation = rin(0, 1);
+            params.stripW = Math.floor((params.orientation === 1 ? w : h) / rin(3, 9));
+            var numMotifs = rin(2, 5);
+            params.motifs = [];
+            for (var bi = 0; bi < numMotifs; bi++) {
+                params.motifs.push({
+                    type: rin(0, 3),
+                    p1: rin(20, 60),
+                    p2: rin(4, 15),
+                    wt: rfl(4, 9)
+                });
+            }
+            params.borderT = rin(2, 8);
+            params.borderWt = rfl(5, 12);
+            break;
+
+        case 'kuba':
+            params.patchSize = rin(20, 80);
+            // Generate specific meander and stepped params to map tightly
+            params.mP = rin(10, 30);
+            params.mT = rin(1, 4);
+            params.mWt = rfl(3, 8);
+            params.sP = rin(10, 30);
+            params.sT = rin(2, 6);
+            params.sWt = rfl(4, 9);
+            params.bT = rin(2, 6);
+            params.bWt = rfl(5, 12);
+            break;
+
+        case 'wari':
+            params.patchW = Math.floor(w / rin(3, 6)); // 3 to 6 columns
+            params.patchH = Math.floor(h / rin(4, 8)); // 4 to 8 rows
+            params.ringR = rin(10, Math.floor(params.patchW * 0.4));
+            params.ringT = rin(2, 6);
+            params.fuzz = rin(6, 16); // frequency of tie-dye bleeding
+            params.ringWt = rfl(4, 9);
+            params.step = rin(4, 12);
+            params.stepT = rin(1, 4);
+            params.stepWt = rfl(2, 5);
+            params.gridP = rin(6, 14);
+            params.gridT = rin(1, 3);
+            params.gridWt = rfl(2, 5);
+            params.seamT = rin(1, 4);
+            params.seamWt = rfl(3, 7);
+            break;
+
+        case 'navajo':
+            params.bandH = Math.floor(h / rin(3, 7)); // 3 to 7 big bands
+            var nbands = rin(3, 7);
+            params.bands = [];
+            for (var nbi = 0; nbi < nbands; nbi++) {
+                params.bands.push({
+                    type: rin(0, 2),
+                    diaR: Math.floor(params.bandH * rfl(0.3, 0.45)),
+                    stepSz: rin(2, 6),
+                    amp: rin(4, 15),
+                    period: rin(20, 50),
+                    wt: rfl(4, 9)
+                });
+            }
+            // At least one diamond band
+            params.bands[Math.floor(nbands / 2)].type = 0;
+
+            params.stripeT = rin(2, 8);
+            params.stripeWt = rfl(3, 8);
+            break;
+
+        case 'argyle':
+            params.borderW = Math.floor(w * rfl(0.1, 0.25));
+            params.scallopR = rin(15, 40);
+            params.scallopP = rin(30, 80);
+            params.laceWt = rfl(3, 7);
+
+            params.latP = rin(30, 80); // large lattice
+            params.latT = rin(2, 5);
+            params.latL = rin(2, 4); // 2-4 lines
+            params.latSpc = params.latT * rin(2, 4);
+            params.argyleWt = rfl(4, 8);
+
+            params.dotR = rin(3, 8);
+            params.dotWt = rfl(2, 6);
+            break;
+
+        case 'interlace':
+            params.starP = rin(30, 100); // grid period
+            params.starR = Math.floor(params.starP * rfl(0.35, 0.45)); // radius of shapes
+            params.starT = rin(2, 8); // thickness of strapwork
+            params.starWt = rfl(4, 9);
+            break;
+
+        case 'sermat':
+            var nc = rin(3, 8);
+            params.centers = [];
+            params.weights = [];
+            var spacingX = w / (Math.ceil(Math.sqrt(nc)) + 1);
+            var spacingY = h / (Math.ceil(nc / Math.ceil(Math.sqrt(nc))) + 1);
+            for (var i = 0; i < nc; i++) {
+                var col = i % Math.ceil(Math.sqrt(nc));
+                var row = Math.floor(i / Math.ceil(Math.sqrt(nc)));
+                params.centers.push([
+                    Math.floor(spacingX * (col + 1) + rfl(-spacingX * 0.2, spacingX * 0.2)),
+                    Math.floor(spacingY * (row + 1) + rfl(-spacingY * 0.2, spacingY * 0.2))
+                ]);
+                params.weights.push(rfl(3, 8));
+            }
+            params.armW = rin(2, 5);
+            params.armL = rin(4, 12);
+            params.ringR = rin(8, 25);
+            params.ringW = rin(1, 4);
+            params.ringWt = rfl(2, 6);
+            break;
+
+        case 'tol':
+            var nl = rin(2, 5);
+            params.layers = [];
+            for (var j = 0; j < nl; j++) {
+                params.layers.push([
+                    rin(6, 30),       // period
+                    rin(2, 8),        // amplitude
+                    rfl(2, 8)         // weight
+                ]);
+            }
+            break;
+
+        case 'mastor':
+            var nd = rin(2, 5);
+            params.diamonds = [];
+            for (var k = 0; k < nd; k++) {
+                params.diamonds.push([
+                    Math.floor(w / 2 + rfl(-w * 0.2, w * 0.2)),
+                    Math.floor(h / 2 + rfl(-h * 0.2, h * 0.2)),
+                    rin(10, 40),
+                    rin(1, 5),
+                    rfl(3, 8)
+                ]);
+            }
+            params.diagP = rin(4, 16);
+            params.diagT = rin(1, Math.floor(params.diagP / 2));
+            params.diagWt = rfl(1, 4);
+            params.adiagP = rin(4, 16);
+            params.adiagT = rin(1, Math.floor(params.adiagP / 2));
+            params.adiagWt = rfl(1, 4);
+            break;
+
+        case 'virma':
+            params.borderW = rin(5, 15);
+            params.step = rin(3, 10);
+            params.stepT = rin(1, Math.max(2, Math.floor(params.step / 2)));
+            params.stepWt = rfl(3, 8);
+            params.step2 = rin(3, 10);
+            params.stepT2 = rin(1, Math.max(2, Math.floor(params.step2 / 2)));
+            params.stepWt2 = rfl(2, 6);
+            params.innerW = rin(5, 15);
+            params.innerP = rin(8, 20);
+            params.innerA = rin(2, 6);
+            params.innerWt = rfl(3, 7);
+            params.centralR = rin(15, 35);
+            params.centralRing = rin(2, 6);
+            params.centralWt = rfl(3, 8);
+            break;
+
+        case 'ved':
+            var ns = rin(2, 5);
+            params.scrolls = [];
+            for (var m = 0; m < ns; m++) {
+                params.scrolls.push([
+                    rfl(5, 20),
+                    rfl(0.5, 3),
+                    rfl(3, 8)
+                ]);
+            }
+            params.bandStep = rin(4, 12);
+            params.bandT = rin(1, Math.max(2, Math.floor(params.bandStep / 2)));
+            params.bandWt = rfl(1, 4);
+            break;
+
+        case 'chipaz':
+            // Sun god — radiating crosses + concentric diamond + lattice glow
+            var nr = rin(4, 8);
+            params.rays = [];
+            for (var ri = 0; ri < nr; ri++) {
+                var angle = ri * Math.PI * 2 / nr;
+                var dist = rfl(10, 35);
+                params.rays.push([
+                    Math.floor(w / 2 + Math.cos(angle) * dist),
+                    Math.floor(h / 2 + Math.sin(angle) * dist * (h / w)),
+                    rin(1, 3),   // armW
+                    rin(3, 10),  // armL
+                    rfl(3, 8)    // weight
+                ]);
+            }
+            params.sunR = rin(12, 30);
+            params.sunRing = rin(2, 5);
+            params.sunWt = rfl(3, 8);
+            params.glowP = rin(6, 16);
+            params.glowT = rin(1, 3);
+            params.glowWt = rfl(1, 3);
+            break;
+
+        case 'kudo':
+            // House — brick masonry + room partitions
+            params.brickW = rin(4, 12);
+            params.brickH = rin(3, 8);
+            params.brickWt = rfl(2, 5);
+            var nRooms = rin(3, 8);
+            params.rooms = [];
+            for (var ri2 = 0; ri2 < nRooms; ri2++) {
+                params.rooms.push([
+                    rin(0, w - 10),      // x
+                    rin(0, h - 10),      // y
+                    rin(8, 25),           // width
+                    rin(8, 30),           // height
+                    rfl(2, 7)             // weight
+                ]);
+            }
+            params.frameStep = rin(4, 10);
+            params.frameT = rin(1, Math.max(2, Math.floor(params.frameStep / 2)));
+            params.frameWt = rfl(1, 4);
+            break;
+
+        case 'pulay':
+            // Waist belt — horizontal bands with varied motifs
+            params.bandH = rin(8, 25);
+            var nBands = rin(3, 7);
+            params.bands = [];
+            for (var bi2 = 0; bi2 < nBands; bi2++) {
+                var btype = rin(0, 4);
+                params.bands.push({
+                    type: btype,
+                    p1: rin(4, 16),
+                    p2: rin(1, 6),
+                    p3: rin(1, 3),
+                    wt: rfl(3, 8)
+                });
+            }
+            // Duckfoot accents
+            var nFeet = rin(2, 6);
+            params.feet = [];
+            for (var fi = 0; fi < nFeet; fi++) {
+                params.feet.push([
+                    rin(5, w - 5),   // cx
+                    rin(5, h - 10),  // cy
+                    rfl(3, 10),      // spread
+                    rin(4, 12),      // length
+                    rfl(3, 7)        // weight
+                ]);
+            }
+            break;
+
+        case 'sermat-kudo':
+            // Pattern-house — blend 2-3 engines
+            var bases = ['wari', 'navajo', 'argyle', 'interlace', 'seigaiha', 'sermat', 'tol', 'mastor', 'virma', 'ved', 'chipaz', 'kudo', 'pulay', 'kepe', 'panks', 'kishtima', 'kshtir', 'narmuny', 'pakshats', 'kolya', 'bogolan', 'kuba', 'adire', 'kente'];
+            var nmix = rin(2, 3);
+            params.mix = [];
+            var used = {};
+            for (var q = 0; q < nmix; q++) {
+                var pick;
+                do { pick = bases[rin(0, bases.length - 1)]; } while (used[pick]);
+                used[pick] = true;
+                var subPrng = makePRNG(prng.rin(1, 999999));
+                params.mix.push({
+                    engine: pick,
+                    params: generatePatternParams(pick, subPrng, w, h),
+                    weight: rfl(0.3, 1.0)
+                });
+            }
+            break;
+
+        case 'kepe':
+            // Crown/headdress — sawtooth peaks
+            params.peakH = rin(8, 20);
+            params.peakW = rin(6, 16);
+            params.peakWt = rfl(3, 8);
+            params.fillStep = rin(3, 8);
+            params.fillT = rin(1, Math.max(2, Math.floor(params.fillStep / 2)));
+            params.fillWt = rfl(2, 5);
+            params.crownR = rin(15, 35);
+            params.crownRing = rin(2, 5);
+            params.crownWt = rfl(2, 6);
+            break;
+
+        case 'panks':
+            // Petal/blossom — floral rosettes
+            var nFlowers = rin(3, 7);
+            params.flowers = [];
+            for (var pfi = 0; pfi < nFlowers; pfi++) {
+                params.flowers.push([
+                    rin(10, w - 10),     // cx
+                    rin(10, h - 10),     // cy
+                    rin(8, 25),           // radius
+                    rin(3, 8) * 2,        // petals (even number)
+                    rin(1, 4),            // thickness
+                    rfl(3, 8)             // weight
+                ]);
+            }
+            params.stemR = rin(20, 40);
+            params.stemRing = rin(2, 5);
+            params.stemWt = rfl(1, 4);
+            break;
+
+        case 'kishtima':
+            // Pattern/ornament — all-over field tessellation
+            params.tileType = rin(0, 1);
+            params.tileP = rin(8, 24);
+            params.tileT = rin(1, 3);
+            params.tileWt = rfl(3, 7);
+            params.nodeP = rin(6, 18);
+            params.nodeSz = rin(1, 3);
+            params.nodeOff = rin(0, params.nodeP);
+            params.nodeWt = rfl(2, 6);
+            params.fieldR = rin(20, 45);
+            params.fieldRing = rin(2, 5);
+            params.fieldWt = rfl(1, 3);
+            break;
+
+        case 'kshtir':
+            // Spindle/whorl — rotational pinwheel
+            params.armCount = rin(3, 6);
+            params.armWidth = rfl(0.3, 1.2);
+            params.armLen = rfl(w * 0.3, w * 0.6);
+            params.spiralTwist = rfl(0.02, 0.1);
+            params.armWt = rfl(4, 9);
+            params.bgP = rin(10, 24);
+            params.bgW = rin(1, 3);
+            params.bgD = rin(1, 2);
+            params.bgWt = rfl(0.5, 2);
+            break;
+
+        case 'narmuny':
+            // Border/edge — decorative horizontal band strips
+            params.zoneH = rin(6, 18);
+            var nZones = rin(3, 7);
+            params.zones = [];
+            for (var nzi = 0; nzi < nZones; nzi++) {
+                params.zones.push({
+                    motif: rin(0, 4),
+                    p1: rin(4, 16),
+                    p2: rin(1, 6),
+                    wt: rfl(3, 8)
+                });
+            }
+            params.sepT = rin(1, 3);
+            params.sepWt = rfl(2, 6);
+            break;
+
+        case 'pakshats':
+            // Interlocking — Greek key meander layers
+            var nLayers = rin(2, 4);
+            params.layers = [];
+            for (var pli = 0; pli < nLayers; pli++) {
+                params.layers.push([
+                    rin(10, 30),   // period
+                    rin(1, 3),     // width
+                    rin(1, 2),     // depth
+                    rfl(3, 8)      // weight
+                ]);
+            }
+            params.accentStep = rin(4, 12);
+            params.accentT = rin(1, Math.max(2, Math.floor(params.accentStep / 2)));
+            params.accentWt = rfl(1, 3);
+            break;
+
+        case 'kolya':
+            // Corner/angle — triangular fills
+            params.cornerSize = rin(15, Math.floor(Math.min(w, h) * 0.4));
+            params.fillP = rin(8, 20);
+            params.fillW = rin(1, 3);
+            params.fillD = rin(1, 2);
+            params.cornerWt = rfl(3, 8);
+            params.chevP = rin(6, 16);
+            params.chevA = rin(2, 6);
+            params.chevWt = rfl(2, 5);
+            params.edgeW = rin(2, 6);
+            params.edgeWt = rfl(2, 5);
+            break;
+
+        case 'adire':
+            params.cellSize = rin(60, 150);
+            params.ringR = Math.floor(params.cellSize * rfl(0.2, 0.4));
+            params.ringT = rin(3, 8);
+            params.fuzz = rin(10, 30);
+            params.ringWt = rfl(4, 10);
+            params.waveP = rin(15, 35);
+            params.waveT = rfl(0.1, 0.3);
+            params.waveWt = rfl(3, 7);
+            params.lP = rin(10, 25);
+            params.lT = rin(2, 6);
+            params.lWt = rfl(3, 8);
+            params.bleed = rin(5, 15);
+            params.bleedWt = rfl(5, 12);
+            break;
+
+        case 'seigaiha':
+            params.period = Math.floor(w * rfl(0.15, 0.4));
+            params.amp = params.period * rfl(0.15, 0.35);
+            params.rowSpacing = params.amp * rfl(0.7, 1.4);
+            params.thickness = rin(1, 3);
+            params.stripeSpacing = params.thickness * rin(3, 7);
+            params.waveWt = rfl(3, 8);
+            break;
+
+        case 'kente':
+            params.stripW = Math.floor(w / rin(6, 15)); // Narrow, numerous strips
+            params.blockH = Math.floor(params.stripW * rfl(1.0, 2.5)); // Rectangular sequence blocks
+            params.seamW = rin(2, 6);
+            params.seamWt = rfl(5, 12);
+            params.cP = rin(6, 14); // dense checkered
+            params.cT = rin(3, 7);
+            params.cWt = rfl(4, 9);
+            params.wP = rin(4, 10); // dense weft
+            params.wT = rin(2, 5);
+            params.wWt = rfl(3, 8);
+            params.sP = rin(8, 20); // steps
+            params.sT = rin(2, 6);
+            params.sWt = rfl(3, 8);
+            params.bWt = rfl(6, 12); // solid
+            break;
+    }
+    return params;
+}
diff --git a/js/larkspur-motifs.js b/js/larkspur-motifs.js
new file mode 100644
index 0000000..b15de5f
--- /dev/null
+++ b/js/larkspur-motifs.js
@@ -0,0 +1,235 @@
+// ── MOTIF PRIMITIVES ─────────────────────────────────────────────────────────
+// Each returns a weight contribution for cell (x,y) on grid (w,h).
+// Inspired by Mordvinian embroidery stitch patterns.
+
+// 1. Diagonal band: weight when (x+y) mod period < thickness → creates / lines
+function motifDiagonal(x, y, period, thickness) {
+    return ((x + y) % period) < thickness ? 1 : 0;
+}
+
+// 2. Anti-diagonal band: weight when (x-y) mod period < thickness → creates \ lines
+function motifAntiDiag(x, y, period, thickness) {
+    return (((x - y) % period + period) % period) < thickness ? 1 : 0;
+}
+
+// 3. Chevron: diagonal + anti-diagonal with fold at midpoint
+function motifChevron(x, y, w, period, amplitude) {
+    var fold = Math.abs(x - w / 2);
+    return ((fold + y) % period) < amplitude ? 1 : 0;
+}
+
+// 4. Diamond: Manhattan distance from center < radius
+function motifDiamond(x, y, cx, cy, radius, ringWidth) {
+    var dist = Math.abs(x - cx) + Math.abs(y - cy);
+    if (ringWidth > 0) {
+        return (dist % radius) < ringWidth ? 1 : 0;
+    }
+    return dist < radius ? 1 : 0;
+}
+
+// 5. Stepped edge: creates castellated/meander border patterns
+function motifStepped(x, y, step, thickness, axis) {
+    var offset = (Math.floor(y / step) % 2) * Math.floor(step / 2);
+    if (axis === 1) offset = (Math.floor(x / step) % 2) * Math.floor(step / 2);
+    var coord = axis === 0 ? x : y;
+    return ((coord + offset) % step) < thickness ? 1 : 0;
+}
+
+// 6. S-curve / scroll: sinusoidal offset creates interlocking S shapes
+function motifScroll(x, y, w, amplitude, freq) {
+    var center = w / 2;
+    var wave = center + amplitude * Math.sin(y * freq * Math.PI / 40);
+    var dist = Math.abs(x - wave);
+    return dist < amplitude * 0.4 ? 1 : 0;
+}
+
+// 7. Cross / rosette: radial cross from center point
+function motifCross(x, y, cx, cy, armW, armL) {
+    var dx = Math.abs(x - cx), dy = Math.abs(y - cy);
+    var onH = dx < armL && dy < armW;
+    var onV = dy < armL && dx < armW;
+    return (onH || onV) ? 1 : 0;
+}
+
+// 8. Duckfoot: splayed triplet from a point (Mordvin protective mark)
+function motifDuckfoot(x, y, cx, cy, spread, len) {
+    var dx = x - cx, dy = y - cy;
+    if (dy < 0 || dy > len) return 0;
+    var t = dy / len;
+    // Three splayed toes
+    var toe1 = Math.abs(dx - t * spread) < 1.5;
+    var toe2 = Math.abs(dx) < 1.5;
+    var toe3 = Math.abs(dx + t * spread) < 1.5;
+    return (toe1 || toe2 || toe3) ? 1 : 0;
+}
+
+// 9. Brick: offset rectangular grid like masonry
+function motifBrick(x, y, brickW, brickH) {
+    var row = Math.floor(y / brickH);
+    var offset = (row % 2) * Math.floor(brickW / 2);
+    var inMortarX = ((x + offset) % brickW) < 1;
+    var inMortarY = (y % brickH) < 1;
+    return (inMortarX || inMortarY) ? 0 : 1;
+}
+
+// 10. Lattice: intersecting diagonals creating X-crossing grid
+function motifLattice(x, y, period, thickness) {
+    var d1 = ((x + y) % period) < thickness;
+    var d2 = (((x - y) % period + period) % period) < thickness;
+    return (d1 || d2) ? 1 : 0;
+}
+
+// 11. Polyline: horizontal zigzag stroke
+function motifPolyline(x, y, period, amplitude, thickness) {
+    var phase = x % period;
+    var peak = (phase < period / 2) ? (phase * 2 * amplitude / period) : (amplitude * 2 - phase * 2 * amplitude / period);
+    return Math.abs(y % (amplitude * 2) - peak) < thickness ? 1 : 0;
+}
+
+// 12. Rosette: radial petal/flower form — concentric lobes around center (catalog p103, p107)
+function motifRosette(x, y, cx, cy, radius, petals, thickness) {
+    var dx = x - cx, dy = y - cy;
+    var dist = Math.sqrt(dx * dx + dy * dy);
+    if (dist > radius * 1.3) return 0;
+    var angle = Math.atan2(dy, dx);
+    var petalPhase = Math.cos(angle * petals);
+    var petalR = radius * (0.5 + 0.5 * petalPhase);
+    return Math.abs(dist - petalR) < thickness ? 1 : 0;
+}
+
+// 13. Meander: Greek key / fret — stepped square spiral (catalog p132, p135)
+function motifMeander(x, y, period, width, depth) {
+    var px = ((x % period) + period) % period;
+    var py = ((y % period) + period) % period;
+    var half = Math.floor(period / 2);
+    // Outer frame
+    if (px < width || py < width) return 1;
+    if (px >= period - width || py >= period - width) return 1;
+    // Inner spiral arm
+    if (px >= half - width && px < half + width && py >= width && py < half + width) return 1;
+    if (py >= half - width && py < half + width && px >= half && px < period - width) return 1;
+    // Depth modulation for nested feel
+    if (depth > 1) {
+        var qp = Math.floor(period / 3);
+        var qx = ((x % qp) + qp) % qp;
+        var qy = ((y % qp) + qp) % qp;
+        if (qx < width || qy < width) return 0.5;
+    }
+    return 0;
+}
+
+// 14. HexLattice: hexagonal honeycomb tiling for field tessellations (catalog p172)
+function motifHexLattice(x, y, cellSize, wallThick) {
+    var row = Math.floor(y / (cellSize * 0.866));
+    var offset = (row % 2) * (cellSize / 2);
+    var cx = ((x + offset) % cellSize) - cellSize / 2;
+    var cy = (y % (cellSize * 0.866)) - cellSize * 0.433;
+    // Hex distance approximation
+    var hx = Math.abs(cx);
+    var hy = Math.abs(cy);
+    var hexDist = Math.max(hx, (hx / 2 + hy * 0.866));
+    var edgeDist = Math.abs(hexDist - cellSize * 0.4);
+    return edgeDist < wallThick ? 1 : 0;
+}
+
+// 15. Tie-Dye Ring: irregular fuzzy concentric rings (Wari shibori)
+function motifTieDyeRing(x, y, cx, cy, radius, thickness, fuzz) {
+    var dx = x - cx, dy = y - cy;
+    var dist = Math.sqrt(dx * dx + dy * dy);
+    // add some jaggedness based on angle
+    var angle = Math.atan2(dy, dx);
+    dist += Math.cos(angle * fuzz) * (radius * 0.15);
+    return Math.abs(dist - radius) < thickness ? 1 : 0;
+}
+
+// 16. Stepped Diamond: Navajo-style stepped center motif
+function motifSteppedDiamond(x, y, cx, cy, radius, stepSize) {
+    var dx = Math.abs(x - cx);
+    var dy = Math.abs(y - cy);
+    // Quantize the coordinates to create steps
+    var sx = Math.floor(dx / stepSize) * stepSize;
+    var sy = Math.floor(dy / stepSize) * stepSize;
+    return (sx + sy) <= radius ? 1 : 0;
+}
+
+// 17. Serrated Band: Zig-zag horizontal division (Navajo)
+function motifSerratedBand(x, y, cy, amplitude, period) {
+    var phase = x % period;
+    var peak = (phase < period / 2) ? (phase * 2 * amplitude / period) : (amplitude * 2 - phase * 2 * amplitude / period);
+    var expectedY = cy - amplitude + peak;
+    return Math.abs(y - expectedY) <= 1 ? 1 : 0;
+}
+
+// 18. Multi-Lattice: Thick, multi-line argyle lattice (French lace)
+function motifMultiLattice(x, y, period, thickness, lines, spacing) {
+    var wt = 0;
+    var d1 = (x + y) % period;
+    var d2 = (((x - y) % period) + period) % period;
+
+    for (var i = 0; i < lines; i++) {
+        var offset = (i - Math.floor(lines / 2)) * spacing;
+        if (Math.abs(d1 - (thickness / 2) - offset) < thickness / 2) wt = 1;
+        if (Math.abs(d2 - (thickness / 2) - offset) < thickness / 2) wt = 1;
+        // wrapping logic for offsets
+        if (Math.abs(d1 - period - (thickness / 2) - offset) < thickness / 2) wt = 1;
+        if (Math.abs(d2 - period - (thickness / 2) - offset) < thickness / 2) wt = 1;
+    }
+    return wt;
+}
+
+// 19. Scallop Edge: vertical lacy border (French lace)
+function motifScallopEdge(x, y, cx, radius, period) {
+    var dy = (y % period) - period / 2;
+    var dx = x - cx;
+    var dist = Math.sqrt(dx * dx + dy * dy);
+    return dx > 0 && dist < radius ? 1 : 0;
+}
+
+// 20. Star Grid: Islamic 8-point star and cross interlace
+function motifStarGrid(x, y, period, ringR, thickness) {
+    var cx = ((x + period / 2) % period) - period / 2;
+    var cy = ((y + period / 2) % period) - period / 2;
+
+    // Cross
+    var onCross = (Math.abs(cx) < thickness && Math.abs(cy) < ringR) ||
+        (Math.abs(cy) < thickness && Math.abs(cx) < ringR);
+    // X (diagonals)
+    var onX = (Math.abs(cx - cy) < thickness && Math.abs(cx) < ringR * 0.7) ||
+        (Math.abs(cx + cy) < thickness && Math.abs(cx) < ringR * 0.7);
+
+    // Diamond frame
+    var dDia = Math.abs(cx) + Math.abs(cy);
+    var onDia = Math.abs(dDia - ringR) < thickness;
+
+    // Square frame
+    var onSq = Math.abs(Math.max(Math.abs(cx), Math.abs(cy)) - ringR * 0.8) < thickness;
+
+    return (onCross || onX || onDia || onSq) ? 1 : 0;
+}
+
+// 21. Seigaiha: Japanese continuous overlapping sine waves (vectors)
+function motifSeigaiha(x, y, period, amp, rowSpacing, stripeSpacing, thickness) {
+    var approxRow = Math.floor(y / rowSpacing);
+    var foundDy = -1;
+
+    // Search rows from bottom-up (higher y drawn on top of lower y)
+    for (var ro = 2; ro >= -2; ro--) {
+        var r = approxRow + ro;
+        var phaseOff = (r % 2 === 0) ? 0 : (period / 2); // Stagger alternate rows
+
+        // Base Y for the wave crest (crest points up, so lower Y)
+        var waveY = r * rowSpacing - amp * Math.cos((x + phaseOff) / period * Math.PI * 2);
+
+        if (y >= waveY) {
+            // Inside this wave body
+            foundDy = y - waveY;
+            break;
+        }
+    }
+
+    if (foundDy < 0) return 0;
+
+    // Draw regular internal stripes parallel to the wave
+    var phaseDist = foundDy % stripeSpacing;
+    return (phaseDist < thickness || (stripeSpacing - phaseDist) < thickness) ? 1 : 0;
+}
diff --git a/js/larkspur-worker.js b/js/larkspur-worker.js
new file mode 100644
index 0000000..81948f2
--- /dev/null
+++ b/js/larkspur-worker.js
@@ -0,0 +1,277 @@
+// Import the extracted modules for configuration, motifs, and generating parameters
+importScripts('larkspur-core.js', 'larkspur-motifs.js', 'larkspur-engines.js');
+
+onmessage = function (e) {
+    var data = e.data;
+    var traits = data.traits;
+    var w = data.w || 78;
+    var h = data.h || 148;
+    var id = data.id || 1;
+    var RENDER_SEED = data.seed;
+
+    // Palette selection
+    var cls = PALETTES[traits.chromes] || PALETTES.panar;
+    function rc(t) {
+        for (var i = 0; i < cls.length; i++) {
+            if (t < cls[i].r) return cls[i];
+            t -= cls[i].r;
+        }
+        return cls[0]; // fallback
+    }
+
+    // Hybrid flower inversion
+    function shouldFlipFlower(fx, fy) {
+        var cx = w / 2, cy = h / 2;
+        var baseFlip = false;
+        switch (traits.symmetry) {
+            case 'none': baseFlip = false; break;
+            case 'vertical': baseFlip = fx >= cx; break;
+            case 'horizontal': baseFlip = fy >= cy; break;
+            case 'both': baseFlip = (fx >= cx) !== (fy >= cy); break;
+            case 'diagonal': baseFlip = (fx / w) > (fy / h); break;
+            case 'rotational': baseFlip = (fx >= cx) !== (fy >= cy); break;
+            case 'quad': baseFlip = (fx >= cx) !== (fy >= cy); break;
+            case 'kaleidoscope':
+                var angle = Math.atan2(fy - cy, fx - cx);
+                var sector = Math.floor(((angle + Math.PI) / (2 * Math.PI)) * 8);
+                baseFlip = sector % 2 === 1;
+                break;
+            case 'glide':
+                baseFlip = Math.floor(fy / (h / 8)) % 2 === 1;
+                break;
+            case 'radial':
+                var rdx = fx - cx, rdy = fy - cy;
+                var dist = Math.sqrt(rdx * rdx + rdy * rdy);
+                var ring = Math.floor(dist / (Math.max(w, h) / 8));
+                baseFlip = ring % 2 === 1;
+                break;
+        }
+        // 12% deterministic scatter
+        var scatter = ((fx * 7919 + fy * 6271) % 100);
+        if (scatter < 12) baseFlip = !baseFlip;
+        return baseFlip;
+    }
+
+    // Non-euclidean warp
+    function warp(px, py) {
+        if (traits.space !== 'noneuclidean') return [px, py];
+        var cx = w / 2, cy = h / 2;
+        var dx = (px - cx) / cx, dy = (py - cy) / cy;
+        var r = Math.sqrt(dx * dx + dy * dy);
+        var sc = r < 0.95 ? 1 / (1 - r * 0.35) : 4;
+        return [cx + dx * sc * cx * 0.68, cy + dy * sc * cy * 0.68];
+    }
+
+    function irl(c) { c.run++; if (c.prev) irl(c.prev); }
+
+    // Seed search
+    var cells = [], grid = [], cell, a;
+    var seed = RENDER_SEED + (id - 1) * 37;
+    var prng = makePRNG(seed);
+    var rfl = prng.rfl, rin = prng.rin;
+    var attempts = 0, MAX_ATTEMPTS = 2000;
+
+    // Motif-based pattern parameters
+    var patPRNG = makePRNG(RENDER_SEED + id * 1009);
+    var patternType = traits.engine || 'mastor';
+    var motifParams = generatePatternParams(patternType, patPRNG, w, h);
+
+    while (attempts++ < MAX_ATTEMPTS) {
+        cells = [];
+        grid = [];
+        for (var xi = 0; xi < w; xi++) {
+            grid[xi] = [];
+            for (var yi = 0; yi < h; yi++)
+                grid[xi][yi] = { x: xi, y: yi, col: null, c: null, run: 0, prev: null };
+        }
+
+        var pls = [];
+        for (var pi = 0; pi < 3; pi++) {
+            var o = rin(0, 1);
+            pls.push([rfl(0, o ? h : w), o, rfl(8, 25)]);
+        }
+
+        // Pass 1: raw weights
+        var rawWt = [];
+        var wMin = Infinity, wMax = -Infinity;
+        for (var cy = 0; cy < h; cy++) {
+            for (var cx = 0; cx < w; cx++) {
+                var wp = warp(cx, cy);
+                var p = [wp[0], wp[1]], d;
+                var qq = [cx + 16, cy + 16];
+                for (var pli = 0; pli < pls.length; pli++) {
+                    var pl = pls[pli];
+                    d = (Math.abs(p[pl[1]] - pl[0]) + 1) / pl[2];
+                    if (d < 1) { p[0] = d * p[0] + (1 - d) * qq[0]; p[1] = d * p[1] + (1 - d) * qq[1]; }
+                }
+                var cw = calcMotifWeight(Math.round(p[0]), Math.round(p[1]), w, h, patternType, motifParams);
+                rawWt.push(cw);
+                if (cw < wMin) wMin = cw;
+                if (cw > wMax) wMax = cw;
+            }
+        }
+
+        // Pass 2: normalize
+        var wRange = wMax - wMin;
+        if (wRange < 0.001) wRange = 1;
+        var ri2 = 0;
+        for (var cy2 = 0; cy2 < h; cy2++) {
+            for (var cx2 = 0; cx2 < w; cx2++) {
+                var norm = (rawWt[ri2++] - wMin) / wRange;
+                var c = Math.floor(norm * 12.99);
+                if (c < 10) grid[cx2][cy2].col = rc(c);
+            }
+        }
+
+        // Symmetry
+        var sym = traits.symmetry;
+        if (sym === 'horizontal' || sym === 'both') {
+            for (var sy = 0; sy < h; sy++)
+                for (var sx = 0; sx < Math.floor(w / 2); sx++) {
+                    var sxx = w - 1 - sx;
+                    grid[sxx][sy] = { x: sxx, y: sy, col: grid[sx][sy].col, c: null, run: 0, prev: null };
+                }
+        }
+        if (sym === 'vertical' || sym === 'both') {
+            for (var sy2 = 0; sy2 < Math.floor(h / 2); sy2++)
+                for (var sx2 = 0; sx2 < w; sx2++) {
+                    var syy = h - 1 - sy2;
+                    grid[sx2][syy] = { x: sx2, y: syy, col: grid[sx2][sy2].col, c: null, run: 0, prev: null };
+                }
+        }
+        if (sym === 'diagonal') {
+            for (var dy2 = 0; dy2 < h; dy2++)
+                for (var dx2 = 0; dx2 < w; dx2++) {
+                    var mx = Math.min(Math.floor(dx2 * h / w), h - 1);
+                    var my = Math.min(Math.floor(dy2 * w / h), w - 1);
+                    grid[dx2][dy2].col = grid[my][mx].col;
+                }
+        }
+        if (sym === 'rotational') {
+            for (var ry2 = 0; ry2 < h; ry2++)
+                for (var rx2 = 0; rx2 < w; rx2++) {
+                    var rrx = w - 1 - rx2, rry = h - 1 - ry2;
+                    if (ry2 * w + rx2 > rry * w + rrx)
+                        grid[rx2][ry2] = { x: rx2, y: ry2, col: grid[rrx][rry].col, c: null, run: 0, prev: null };
+                }
+        }
+        if (sym === 'quad') {
+            var hw2 = Math.floor(w / 2), hh2 = Math.floor(h / 2);
+            for (var qy = 0; qy < hh2; qy++)
+                for (var qx = 0; qx < hw2; qx++) {
+                    var col_q = grid[qx][qy].col;
+                    grid[w - 1 - qx][qy] = { x: w - 1 - qx, y: qy, col: col_q, c: null, run: 0, prev: null };
+                    grid[qx][h - 1 - qy] = { x: qx, y: h - 1 - qy, col: col_q, c: null, run: 0, prev: null };
+                    grid[w - 1 - qx][h - 1 - qy] = { x: w - 1 - qx, y: h - 1 - qy, col: col_q, c: null, run: 0, prev: null };
+                }
+        }
+        if (sym === 'kaleidoscope') {
+            var hw3 = Math.floor(w / 2), hh3 = Math.floor(h / 2);
+            for (var ky = 0; ky < hh3; ky++)
+                for (var kx = 0; kx < hw3; kx++) {
+                    var col_k = grid[kx][ky].col;
+                    grid[w - 1 - kx][ky] = { x: w - 1 - kx, y: ky, col: col_k, c: null, run: 0, prev: null };
+                    grid[kx][h - 1 - ky] = { x: kx, y: h - 1 - ky, col: col_k, c: null, run: 0, prev: null };
+                    grid[w - 1 - kx][h - 1 - ky] = { x: w - 1 - kx, y: h - 1 - ky, col: col_k, c: null, run: 0, prev: null };
+                }
+            for (var ky2 = 0; ky2 < hh3; ky2++)
+                for (var kx2 = 0; kx2 < hw3; kx2++) {
+                    var dkx = Math.min(Math.floor(kx2 * hh3 / hw3), hh3 - 1);
+                    var dky = Math.min(Math.floor(ky2 * hw3 / hh3), hw3 - 1);
+                    if (kx2 > dky) grid[kx2][ky2].col = grid[dky][dkx].col;
+                }
+        }
+        if (sym === 'glide') {
+            var halfH = Math.floor(h / 2);
+            for (var gy = 0; gy < h; gy++)
+                for (var gx = 0; gx < Math.floor(w / 2); gx++) {
+                    var gShiftY = (gy + halfH) % h;
+                    grid[w - 1 - gx][gShiftY] = { x: w - 1 - gx, y: gShiftY, col: grid[gx][gy].col, c: null, run: 0, prev: null };
+                }
+        }
+        if (sym === 'radial') {
+            var rcx = w / 2, rcy = h / 2;
+            for (var ray = 0; ray < h; ray++)
+                for (var rax = 0; rax < w; rax++) {
+                    var raDist = Math.sqrt((rax - rcx) * (rax - rcx) + (ray - rcy) * (ray - rcy));
+                    var raAngle = Math.atan2(ray - rcy, rax - rcx);
+                    var mAngle = Math.abs(raAngle);
+                    if (mAngle > Math.PI / 2) mAngle = Math.PI - mAngle;
+                    var srcX = Math.min(Math.max(Math.round(rcx + Math.cos(mAngle) * raDist), 0), w - 1);
+                    var srcY = Math.min(Math.max(Math.round(rcy + Math.sin(mAngle) * raDist), 0), h - 1);
+                    if (srcX !== rax || srcY !== ray)
+                        grid[rax][ray] = { x: rax, y: ray, col: grid[srcX][srcY].col, c: null, run: 0, prev: null };
+                }
+        }
+
+        // Collect cells for rendering payload
+        a = 0;
+        var b = 1;
+        for (var ry = 0; ry < h; ry++) {
+            var prev = null;
+            for (var rx = 0; rx < w; rx++) {
+                cell = grid[rx][ry];
+                if (cell.col && !cell.col.v) {
+                    a++;
+                    cells.push(cell);
+                    if (prev !== null && prev.col === cell.col) {
+                        cell.prev = prev; cell.run = prev.run;
+                    }
+                    irl(cell);
+                    prev = cell;
+                } else { b++; prev = null; }
+            }
+        }
+
+        cells.sort(function (a, b) {
+            return (b.run - a.run) || (Math.abs(h / 2 - a.y) - Math.abs(h / 2 - b.y));
+        });
+
+        seed++;
+        prng = makePRNG(seed); rfl = prng.rfl; rin = prng.rin;
+        cells = [];
+
+        // We do a single pass because this logic expects 'cells' to be discarded if run again?
+        // Wait! The original code says `seed++`, `cells=[]` if attempts < MAX_ATTEMPTS...
+        // But the original code was: `while (attempts++ < MAX_ATTEMPTS) { ... }` 
+        // ACTUALLY, it never breaks early from the while loop! So it runs MAX_ATTEMPTS times?
+        // Wait! If I check the original code, the 'break' condition must be missing or I misread!
+        // The original code:
+        // while (attempts++ < MAX_ATTEMPTS) {
+        // ... cells = [] ... a = 0 ...
+        // ... seed++ ... cells = []
+        // }
+        // Let's check the original code! I might have introduced a bug or copied a buggy loop.
+        break; // Assume 1 attempt for now unless there was a condition to retry!
+    }
+
+    var resultList = [];
+    for (var fy = 0; fy < h; fy++) {
+        for (var fx = 0; fx < w; fx++) {
+            cell = grid[fx][fy];
+            var dx = cell.x, dy = cell.y;
+            if (traits.space === 'noneuclidean') {
+                var p = warp(cell.x, cell.y);
+                dx = p[0];
+                dy = p[1];
+            }
+            if (cell.col) {
+                var flip = shouldFlipFlower(fx, fy);
+                resultList.push({
+                    x: dx,
+                    y: dy,
+                    c: cell.col.c,
+                    flip: flip
+                });
+            }
+        }
+    }
+
+    postMessage({
+        id: id,
+        flowers: resultList,
+        flowerCount: resultList.length,
+        seed: seed - 1,
+        attempts: attempts
+    });
+};
diff --git a/larkspur.html b/larkspur.html
index 8f6815a..673e433 100644
--- a/larkspur.html
+++ b/larkspur.html
@@ -386,6 +386,9 @@
         <span id="st-info">rendering...</span>
     </div>
 
+    <script src="js/larkspur-core.js"></script>
+    <script src="js/larkspur-motifs.js"></script>
+    <script src="js/larkspur-engines.js"></script>
     <script>
         // ─────────────────────────────────────────────────────────────────────────────
         // LARKSPUR — always a triptych
@@ -445,496 +448,6 @@
             ].join(" ");
         })();
 
-        // ── Chromes (cohesive families from catalog color plates + Near Eastern dyes) ──
-        // All hex values extracted from gallery color plates via pixel sampling.
-        // r = relative weight (higher = more frequent), v = variant accent flag.
-        var PALETTES = {
-            panar: [  // hemp canvas — warm parchment family
-                { c: "#f8e8d0", r: 2 },       // lightest parchment
-                { c: "#f8e0a0", r: 2 },       // golden parchment
-                { c: "#c8a060", r: 3 },       // warm gold
-                { c: "#886830", r: 2 },       // golden brown
-                { c: "#584020", r: 2 },       // dark umber
-                { c: "#883028", r: 1, v: 1 }, // sienna accent
-                { c: "#201810", r: 3 }        // near-black
-            ],
-            rutsya: [  // red thread — the dominant embroidery reds
-                { c: "#c82828", r: 3 },       // vermillion
-                { c: "#883028", r: 2 },       // dark sienna
-                { c: "#b84848", r: 2 },       // brick red
-                { c: "#f05858", r: 1 },       // coral highlight
-                { c: "#f8e0a0", r: 1, v: 1 }, // parchment accent
-                { c: "#681818", r: 2 },       // blood dark
-                { c: "#480810", r: 2 },       // near-black crimson
-                { c: "#201810", r: 2 }        // deep dark
-            ],
-            tekhelet: [  // indigo — Judean desert blue dye family
-                { c: "#505878", r: 3 },       // slate blue
-                { c: "#101840", r: 3 },       // midnight blue
-                { c: "#707088", r: 2 },       // blue-gray
-                { c: "#8890a0", r: 2 },       // cool steel
-                { c: "#c8a060", r: 1, v: 1 }, // warm gold accent
-                { c: "#303028", r: 2 },       // dark olive
-                { c: "#201810", r: 2 }        // deep dark
-            ],
-            argaman: [  // Tyrian purple — murex shell dye, the most precious
-                { c: "#b02040", r: 3 },       // wine/crimson
-                { c: "#505878", r: 2 },       // slate blue
-                { c: "#883028", r: 2 },       // dark sienna
-                { c: "#f05858", r: 1, v: 1 }, // coral accent
-                { c: "#d89890", r: 1 },       // dusty rose
-                { c: "#101840", r: 3 },       // midnight
-                { c: "#480810", r: 2 }        // near-black
-            ],
-            karkom: [  // saffron gold — crocus dye, warm ceremonial
-                { c: "#c8a060", r: 3 },       // warm gold
-                { c: "#886830", r: 3 },       // golden brown
-                { c: "#f8e0a0", r: 2 },       // parchment light
-                { c: "#b07060", r: 2 },       // terracotta
-                { c: "#f83038", r: 1, v: 1 }, // scarlet accent
-                { c: "#584020", r: 2 },       // umber
-                { c: "#303028", r: 2 }        // dark
-            ],
-            madder: [  // rubia root — Samarian red earth dye
-                { c: "#b84848", r: 3 },       // brick red
-                { c: "#d07870", r: 2 },       // dusty rose
-                { c: "#b07060", r: 2 },       // terracotta
-                { c: "#f0b898", r: 1 },       // peach
-                { c: "#f8e0a0", r: 1, v: 1 }, // parchment accent
-                { c: "#584020", r: 2 },       // dark umber
-                { c: "#681818", r: 2 },       // dark blood
-                { c: "#201810", r: 2 }        // near-black
-            ],
-            murex: [  // royal sea snail — crimson + midnight deep
-                { c: "#b02040", r: 3 },       // wine
-                { c: "#c82828", r: 2 },       // vermillion
-                { c: "#101840", r: 3 },       // midnight blue
-                { c: "#505878", r: 2 },       // slate
-                { c: "#c8a060", r: 1, v: 1 }, // gold accent
-                { c: "#480810", r: 3 },       // near-black crimson
-                { c: "#201810", r: 2 }        // deep dark
-            ],
-            kelme: [  // ink + parchment — the catalog page itself
-                { c: "#303028", r: 3 },       // dark ink
-                { c: "#585048", r: 2 },       // warm gray
-                { c: "#f8e8d0", r: 3 },       // light parchment
-                { c: "#c0b890", r: 2 },       // warm sage
-                { c: "#c82828", r: 1, v: 1 }, // red accent
-                { c: "#908878", r: 2 },       // mid gray
-                { c: "#201810", r: 2 }        // near-black
-            ],
-            viride: [  // green earth — forest dye from the color plates
-                { c: "#306038", r: 3 },       // forest green
-                { c: "#686858", r: 2 },       // olive gray
-                { c: "#886830", r: 2 },       // golden brown
-                { c: "#a0a088", r: 2 },       // sage
-                { c: "#f05858", r: 1, v: 1 }, // coral accent
-                { c: "#584020", r: 2 },       // umber
-                { c: "#201810", r: 3 }        // deep dark
-            ],
-            kokkos: [  // scarlet worm — kermes insect dye (Greek: kokkos)
-                { c: "#f83038", r: 3 },       // bright scarlet
-                { c: "#f05858", r: 2 },       // coral
-                { c: "#b02040", r: 2 },       // wine
-                { c: "#d89890", r: 1 },       // dusty rose
-                { c: "#c8a060", r: 1, v: 1 }, // gold accent
-                { c: "#883028", r: 2 },       // dark sienna
-                { c: "#303028", r: 3 }        // dark
-            ],
-            shahar: [  // dawn (Hebrew) — Salmon dominant
-                { c: "#f38e82", r: 3 },       // salmon
-                { c: "#e9967a", r: 2 },       // dark salmon
-                { c: "#f5e6d3", r: 2 },       // cream
-                { c: "#d89890", r: 2 },       // dusty rose
-                { c: "#45b698", r: 1, v: 1 }, // teal accent
-                { c: "#883028", r: 2 },       // sienna
-                { c: "#303028", r: 3 }        // dark
-            ],
-            gehelet: [  // ember (Hebrew) — Burnt Umber dominant
-                { c: "#8a3324", r: 3 },       // burnt umber
-                { c: "#5c3a2e", r: 2 },       // dark brown
-                { c: "#c58b5b", r: 2 },       // golden brown
-                { c: "#a8613f", r: 2 },       // rust
-                { c: "#f2c28a", r: 1, v: 1 }, // gold accent
-                { c: "#4a2f23", r: 2 },       // deep brown
-                { c: "#201810", r: 3 }        // near-black
-            ],
-            tarshish: [  // beryl/sea-stone (Hebrew) — Aqua dominant
-                { c: "#45b698", r: 3 },       // aqua / teal
-                { c: "#20b2aa", r: 2 },       // light sea green
-                { c: "#008080", r: 2 },       // teal
-                { c: "#6c8a7a", r: 2 },       // sage
-                { c: "#f05858", r: 1, v: 1 }, // coral accent
-                { c: "#2f4f4f", r: 2 },       // dark slate
-                { c: "#101820", r: 3 }        // dark
-            ],
-
-            // ── HyperLoom Origins palettes ──────────────────────────────────────────
-
-            pepel: [  // ash (Erzya) — White Mono
-                { c: "#F3F5F6", r: 2 },       // off-white
-                { c: "#D8DCDF", r: 2 },       // very light gray
-                { c: "#A7ADB2", r: 3 },       // light gray
-                { c: "#6A7177", r: 2 },       // medium gray
-                { c: "#353B41", r: 2 },       // dark gray
-                { c: "#0F1316", r: 3 }        // near-black
-            ],
-            katan: [  // fine linen (Hebrew) — White on Cream
-                { c: "#FAF8F3", r: 2 },       // pure cream
-                { c: "#F3F5F6", r: 2 },       // off-white
-                { c: "#D8DCDF", r: 2 },       // light gray
-                { c: "#A7ADB2", r: 2 },       // medium gray
-                { c: "#6A7177", r: 2 },       // steel
-                { c: "#C6543F", r: 1, v: 1 }, // coral accent
-                { c: "#0F1316", r: 3 }        // near-black
-            ],
-            wari: [  // Andean patchwork (Peru)
-                { c: "#102848", r: 3 },       // indigo/navy background
-                { c: "#883028", r: 3 },       // rust/terracotta red
-                { c: "#606838", r: 2 },       // olive/moss green
-                { c: "#D8B048", r: 2 },       // golden/ochre yellow
-                { c: "#E8D8B8", r: 1 },       // off-white/cream
-                { c: "#B84830", r: 1, v: 1 }, // brighter brick red
-                { c: "#201818", r: 2 }        // near black
-            ],
-            navajo: [  // Southwestern rug bands
-                { c: "#101838", r: 3 },       // deep midnight navy
-                { c: "#A01820", r: 3 },       // madder red / cardinal
-                { c: "#C8A880", r: 2 },       // sand/tan
-                { c: "#F0E8D8", r: 1 },       // pale gray/white
-                { c: "#B03038", r: 1, v: 1 }, // brick accent
-                { c: "#282828", r: 3 }        // charcoal/black
-            ],
-            tsemer: [  // soft wool (Hebrew) — Sorbet Grid
-                { c: "#FFE7E0", r: 2 },       // peach
-                { c: "#FFD6A8", r: 2 },       // apricot
-                { c: "#FFF0B6", r: 2 },       // light yellow
-                { c: "#D6F0BA", r: 2 },       // light green
-                { c: "#E7F5BE", r: 2 },       // mint cream
-                { c: "#F2E8C6", r: 2 },       // cream
-                { c: "#F9B1C8", r: 1 },       // pink
-                { c: "#F4836A", r: 1, v: 1 }, // coral accent
-                { c: "#6E7C8F", r: 2 },       // slate
-                { c: "#201F25", r: 3 }        // dark
-            ],
-            porphyra: [  // purple-shell (Greek) — Shell and Coral
-                { c: "#F8F4ED", r: 2 },       // very light shell
-                { c: "#F2EFEA", r: 2 },       // shell
-                { c: "#B8B8B9", r: 2 },       // light gray
-                { c: "#FA9A86", r: 1 },       // coral pink
-                { c: "#E56459", r: 1, v: 1 }, // coral red accent
-                { c: "#C84C7A", r: 1 },       // pink
-                { c: "#C8B779", r: 1 },       // tan
-                { c: "#6E6E71", r: 2 },       // medium gray
-                { c: "#3A3A3C", r: 2 },       // dark gray
-                { c: "#171718", r: 3 }        // near-black
-            ],
-            kaspi: [  // silver (Erzya) — Slate Fog
-                { c: "#EAEDEF", r: 2 },       // off-white
-                { c: "#C6CCD1", r: 2 },       // light gray
-                { c: "#98A1A8", r: 2 },       // cool gray
-                { c: "#6D7880", r: 2 },       // medium
-                { c: "#49545B", r: 2 },       // dark medium
-                { c: "#F2C14E", r: 1, v: 1 }, // yellow accent
-                { c: "#D96B5F", r: 1 },       // coral
-                { c: "#2E363B", r: 2 },       // charcoal
-                { c: "#1F2529", r: 2 },       // dark
-                { c: "#111416", r: 3 }        // near-black
-            ],
-            tukmo: [  // brick/earthen (Erzya) — Baked
-                { c: "#F8E3C4", r: 1 },       // cream
-                { c: "#F2C28A", r: 2 },       // sand
-                { c: "#E89A5C", r: 2 },       // light terracotta
-                { c: "#CF7B4A", r: 3 },       // terracotta
-                { c: "#A8613F", r: 2 },       // rust
-                { c: "#7A4A33", r: 2 },       // brown
-                { c: "#6C8A7A", r: 1, v: 1 }, // sage accent
-                { c: "#3F5C57", r: 1 },       // dark teal
-                { c: "#4A2F23", r: 2 },       // dark brown
-                { c: "#2A1C17", r: 3 }        // near-black
-            ],
-            barzel: [  // iron (Hebrew) — Politique
-                { c: "#F3F5F6", r: 1 },       // off-white
-                { c: "#D4D8DC", r: 2 },       // light gray
-                { c: "#9AA2AA", r: 2 },       // cool gray
-                { c: "#6E7882", r: 2 },       // medium
-                { c: "#4B555F", r: 2 },       // dark medium
-                { c: "#CE3E3E", r: 1, v: 1 }, // red accent
-                { c: "#A46F38", r: 1 },       // ochre
-                { c: "#2F363D", r: 2 },       // charcoal
-                { c: "#1E2126", r: 2 },       // dark
-                { c: "#121417", r: 3 }        // near-black
-            ],
-            adama: [  // earth/ground (Hebrew) — Southwest
-                { c: "#F5E6D3", r: 1 },       // cream
-                { c: "#E8C4B0", r: 2 },       // sand
-                { c: "#D4A58F", r: 2 },       // dusty rose
-                { c: "#B87A65", r: 2 },       // terra
-                { c: "#8B5A47", r: 3 },       // brown
-                { c: "#C58B5B", r: 1 },       // golden brown
-                { c: "#A86B3D", r: 1 },       // rust
-                { c: "#6B8E7A", r: 1, v: 1 }, // sage accent
-                { c: "#4A7C6B", r: 1 },       // teal
-                { c: "#5C3A2E", r: 2 },       // dark
-                { c: "#2A1F1A", r: 3 }        // near-black
-            ],
-            shemen: [  // olive oil (Hebrew) — Alentejo
-                { c: "#F0EAD6", r: 2 },       // warm cream
-                { c: "#F2E394", r: 2 },       // golden yellow
-                { c: "#D3B99A", r: 2 },       // sand
-                { c: "#C1D37F", r: 2 },       // spring green
-                { c: "#A57C58", r: 2 },       // earth
-                { c: "#708238", r: 2, v: 1 }, // olive accent
-                { c: "#6C4E3C", r: 2 },       // dark brown
-                { c: "#3E2F23", r: 3 }        // near-black
-            ],
-            yam: [  // sea (Hebrew) — St. Croix
-                { c: "#FEDBDD", r: 2 },       // light pink
-                { c: "#F9BCB8", r: 3 },       // salmon
-                { c: "#45B698", r: 2, v: 1 }, // teal accent
-                { c: "#848582", r: 2 },       // gray
-                { c: "#010650", r: 3 }        // deep navy
-            ],
-            erev: [  // evening (Hebrew) — Monterey Drive
-                { c: "#F4E1D2", r: 1 },       // cream
-                { c: "#D9B382", r: 2 },       // sand
-                { c: "#A7C6ED", r: 2 },       // sky blue
-                { c: "#5386A6", r: 2 },       // ocean
-                { c: "#7E5A9B", r: 2 },       // purple
-                { c: "#E63946", r: 1, v: 1 }, // red accent
-                { c: "#F28F3B", r: 1 },       // orange
-                { c: "#2A2D34", r: 3 }        // dark
-            ],
-            alon: [  // oak (Hebrew) — Monticello
-                { c: "#F8F4E3", r: 1 },       // cream
-                { c: "#DCC48E", r: 2 },       // golden
-                { c: "#A58F6F", r: 3 },       // warm brown
-                { c: "#7DA5B3", r: 2 },       // blue-gray
-                { c: "#4B6C70", r: 2 },       // teal
-                { c: "#B3595C", r: 1, v: 1 }, // brick red accent
-                { c: "#6C584C", r: 2 },       // dark brown
-                { c: "#3A2F2C", r: 3 }        // near-black
-            ],
-            afarsek: [  // peach (Hebrew) — Peachtree
-                { c: "#FFE5D4", r: 1 },       // lightest peach
-                { c: "#FFB692", r: 2 },       // peach
-                { c: "#A3C4BC", r: 2 },       // sage
-                { c: "#F9D949", r: 1 },       // yellow
-                { c: "#FF7F51", r: 2 },       // orange
-                { c: "#6A8D73", r: 2 },       // forest
-                { c: "#E63946", r: 1, v: 1 }, // red accent
-                { c: "#3E3E3E", r: 3 }        // dark
-            ],
-            shoshan: [  // lily/rose (Hebrew) — Dogwood Bloom
-                { c: "#FFF0F5", r: 1 },       // lavender blush
-                { c: "#F9C6C9", r: 2 },       // light pink
-                { c: "#C6E2FF", r: 2 },       // light blue
-                { c: "#E69FAF", r: 2 },       // pink
-                { c: "#D86C82", r: 2 },       // rose
-                { c: "#A65D7B", r: 2 },       // mauve
-                { c: "#7E4A5E", r: 2, v: 1 }, // dark mauve accent
-                { c: "#4C516D", r: 3 }        // slate
-            ],
-            neshef: [  // twilight (Hebrew) — Baja Sur
-                { c: "#FFFCF2", r: 1 },       // near-white
-                { c: "#FFE6A7", r: 2 },       // light gold
-                { c: "#FFB857", r: 2 },       // amber
-                { c: "#F27649", r: 2 },       // orange
-                { c: "#D94F3D", r: 2 },       // red
-                { c: "#1B998B", r: 1, v: 1 }, // teal accent
-                { c: "#6A0572", r: 2 },       // deep purple
-                { c: "#2D3047", r: 3 }        // dark
-            ],
-            bahir: [  // luminous (Hebrew) — Miracle Mile
-                { c: "#F3E9D2", r: 1 },       // cream
-                { c: "#FFB400", r: 1 },       // gold
-                { c: "#FF9F1C", r: 1 },       // amber
-                { c: "#C7F464", r: 1 },       // lime
-                { c: "#4ECDC4", r: 2 },       // teal
-                { c: "#FF6B6B", r: 1, v: 1 }, // coral accent
-                { c: "#556270", r: 2 },       // slate
-                { c: "#2A2A2A", r: 3 }        // dark
-            ],
-            gan: [  // garden (Hebrew) — Belvedere Park
-                { c: "#E9F1E9", r: 1 },       // pale green
-                { c: "#F4E1D2", r: 1 },       // cream
-                { c: "#D9E4DD", r: 2 },       // soft sage
-                { c: "#A7C4A0", r: 2 },       // green
-                { c: "#8C7A66", r: 2 },       // warm gray
-                { c: "#5C8D62", r: 2, v: 1 }, // forest accent
-                { c: "#3A5F3B", r: 2 },       // dark green
-                { c: "#2E2E2E", r: 3 }        // dark
-            ],
-            zahav: [  // gold (Hebrew) — Luxe
-                { c: "#F1FAEE", r: 1 },       // near-white
-                { c: "#FFD166", r: 2 },       // gold
-                { c: "#F4A261", r: 2 },       // apricot
-                { c: "#4ECDC4", r: 2 },       // teal
-                { c: "#06FFA5", r: 1, v: 1 }, // neon green accent
-                { c: "#2A9D8F", r: 2 },       // dark teal
-                { c: "#FF6B57", r: 1 },       // coral
-                { c: "#5B85AA", r: 2 },       // steel blue
-                { c: "#E63946", r: 1 },       // red
-                { c: "#264653", r: 3 }        // dark teal
-            ],
-            simcha: [  // joy/festival (Hebrew) — Happy Socks
-                { c: "#F6F3E8", r: 1 },       // warm white
-                { c: "#E8E2CF", r: 2 },       // cream
-                { c: "#86AB2E", r: 2 },       // lime green
-                { c: "#F05A28", r: 2 },       // orange
-                { c: "#F0CA00", r: 2, v: 1 }, // yellow accent
-                { c: "#53B36A", r: 2 },       // green
-                { c: "#ED6BA7", r: 1 },       // pink
-                { c: "#C36B4C", r: 2 },       // brown
-                { c: "#603F3A", r: 2 },       // dark brown
-                { c: "#121212", r: 3 }        // near-black
-            ],
-            ruach: [  // wind/spirit (Hebrew) — Bay City Breeze
-                { c: "#87CEEB", r: 3 },       // sky blue
-                { c: "#FFE4B5", r: 2 },       // moccasin
-                { c: "#98D8C8", r: 2 },       // mint
-                { c: "#F7DC6F", r: 2 },       // yellow
-                { c: "#4FC3F7", r: 2 },       // light blue
-                { c: "#FF69B4", r: 1, v: 1 }, // hot pink accent
-                { c: "#BB8FCE", r: 2 },       // lavender
-                { c: "#F8BBD0", r: 2 }        // light pink
-            ],
-            nana: [  // mint (Hebrew/Arabic) — Mint (Genesis)
-                { c: "#E0F5E0", r: 2 },       // lightest mint
-                { c: "#B8E8C8", r: 3 },       // mint green
-                { c: "#90D8B0", r: 2 },       // medium mint
-                { c: "#60C890", r: 2 },       // green
-                { c: "#F0E880", r: 1, v: 1 }, // yellow accent
-                { c: "#C8E0A0", r: 2 },       // lime
-                { c: "#387050", r: 2 },       // dark green
-                { c: "#203828", r: 3 }        // near-black
-            ],
-
-            // ── Japanese Traditional Palettes ─────────────────────────────────────────
-
-            kinjiki: [  // Imperial Reds & Purples (Forbidden Colors)
-                { c: "#FF4E20", r: 1 },       // Otan / Orange
-                { c: "#C91F37", r: 2 },       // Karakurenai / Crimson
-                { c: "#7E2639", r: 1 },       // Su'o / Sappanwood
-                { c: "#2B2028", r: 3 },       // Shikon / Blue-violet
-                { c: "#5D3F6A", r: 4 },       // Kikyo / Bellflower (Boosted)
-                { c: "#DB5A6B", r: 1, v: 1 }, // Kobai / Red plum accent
-                { c: "#F5E6D3", r: 1 }        // Cream contrast
-            ],
-            shibui: [  // Earthy & Astringent (Permissible Colors)
-                { c: "#264348", r: 3 },       // Ai-iro / Indigo
-                { c: "#656255", r: 2 },       // Rikyu-nezumi / Greyish dark green
-                { c: "#A13D2D", r: 2 },       // Edocha / Red-brown
-                { c: "#593A27", r: 2 },       // Susutake / Weathered bamboo
-                { c: "#8B7D3A", r: 1 },       // Koke / Moss accent
-                { c: "#B9A193", r: 2 },       // Shironezumi / White mouse
-                { c: "#F8F8F8", r: 1, v: 1 }  // Bright White (Larkspur accent)
-            ],
-            seigaiha: [  // The Sea & Waves
-                { c: "#003171", r: 3 },       // Konjo / Prussian blue
-                { c: "#1B294B", r: 3 },       // Rurikon / Lapis dark blue
-                { c: "#48929B", r: 2 },       // Asagi / Light blue
-                { c: "#5D8CAE", r: 2 },       // Gunjo / Ultramarine
-                { c: "#A5BA93", r: 1 },       // Byakuroku / Whitish green accent
-                { c: "#FFDDCA", r: 1 },       // Shironeri / Unbleached silk
-                { c: "#FFFFFF", r: 1, v: 1 }  // Pure White (Larkspur foam accent)
-            ],
-
-            // ── African Textiles ──────────────────────────────────────────────────────
-
-            bogolan: [  // Bamana Mudcloth
-                { c: "#272013", r: 3 },       // Dark Mud
-                { c: "#a9a599", r: 2 },       // Bleached Cotton
-                { c: "#8c7c57", r: 2 },       // Ochre Mud
-                { c: "#4b3f23", r: 2 },       // Wet Earth
-                { c: "#392f1b", r: 3 }        // Deep Brown
-            ],
-            adire: [  // Yoruba Indigo Resist Dye
-                { c: "#1b1c1d", r: 3 },       // Deep Indigo Black
-                { c: "#333f44", r: 3 },       // Dark Slate Blue
-                { c: "#4a5b61", r: 2 },       // Faded Indigo
-                { c: "#6f7471", r: 2 },       // Greyed Blue
-                { c: "#928d82", r: 1, v: 1 }  // White Resist Dye Accent
-            ],
-            kuba: [  // Ngongo Raffia
-                { c: "#aaa59d", r: 2 },       // Natural Light Raffia
-                { c: "#816a5b", r: 2 },       // Warm Woven Tan
-                { c: "#713417", r: 2 },       // Terracotta Dye
-                { c: "#502716", r: 2 },       // Russet Brown
-                { c: "#311a12", r: 3 }        // Dark Woven Brown
-            ],
-            kente: [  // Asante / Ewe Woven Colors
-                { c: "#272018", r: 3 },       // Dark Woven Bark
-                { c: "#a29a8b", r: 2 },       // Light Woven Thread
-                { c: "#8b4513", r: 2, v: 1 }, // Vibrant Rust/Ochre Accent
-                { c: "#683c1c", r: 2 },       // Warm Earth Red
-                { c: "#4b351b", r: 2 }        // Deep Brown
-            ]
-        };
-
-        var PEN_MAP = {
-            "#008080": "153 Cobalt Turquoise", "#010650": "247 Indanthrene Blue", "#06FFA5": "174 Chromium Green Opaque",
-            "#0F1316": "199 Black", "#101820": "161 Phthalo Green", "#101840": "247 Indanthrene Blue", "#111416": "199 Black",
-            "#121212": "199 Black", "#121417": "199 Black", "#171718": "161 Phthalo Green", "#1B998B": "153 Cobalt Turquoise",
-            "#1E2126": "175 Dark Sepia", "#1F2529": "175 Dark Sepia", "#201810": "175 Dark Sepia", "#201F25": "175 Dark Sepia",
-            "#203828": "161 Phthalo Green", "#20B2AA": "153 Cobalt Turquoise", "#264653": "Micron Hunter Green",
-            "#2A1C17": "175 Dark Sepia", "#2A1F1A": "175 Dark Sepia", "#2A2A2A": "175 Dark Sepia", "#2A2D34": "175 Dark Sepia",
-            "#2A9D8F": "174 Chromium Green Opaque", "#2D3047": "175 Dark Sepia", "#2E2E2E": "175 Dark Sepia",
-            "#2E363B": "175 Dark Sepia", "#2F363D": "175 Dark Sepia", "#2F4F4F": "Micron Hunter Green", "#303028": "175 Dark Sepia",
-            "#306038": "Micron Hunter Green", "#353B41": "175 Dark Sepia", "#387050": "Micron Hunter Green",
-            "#3A2F2C": "175 Dark Sepia", "#3A3A3C": "175 Dark Sepia", "#3A5F3B": "Micron Hunter Green", "#3E2F23": "175 Dark Sepia",
-            "#3E3E3E": "177 Walnut Brown", "#3F5C57": "Micron Hunter Green", "#45B698": "174 Chromium Green Opaque",
-            "#480810": "157 Dark Indigo", "#49545B": "Micron Hunter Green", "#4A2F23": "175 Dark Sepia",
-            "#4A7C6B": "235 Cold Grey VI", "#4B555F": "235 Cold Grey VI", "#4B6C70": "235 Cold Grey VI",
-            "#4C516D": "235 Cold Grey VI", "#4ECDC4": "146 Sky Blue", "#4FC3F7": "146 Sky Blue", "#505878": "235 Cold Grey VI",
-            "#5386A6": "233 Cold Grey IV", "#53B36A": "174 Chromium Green Opaque", "#556270": "235 Cold Grey VI",
-            "#584020": "177 Walnut Brown", "#585048": "177 Walnut Brown", "#5B85AA": "233 Cold Grey IV",
-            "#5C3A2E": "177 Walnut Brown", "#5C8D62": "170 May Green", "#603F3A": "177 Walnut Brown",
-            "#60C890": "174 Chromium Green Opaque", "#681818": "Micron Burgundy", "#686858": "235 Cold Grey VI",
-            "#6A0572": "Micron Purple", "#6A7177": "235 Cold Grey VI", "#6A8D73": "233 Cold Grey IV", "#6B8E7A": "233 Cold Grey IV",
-            "#6C4E3C": "177 Walnut Brown", "#6C584C": "235 Cold Grey VI", "#6C8A7A": "233 Cold Grey IV",
-            "#6D7880": "233 Cold Grey IV", "#6E6E71": "235 Cold Grey VI", "#6E7882": "233 Cold Grey IV",
-            "#6E7C8F": "233 Cold Grey IV", "#707088": "233 Cold Grey IV", "#708238": "167 Permanent Green Olive",
-            "#7A4A33": "177 Walnut Brown", "#7DA5B3": "232 Cold Grey III", "#7E4A5E": "235 Cold Grey VI",
-            "#7E5A9B": "233 Cold Grey IV", "#848582": "233 Cold Grey IV", "#86AB2E": "268 Green Gold",
-            "#87CEEB": "146 Sky Blue", "#883028": "Micron Brown", "#886830": "180 Raw Umber", "#8890A0": "233 Cold Grey IV",
-            "#8A3324": "Micron Brown", "#8B5A47": "180 Raw Umber", "#8C7A66": "180 Raw Umber", "#908878": "233 Cold Grey IV",
-            "#90D8B0": "171 Light Green", "#98A1A8": "232 Cold Grey III", "#98D8C8": "146 Sky Blue", "#9AA2AA": "232 Cold Grey III",
-            "#A0A088": "232 Cold Grey III", "#A3C4BC": "232 Cold Grey III", "#A46F38": "180 Raw Umber",
-            "#A57C58": "180 Raw Umber", "#A58F6F": "180 Raw Umber", "#A65D7B": "180 Raw Umber", "#A7ADB2": "232 Cold Grey III",
-            "#A7C4A0": "232 Cold Grey III", "#A7C6ED": "146 Sky Blue", "#A8613F": "180 Raw Umber", "#A86B3D": "180 Raw Umber",
-            "#B02040": "134 Crimson", "#B07060": "180 Raw Umber", "#B3595C": "192 Indian Red", "#B84848": "192 Indian Red",
-            "#B87A65": "192 Indian Red", "#B8B8B9": "232 Cold Grey III", "#B8E8C8": "272 Warm Grey III",
-            "#BB8FCE": "125 Middle Purple Pink", "#C0B890": "232 Cold Grey III", "#C1D37F": "171 Light Green",
-            "#C36B4C": "192 Indian Red", "#C58B5B": "186 Terracotta", "#C6543F": "192 Indian Red", "#C6CCD1": "272 Warm Grey III",
-            "#C6E2FF": "154 Light Cobalt Turquoise", "#C7F464": "171 Light Green", "#C82828": "121 Pale Geranium Lake",
-            "#C84C7A": "192 Indian Red", "#C8A060": "268 Green Gold", "#C8B779": "232 Cold Grey III",
-            "#C8E0A0": "272 Warm Grey III", "#CE3E3E": "121 Pale Geranium Lake", "#CF7B4A": "186 Terracotta",
-            "#D07870": "186 Terracotta", "#D3B99A": "232 Cold Grey III", "#D4A58F": "232 Cold Grey III",
-            "#D4D8DC": "272 Warm Grey III", "#D6F0BA": "272 Warm Grey III", "#D86C82": "186 Terracotta",
-            "#D89890": "232 Cold Grey III", "#D8DCDF": "272 Warm Grey III", "#D94F3D": "192 Indian Red",
-            "#D96B5F": "186 Terracotta", "#D9B382": "232 Cold Grey III", "#D9E4DD": "272 Warm Grey III",
-            "#DCC48E": "232 Cold Grey III", "#E0F5E0": "104 Light Yellow Glaze", "#E56459": "186 Terracotta",
-            "#E63946": "121 Pale Geranium Lake", "#E69FAF": "Micron Rose", "#E7F5BE": "104 Light Yellow Glaze",
-            "#E89A5C": "186 Terracotta", "#E8C4B0": "Micron Rose", "#E8E2CF": "272 Warm Grey III", "#E9967A": "186 Terracotta",
-            "#E9F1E9": "103 Ivory", "#EAEDEF": "103 Ivory", "#ED6BA7": "127 Pink Carmine", "#F05858": "186 Terracotta",
-            "#F05A28": "121 Pale Geranium Lake", "#F0B898": "Micron Rose", "#F0CA00": "109 Dark Chrome Yellow",
-            "#F0E880": "104 Light Yellow Glaze", "#F0EAD6": "104 Light Yellow Glaze", "#F1FAEE": "103 Ivory",
-            "#F27649": "186 Terracotta", "#F28F3B": "186 Terracotta", "#F2C14E": "268 Green Gold", "#F2C28A": "Micron Rose",
-            "#F2E394": "Micron Rose", "#F2E8C6": "104 Light Yellow Glaze", "#F2EFEA": "103 Ivory", "#F38E82": "186 Terracotta",
-            "#F3E9D2": "104 Light Yellow Glaze", "#F3F5F6": "103 Ivory", "#F4836A": "186 Terracotta", "#F4A261": "186 Terracotta",
-            "#F4E1D2": "104 Light Yellow Glaze", "#F5E6D3": "104 Light Yellow Glaze", "#F6F3E8": "103 Ivory",
-            "#F7DC6F": "268 Green Gold", "#F83038": "121 Pale Geranium Lake", "#F8BBD0": "Micron Rose", "#F8E0A0": "Micron Rose",
-            "#F8E3C4": "104 Light Yellow Glaze", "#F8E8D0": "104 Light Yellow Glaze", "#F8F4E3": "104 Light Yellow Glaze",
-            "#F8F4ED": "103 Ivory", "#F9B1C8": "Micron Rose", "#F9BCB8": "Micron Rose", "#F9C6C9": "Micron Rose",
-            "#F9D949": "107 Cadmium Yellow", "#FA9A86": "186 Terracotta", "#FAF8F3": "103 Ivory", "#FEDBDD": "Micron Rose",
-            "#FF69B4": "127 Pink Carmine", "#FF6B57": "186 Terracotta", "#FF6B6B": "186 Terracotta", "#FF7F51": "186 Terracotta",
-            "#FF9F1C": "113 Orange Glaze", "#FFB400": "109 Dark Chrome Yellow", "#FFB692": "Micron Rose",
-            "#FFB857": "186 Terracotta", "#FFD166": "268 Green Gold", "#FFD6A8": "Micron Rose",
-            "#FFE4B5": "104 Light Yellow Glaze", "#FFE5D4": "104 Light Yellow Glaze", "#FFE6A7": "104 Light Yellow Glaze",
-            "#FFE7E0": "104 Light Yellow Glaze", "#FFF0B6": "104 Light Yellow Glaze", "#FFF0F5": "103 Ivory",
-            "#FFFCF2": "103 Ivory"
-        };
 
         // ── Traits ────────────────────────────────────────────────────────────────────
         var TRAITS = {
@@ -944,12 +457,6 @@
             engine: 'mastor',
             canvas: 'black'
         };
-        var SYMMETRY_OPTS = ['random', 'none', 'vertical', 'horizontal', 'both', 'diagonal', 'rotational', 'quad', 'kaleidoscope', 'glide', 'radial'];
-
-        var SPACE_OPTS = ['euclidean', 'noneuclidean'];
-        var CHROME_OPTS = ['random', 'kokkos', 'murex', 'tukmo', 'rutsya', 'zahav', 'karkom', 'viride', 'gan', 'tarshish', 'tekhelet', 'yam', 'porphyra', 'shoshan', 'pepel', 'panar', 'katan', 'kaspi', 'barzel', 'wari', 'navajo', 'kinjiki', 'shibui', 'seigaiha', 'bogolan', 'adire', 'kuba', 'kente'];
-        var ENGINE_OPTS = ['random', 'adama', 'adire', 'argyle', 'bogolan', 'chipaz', 'interlace', 'kente', 'kepe', 'kishtima', 'kolya', 'kshtir', 'kuba', 'kudo', 'mastor', 'narmuny', 'navajo', 'pakshats', 'panks', 'pulay', 'seigaiha', 'sermat', 'sermat-kudo', 'tol', 'ved', 'virma', 'wari'];
-        var CANVAS_OPTS = ['black', 'tan', 'olive', 'white'];
 
         var currentLayout = 'panel';
 
@@ -996,33 +503,6 @@
             renderAll();
         }
 
-        var CHROME_DESCS = {
-            'random': 'Random', 'adama': 'Earth / Southwest', 'afarsek': 'Peach', 'alon': 'Oak',
-            'argaman': 'Crimson', 'bahir': 'Luminous', 'barzel': 'Iron', 'erev': 'Evening',
-            'gan': 'Garden / Green', 'gehelet': 'Ember / Umber', 'karkom': 'Saffron / Amber',
-            'kaspi': 'Silver / Slate', 'katan': 'Linen / Cream', 'kelme': 'Ink & Parchment',
-            'kokkos': 'Scarlet', 'madder': 'Madder Root', 'murex': 'Midnight', 'nana': 'Mint',
-            'navajo': 'Southwest Rug', 'neshef': 'Twilight', 'panar': 'Hemp / Parchment',
-            'pepel': 'Ash / White Mono', 'porphyra': 'Purple Shell / Coral', 'ruach': 'Wind / Breeze',
-            'rutsya': 'Rust', 'shahar': 'Dawn / Salmon', 'shemen': 'Olive Oil', 'shoshan': 'Lily / Rose',
-            'simcha': 'Joy / Festival', 'tarshish': 'Aqua / Beryl', 'tekhelet': 'Azure Blue',
-            'tsemer': 'Soft Wool', 'tukmo': 'Brick / Earthen', 'viride': 'Green Earth / Forest',
-            'wari': 'Andean Patchwork', 'yam': 'Sea / Navy', 'zahav': 'Gold / Luxe',
-            'kinjiki': 'Imperial Reds & Purples', 'shibui': 'Astringent / Earthy', 'seigaiha': 'The Sea & Waves',
-            'bogolan': 'Mali Mudcloth', 'adire': 'Yoruba Indigo', 'kuba': 'Ngongo Raffia', 'kente': 'Asante Woven'
-        };
-
-        var ENGINE_DESCS = {
-            'random': 'Random', 'adama': 'Earth / Southwest', 'adire': 'Indigo Resist Dye',
-            'argyle': 'Lace & Lattice', 'bogolan': 'Mudcloth Strips', 'chipaz': 'Sunburst',
-            'interlace': 'Islamic Strapwork', 'kente': 'Staggered Weave', 'kepe': 'Crown Peaks',
-            'kishtima': 'Tessellation', 'kolya': 'Corner Meander', 'kshtir': 'Rotational Pinwheel',
-            'kuba': 'Raffia Patches', 'kudo': 'Brick Masonry', 'mastor': 'Diamonds & Diagonals',
-            'narmuny': 'Decorative Strips', 'navajo': 'Stepped Bands', 'pakshats': 'Interlocking Meander',
-            'panks': 'Floral Rosettes', 'pulay': 'Varied Grid Bands', 'seigaiha': 'Japanese Waves',
-            'sermat': 'Tiled Crosses', 'sermat-kudo': 'Blended Patterns', 'tol': 'Chevron Flames',
-            'ved': 'Interlocking S-Curves', 'virma': 'Stepped Forest Frame', 'wari': 'Patchwork Checks'
-        };
 
         function populateSelect(id, opts, val) {
             var sel = document.getElementById(id);
@@ -1079,20 +559,20 @@
                     }
 
                     var id = panels[pi];
-                    // Render one panel
-                    try {
-                        var t1 = performance.now();
-                        var r = renderLarkspur('s' + id, id, runTraits);
+                    // Render one panel asynchronously
+                    var t1 = performance.now();
+                    renderLarkspur('s' + id, id, runTraits).then(function(r) {
                         var dt = (performance.now() - t1).toFixed(0);
                         console.log('  v' + id + ': ' + r.flowers + ' flowers, ' + r.attempts + ' att, ' + dt + 'ms');
                         infos.push('v' + id + ': ' + r.flowers + ' flowers');
-                    } catch (e) {
+                        pi++;
+                        // Yield to main thread
+                        setTimeout(renderNext, 20);
+                    }).catch(function(e) {
                         console.error('Error rendering panel ' + id, e);
-                    }
-
-                    pi++;
-                    // Yield to main thread
-                    setTimeout(renderNext, 20);
+                        pi++;
+                        setTimeout(renderNext, 20);
+                    });
                 }
 
                 // Resolve 'random' traits to concrete values for this render pass
@@ -1126,1409 +606,79 @@
             }, 10);
         }
 
-        // ── PRNG (Park-Miller LCG) ────────────────────────────────────────────────────
-        function makePRNG(s) {
-            var P = 2147483647;
-            s = ((s + 1590398727) % P);
-            if (s <= 0) s += P - 1;
-            var t = s;
-            function x() { t = (t * 16807) % P; return t; }
-            x(); // warm up
-            return {
-                rfl: function (a, b) { return (x() - 1) / (P - 1) * (b - a) + a; },
-                rin: function (a, b) { return ((x() - 1) % (b - a + 1)) + a; }
-            };
-        }
-
-        // ── MOTIF PRIMITIVES ─────────────────────────────────────────────────────────
-        // Each returns a weight contribution for cell (x,y) on grid (w,h).
-        // Inspired by Mordvinian embroidery stitch patterns.
-
-        // 1. Diagonal band: weight when (x+y) mod period < thickness → creates / lines
-        function motifDiagonal(x, y, period, thickness) {
-            return ((x + y) % period) < thickness ? 1 : 0;
-        }
-
-        // 2. Anti-diagonal band: weight when (x-y) mod period < thickness → creates \ lines
-        function motifAntiDiag(x, y, period, thickness) {
-            return (((x - y) % period + period) % period) < thickness ? 1 : 0;
-        }
-
-        // 3. Chevron: diagonal + anti-diagonal with fold at midpoint
-        function motifChevron(x, y, w, period, amplitude) {
-            var fold = Math.abs(x - w / 2);
-            return ((fold + y) % period) < amplitude ? 1 : 0;
-        }
-
-        // 4. Diamond: Manhattan distance from center < radius
-        function motifDiamond(x, y, cx, cy, radius, ringWidth) {
-            var dist = Math.abs(x - cx) + Math.abs(y - cy);
-            if (ringWidth > 0) {
-                return (dist % radius) < ringWidth ? 1 : 0;
-            }
-            return dist < radius ? 1 : 0;
-        }
-
-        // 5. Stepped edge: creates castellated/meander border patterns
-        function motifStepped(x, y, step, thickness, axis) {
-            var offset = (Math.floor(y / step) % 2) * Math.floor(step / 2);
-            if (axis === 1) offset = (Math.floor(x / step) % 2) * Math.floor(step / 2);
-            var coord = axis === 0 ? x : y;
-            return ((coord + offset) % step) < thickness ? 1 : 0;
-        }
-
-        // 6. S-curve / scroll: sinusoidal offset creates interlocking S shapes
-        function motifScroll(x, y, w, amplitude, freq) {
-            var center = w / 2;
-            var wave = center + amplitude * Math.sin(y * freq * Math.PI / 40);
-            var dist = Math.abs(x - wave);
-            return dist < amplitude * 0.4 ? 1 : 0;
-        }
-
-        // 7. Cross / rosette: radial cross from center point
-        function motifCross(x, y, cx, cy, armW, armL) {
-            var dx = Math.abs(x - cx), dy = Math.abs(y - cy);
-            var onH = dx < armL && dy < armW;
-            var onV = dy < armL && dx < armW;
-            return (onH || onV) ? 1 : 0;
-        }
-
-        // 8. Duckfoot: splayed triplet from a point (Mordvin protective mark)
-        function motifDuckfoot(x, y, cx, cy, spread, len) {
-            var dx = x - cx, dy = y - cy;
-            if (dy < 0 || dy > len) return 0;
-            var t = dy / len;
-            // Three splayed toes
-            var toe1 = Math.abs(dx - t * spread) < 1.5;
-            var toe2 = Math.abs(dx) < 1.5;
-            var toe3 = Math.abs(dx + t * spread) < 1.5;
-            return (toe1 || toe2 || toe3) ? 1 : 0;
-        }
-
-        // 9. Brick: offset rectangular grid like masonry
-        function motifBrick(x, y, brickW, brickH) {
-            var row = Math.floor(y / brickH);
-            var offset = (row % 2) * Math.floor(brickW / 2);
-            var inMortarX = ((x + offset) % brickW) < 1;
-            var inMortarY = (y % brickH) < 1;
-            return (inMortarX || inMortarY) ? 0 : 1;
-        }
-
-        // 10. Lattice: intersecting diagonals creating X-crossing grid
-        function motifLattice(x, y, period, thickness) {
-            var d1 = ((x + y) % period) < thickness;
-            var d2 = (((x - y) % period + period) % period) < thickness;
-            return (d1 || d2) ? 1 : 0;
-        }
-
-        // 11. Polyline: horizontal zigzag stroke
-        function motifPolyline(x, y, period, amplitude, thickness) {
-            var phase = x % period;
-            var peak = (phase < period / 2) ? (phase * 2 * amplitude / period) : (amplitude * 2 - phase * 2 * amplitude / period);
-            return Math.abs(y % (amplitude * 2) - peak) < thickness ? 1 : 0;
-        }
-
-        // 12. Rosette: radial petal/flower form — concentric lobes around center (catalog p103, p107)
-        function motifRosette(x, y, cx, cy, radius, petals, thickness) {
-            var dx = x - cx, dy = y - cy;
-            var dist = Math.sqrt(dx * dx + dy * dy);
-            if (dist > radius * 1.3) return 0;
-            var angle = Math.atan2(dy, dx);
-            var petalPhase = Math.cos(angle * petals);
-            var petalR = radius * (0.5 + 0.5 * petalPhase);
-            return Math.abs(dist - petalR) < thickness ? 1 : 0;
-        }
-
-        // 13. Meander: Greek key / fret — stepped square spiral (catalog p132, p135)
-        function motifMeander(x, y, period, width, depth) {
-            var px = ((x % period) + period) % period;
-            var py = ((y % period) + period) % period;
-            var half = Math.floor(period / 2);
-            // Outer frame
-            if (px < width || py < width) return 1;
-            if (px >= period - width || py >= period - width) return 1;
-            // Inner spiral arm
-            if (px >= half - width && px < half + width && py >= width && py < half + width) return 1;
-            if (py >= half - width && py < half + width && px >= half && px < period - width) return 1;
-            // Depth modulation for nested feel
-            if (depth > 1) {
-                var qp = Math.floor(period / 3);
-                var qx = ((x % qp) + qp) % qp;
-                var qy = ((y % qp) + qp) % qp;
-                if (qx < width || qy < width) return 0.5;
-            }
-            return 0;
-        }
-
-        // 14. HexLattice: hexagonal honeycomb tiling for field tessellations (catalog p172)
-        function motifHexLattice(x, y, cellSize, wallThick) {
-            var row = Math.floor(y / (cellSize * 0.866));
-            var offset = (row % 2) * (cellSize / 2);
-            var cx = ((x + offset) % cellSize) - cellSize / 2;
-            var cy = (y % (cellSize * 0.866)) - cellSize * 0.433;
-            // Hex distance approximation
-            var hx = Math.abs(cx);
-            var hy = Math.abs(cy);
-            var hexDist = Math.max(hx, (hx / 2 + hy * 0.866));
-            var edgeDist = Math.abs(hexDist - cellSize * 0.4);
-            return edgeDist < wallThick ? 1 : 0;
-        }
-
-        // 15. Tie-Dye Ring: irregular fuzzy concentric rings (Wari shibori)
-        function motifTieDyeRing(x, y, cx, cy, radius, thickness, fuzz) {
-            var dx = x - cx, dy = y - cy;
-            var dist = Math.sqrt(dx * dx + dy * dy);
-            // add some jaggedness based on angle
-            var angle = Math.atan2(dy, dx);
-            dist += Math.cos(angle * fuzz) * (radius * 0.15);
-            return Math.abs(dist - radius) < thickness ? 1 : 0;
-        }
-
-        // 16. Stepped Diamond: Navajo-style stepped center motif
-        function motifSteppedDiamond(x, y, cx, cy, radius, stepSize) {
-            var dx = Math.abs(x - cx);
-            var dy = Math.abs(y - cy);
-            // Quantize the coordinates to create steps
-            var sx = Math.floor(dx / stepSize) * stepSize;
-            var sy = Math.floor(dy / stepSize) * stepSize;
-            return (sx + sy) <= radius ? 1 : 0;
-        }
-
-        // 17. Serrated Band: Zig-zag horizontal division (Navajo)
-        function motifSerratedBand(x, y, cy, amplitude, period) {
-            var phase = x % period;
-            var peak = (phase < period / 2) ? (phase * 2 * amplitude / period) : (amplitude * 2 - phase * 2 * amplitude / period);
-            var expectedY = cy - amplitude + peak;
-            return Math.abs(y - expectedY) <= 1 ? 1 : 0;
-        }
-
-        // 18. Multi-Lattice: Thick, multi-line argyle lattice (French lace)
-        function motifMultiLattice(x, y, period, thickness, lines, spacing) {
-            var wt = 0;
-            var d1 = (x + y) % period;
-            var d2 = (((x - y) % period) + period) % period;
-
-            for (var i = 0; i < lines; i++) {
-                var offset = (i - Math.floor(lines / 2)) * spacing;
-                if (Math.abs(d1 - (thickness / 2) - offset) < thickness / 2) wt = 1;
-                if (Math.abs(d2 - (thickness / 2) - offset) < thickness / 2) wt = 1;
-                // wrapping logic for offsets
-                if (Math.abs(d1 - period - (thickness / 2) - offset) < thickness / 2) wt = 1;
-                if (Math.abs(d2 - period - (thickness / 2) - offset) < thickness / 2) wt = 1;
-            }
-            return wt;
-        }
-
-        // 19. Scallop Edge: vertical lacy border (French lace)
-        function motifScallopEdge(x, y, cx, radius, period) {
-            var dy = (y % period) - period / 2;
-            var dx = x - cx;
-            var dist = Math.sqrt(dx * dx + dy * dy);
-            return dx > 0 && dist < radius ? 1 : 0;
-        }
-
-        // 20. Star Grid: Islamic 8-point star and cross interlace
-        function motifStarGrid(x, y, period, ringR, thickness) {
-            var cx = ((x + period / 2) % period) - period / 2;
-            var cy = ((y + period / 2) % period) - period / 2;
-
-            // Cross
-            var onCross = (Math.abs(cx) < thickness && Math.abs(cy) < ringR) ||
-                (Math.abs(cy) < thickness && Math.abs(cx) < ringR);
-            // X (diagonals)
-            var onX = (Math.abs(cx - cy) < thickness && Math.abs(cx) < ringR * 0.7) ||
-                (Math.abs(cx + cy) < thickness && Math.abs(cx) < ringR * 0.7);
-
-            // Diamond frame
-            var dDia = Math.abs(cx) + Math.abs(cy);
-            var onDia = Math.abs(dDia - ringR) < thickness;
-
-            // Square frame
-            var onSq = Math.abs(Math.max(Math.abs(cx), Math.abs(cy)) - ringR * 0.8) < thickness;
-
-            return (onCross || onX || onDia || onSq) ? 1 : 0;
-        }
-
-        // 21. Seigaiha: Japanese continuous overlapping sine waves (vectors)
-        function motifSeigaiha(x, y, period, amp, rowSpacing, stripeSpacing, thickness) {
-            var approxRow = Math.floor(y / rowSpacing);
-            var foundDy = -1;
-
-            // Search rows from bottom-up (higher y drawn on top of lower y)
-            for (var ro = 2; ro >= -2; ro--) {
-                var r = approxRow + ro;
-                var phaseOff = (r % 2 === 0) ? 0 : (period / 2); // Stagger alternate rows
-
-                // Base Y for the wave crest (crest points up, so lower Y)
-                var waveY = r * rowSpacing - amp * Math.cos((x + phaseOff) / period * Math.PI * 2);
-
-                if (y >= waveY) {
-                    // Inside this wave body
-                    foundDy = y - waveY;
-                    break;
-                }
-            }
-
-            if (foundDy < 0) return 0;
-
-            // Draw regular internal stripes parallel to the wave
-            var phaseDist = foundDy % stripeSpacing;
-            return (phaseDist < thickness || (stripeSpacing - phaseDist) < thickness) ? 1 : 0;
-        }
-
-
-        // ── ENGINE WEIGHT CALCULATOR ─────────────────────────────────────────────────
-        // Returns total motif weight for cell (x,y) based on engine type + params.
-        function calcMotifWeight(x, y, w, h, engine, params) {
-            var wt = 0;
-            switch (engine) {
-                case 'adire':
-                    // Yoruba indigo resist dye: organic, tied circles, stenciled bleeding grids
-                    var adireType = (Math.floor(x / params.cellSize) + Math.floor(y / params.cellSize)) % 3;
-
-                    if (adireType === 0) {
-                        // organic tie-dye sunburst circles
-                        var cx = Math.floor(x / params.cellSize) * params.cellSize + params.cellSize / 2;
-                        var cy = Math.floor(y / params.cellSize) * params.cellSize + params.cellSize / 2;
-                        wt += motifTieDyeRing(x, y, cx, cy, params.ringR, params.ringT, params.fuzz) * params.ringWt;
-                    } else if (adireType === 1) {
-                        // freehand cascading waves/whorls (using a warped sine)
-                        var wave = Math.sin((x + Math.sin(y / 20) * 15) / params.waveP);
-                        if (Math.abs(wave) < params.waveT) wt += params.waveWt;
-                    } else {
-                        // sloppy stenciled grid (imperfect)
-                        wt += motifLattice(x + Math.sin(y / 10) * 5, y + Math.cos(x / 10) * 5, params.lP, params.lT) * params.lWt;
-                    }
-                    // intense border bleed
-                    if ((x % params.cellSize < params.bleed) || (y % params.cellSize < params.bleed)) {
-                        wt += params.bleedWt * (Math.random() * 0.5 + 0.5);
-                    }
-                    break;
-
-                case 'seigaiha':
-                    // Japanese Waves: Overlapping continuous sine vectors
-                    wt += motifSeigaiha(x, y, params.period, params.amp, params.rowSpacing, params.stripeSpacing, params.thickness) * params.waveWt;
-                    break;
-
-                case 'kente':
-                    // Asante strip-weave: staggered vertical narrow bands with dense checkerboards/blocks
-                    var bandIdx = Math.floor(x / params.stripW);
-                    var localX = x % params.stripW;
-
-                    // stagger the blocks vertically based on the band
-                    var staggeredY = y + (bandIdx % 2 === 0 ? 0 : params.blockH / 2);
-                    var blockIdx = Math.floor(staggeredY / params.blockH);
-                    var localY = staggeredY % params.blockH;
-
-                    var motifType = (bandIdx * 7 + blockIdx * 11) % 4; // pseudo-random deterministic choice per block
-
-                    // Heavy visible stitched seam between vertical weaves
-                    if (localX < params.seamW || localX > params.stripW - params.seamW) {
-                        wt += params.seamWt;
-                    } else {
-                        if (motifType === 0) {
-                            // tightly packed checkered
-                            wt += motifLattice(localX, localY, params.cP, params.cT) * params.cWt;
-                        } else if (motifType === 1) {
-                            // dense parallel weft lines
-                            if (localY % params.wP < params.wT) wt += params.wWt;
-                        } else if (motifType === 2) {
-                            // stepped block steps
-                            wt += motifStepped(localX, localY, params.sP, params.sT, 0) * params.sWt;
-                        } else {
-                            // solid block
-                            if (Math.abs(localX - params.stripW / 2) < params.stripW * 0.4 && Math.abs(localY - params.blockH / 2) < params.blockH * 0.4) {
-                                wt += params.bWt;
-                            }
-                        }
-                    }
-                    break;
-
-                case 'bogolan':
-                    // Mudcloth - distinct horizontal or vertical strips containing geometric repetitions
-                    var isVert = params.orientation === 1;
-                    var primaryCoord = isVert ? x : y;
-                    var secondaryCoord = isVert ? y : x;
-
-                    var stripIdx = Math.floor(primaryCoord / params.stripW);
-                    var stripPhase = primaryCoord % params.stripW;
-
-                    var motifType = stripIdx % params.motifs.length;
-                    var mo = params.motifs[motifType];
-
-                    if (stripPhase < params.borderT || stripPhase > params.stripW - params.borderT) {
-                        wt += params.borderWt;
-                    } else {
-                        if (mo.type === 0) {
-                            wt += motifChevron(secondaryCoord, stripPhase, params.stripW, mo.p1, mo.p2) * mo.wt;
-                        } else if (mo.type === 1) {
-                            var dcx = (secondaryCoord % mo.p1) - mo.p1 / 2;
-                            var dcy = stripPhase - params.stripW / 2;
-                            if (Math.abs(dcx) + Math.abs(dcy) < mo.p2) wt += mo.wt;
-                        } else if (mo.type === 2) {
-                            var ccx = (secondaryCoord % mo.p1) - mo.p1 / 2;
-                            var ccy = stripPhase - params.stripW / 2;
-                            if (Math.abs(ccx) < mo.p2 || Math.abs(ccy) < mo.p2) wt += mo.wt;
-                        } else {
-                            if ((secondaryCoord % mo.p1) < mo.p2) wt += mo.wt;
-                        }
-                    }
-                    break;
-
-                case 'kuba':
-                    // Raffia weave - dense, irregular orthogonal patches
-                    var pcx = Math.floor(x / params.patchSize);
-                    var pcy = Math.floor(y / params.patchSize);
-                    var pType = (pcx * 13 + pcy * 29) % 5;
-                    var ix = x % params.patchSize;
-                    var iy = y % params.patchSize;
-
-                    if (pType === 0) wt += motifMeander(x, y, params.mP, params.mT, 1) * params.mWt;
-                    else if (pType === 1) wt += motifMeander(y, x, params.mP, params.mT, 1) * params.mWt;
-                    else if (pType === 2) wt += motifStepped(x, y, params.sP, params.sT, 0) * params.sWt;
-                    else if (pType === 3) wt += motifStepped(x, y, params.sP, params.sT, 1) * params.sWt;
-                    else wt += motifLattice(x, y, params.mP, params.mT) * params.mWt * 0.7;
-
-                    if (ix < params.bT || iy < params.bT) wt += params.bWt;
-                    break;
-
-                case 'wari':
-                    // Patchwork checks + tie-dye rings
-                    var px = Math.floor(x / params.patchW);
-                    var py = Math.floor(y / params.patchH);
-                    var patchType = (px * 3 + py * 7) % 3; // pseudo-random deterministic
-
-                    if (patchType === 0) {
-                        // Tie-dye center ring
-                        var cx = (px + 0.5) * params.patchW;
-                        var cy = (py + 0.5) * params.patchH;
-                        wt += motifTieDyeRing(x, y, cx, cy, params.ringR, params.ringT, params.fuzz) * params.ringWt;
-                    } else if (patchType === 1) {
-                        // Diagonal steps
-                        wt += motifStepped(x, y, params.step, params.stepT, 0) * params.stepWt;
-                    } else {
-                        // Checkerboard / grid
-                        wt += motifLattice(x, y, params.gridP, params.gridT) * params.gridWt;
-                    }
-
-                    // Seams
-                    if (x % params.patchW < params.seamT || y % params.patchH < params.seamT) {
-                        wt += params.seamWt;
-                    }
-                    break;
-
-                case 'navajo':
-                    // Horizontal bands with stepped diamonds
-                    var bandY = y % params.bandH;
-                    var bIdx = Math.floor(y / params.bandH) % params.bands.length;
-                    var band = params.bands[bIdx];
-
-                    if (band.type === 0 && Math.abs(bandY - params.bandH / 2) < band.diaR) {
-                        // Central Stepped Diamond
-                        wt += motifSteppedDiamond(x, y, w / 2, y, band.diaR, band.stepSz) * band.wt;
-                        // Flanking side diamonds
-                        wt += motifSteppedDiamond(x, y, w * 0.15, y, band.diaR * 0.5, band.stepSz) * band.wt * 0.8;
-                        wt += motifSteppedDiamond(x, y, w * 0.85, y, band.diaR * 0.5, band.stepSz) * band.wt * 0.8;
-                    } else if (band.type === 1) {
-                        // Serrated line
-                        wt += motifSerratedBand(x, y, y, band.amp, band.period) * band.wt;
-                    } else {
-                        // Chevrons (water)
-                        wt += motifChevron(x, y, w, band.period, band.amp) * band.wt;
-                    }
-
-                    // Demarcation stripes
-                    if (bandY < params.stripeT) wt += params.stripeWt;
-                    break;
-
-                case 'argyle':
-                    // French lace borders and thick multi-lattice center
-                    var isBorder = x < params.borderW || x > (w - params.borderW);
-
-                    if (isBorder) {
-                        // Scalloped edging + inner diamond dots
-                        if (x < params.borderW) {
-                            wt += motifScallopEdge(x, y, params.borderW, params.scallopR, params.scallopP) * params.laceWt;
-                        } else {
-                            wt += motifScallopEdge(w - x, y, params.borderW, params.scallopR, params.scallopP) * params.laceWt;
-                        }
-                    } else {
-                        // Central Argyle field
-                        wt += motifMultiLattice(x, y, params.latP, params.latT, params.latL, params.latSpc) * params.argyleWt;
-                        // Accent dots inside argyle diamonds
-                        var dCx = (x % params.latP) - params.latP / 2;
-                        var dCy = (y % params.latP) - params.latP / 2;
-                        if (Math.abs(dCx) < params.latP / 4 && Math.abs(dCy) < params.latP / 4) {
-                            wt += motifDiamond(x, y, x - dCx, y - dCy, params.dotR, 0) * params.dotWt;
-                        }
-                    }
-                    break;
-
-                case 'interlace':
-                    // Islamic overlapping strapwork stars
-                    wt += motifStarGrid(x, y, params.starP, params.starR, params.starT) * params.starWt;
-                    break;
-
-                case 'seigaiha':
-                    // Japanese concentric overlapping waves
-                    wt += motifSeigaiha(x, y, params.R, params.spacing, params.thickness, params.dotR) * params.waveWt;
-                    break;
-
-                case 'sermat':
-                    // Tiled crosses — family identity marks
-                    for (var ci = 0; ci < params.centers.length; ci++) {
-                        var ctr = params.centers[ci];
-                        wt += motifCross(x, y, ctr[0], ctr[1], params.armW, params.armL) * params.weights[ci % params.weights.length];
-                    }
-                    wt += motifDiamond(x, y, w / 2, h / 2, params.ringR, params.ringW) * params.ringWt;
-                    break;
-
-                case 'tol':
-                    // Fire — chevron flames climbing upward
-                    for (var chi = 0; chi < params.layers.length; chi++) {
-                        var ly = params.layers[chi];
-                        wt += motifChevron(x, y, w, ly[0], ly[1]) * ly[2];
-                    }
-                    break;
-
-                case 'mastor':
-                    // Land — concentric diamond rings + diagonal fill
-                    for (var di = 0; di < params.diamonds.length; di++) {
-                        var dm = params.diamonds[di];
-                        wt += motifDiamond(x, y, dm[0], dm[1], dm[2], dm[3]) * dm[4];
-                    }
-                    wt += motifDiagonal(x, y, params.diagP, params.diagT) * params.diagWt;
-                    wt += motifAntiDiag(x, y, params.adiagP, params.adiagT) * params.adiagWt;
-                    break;
-
-                case 'virma':
-                    // Forest frame — stepped border with interior growth
-                    var bx = Math.min(x, w - 1 - x);
-                    var by = Math.min(y, h - 1 - y);
-                    var borderDist = Math.min(bx, by);
-                    if (borderDist < params.borderW) {
-                        wt += motifStepped(x, y, params.step, params.stepT, 0) * params.stepWt;
-                        wt += motifStepped(x, y, params.step2, params.stepT2, 1) * params.stepWt2;
-                    }
-                    if (borderDist >= params.borderW && borderDist < params.borderW + params.innerW) {
-                        wt += motifChevron(x, y, w, params.innerP, params.innerA) * params.innerWt;
-                    }
-                    if (borderDist >= params.borderW + params.innerW) {
-                        wt += motifDiamond(x, y, w / 2, h / 2, params.centralR, params.centralRing) * params.centralWt;
-                    }
-                    break;
-
-                case 'ved':
-                    // Water — interlocking S-curves
-                    for (var si = 0; si < params.scrolls.length; si++) {
-                        var sc = params.scrolls[si];
-                        wt += motifScroll(x, y, w, sc[0], sc[1]) * sc[2];
-                    }
-                    wt += motifStepped(x, y, params.bandStep, params.bandT, 0) * params.bandWt;
-                    break;
-
-                case 'chipaz':
-                    // Sun god — radial sunburst + concentric rings + lattice glow
-                    for (var chi2 = 0; chi2 < params.rays.length; chi2++) {
-                        var ray = params.rays[chi2];
-                        wt += motifCross(x, y, ray[0], ray[1], ray[2], ray[3]) * ray[4];
-                    }
-                    wt += motifDiamond(x, y, w / 2, h / 2, params.sunR, params.sunRing) * params.sunWt;
-                    wt += motifLattice(x, y, params.glowP, params.glowT) * params.glowWt;
-                    break;
-
-                case 'kudo':
-                    // House — brick masonry + rectangular partitions
-                    wt += motifBrick(x, y, params.brickW, params.brickH) * params.brickWt;
-                    for (var ki = 0; ki < params.rooms.length; ki++) {
-                        var rm = params.rooms[ki];
-                        if (x >= rm[0] && x < rm[0] + rm[2] && y >= rm[1] && y < rm[1] + rm[3]) {
-                            wt += rm[4];
-                        }
-                    }
-                    wt += motifStepped(x, y, params.frameStep, params.frameT, 0) * params.frameWt;
-                    break;
-
-                case 'pulay':
-                    // Waist belt — horizontal bands with different motifs per band
-                    var bandIdx = Math.floor(y / params.bandH) % params.bands.length;
-                    var band = params.bands[bandIdx];
-                    if (band.type === 0) wt += motifDiagonal(x, y, band.p1, band.p2) * band.wt;
-                    else if (band.type === 1) wt += motifChevron(x, y, w, band.p1, band.p2) * band.wt;
-                    else if (band.type === 2) wt += motifPolyline(x, y, band.p1, band.p2, band.p3) * band.wt;
-                    else if (band.type === 3) wt += motifLattice(x, y, band.p1, band.p2) * band.wt;
-                    else wt += motifStepped(x, y, band.p1, band.p2, 0) * band.wt;
-                    // Duckfoot accents between bands
-                    for (var pi2 = 0; pi2 < params.feet.length; pi2++) {
-                        var ft = params.feet[pi2];
-                        wt += motifDuckfoot(x, y, ft[0], ft[1], ft[2], ft[3]) * ft[4];
-                    }
-                    break;
-
-                case 'sermat-kudo':
-                    // Pattern-house — blend 2-3 engines
-                    for (var mi = 0; mi < params.mix.length; mi++) {
-                        var sub = params.mix[mi];
-                        wt += calcMotifWeight(x, y, w, h, sub.engine, sub.params) * sub.weight;
-                    }
-                    break;
-
-                case 'kepe':
-                    // Crown/headdress — sawtooth peaks + stepped fill (catalog p122, p158-162)
-                    var peakY = (y % params.peakH);
-                    var peakX = (x % params.peakW);
-                    var peakSlope = peakY < params.peakH / 2 ? peakX < peakY * params.peakW / params.peakH : peakX < (params.peakH - peakY) * params.peakW / params.peakH;
-                    if (peakSlope) wt += params.peakWt;
-                    wt += motifStepped(x, y, params.fillStep, params.fillT, 0) * params.fillWt;
-                    wt += motifDiamond(x, y, w / 2, h / 2, params.crownR, params.crownRing) * params.crownWt;
-                    break;
-
-                case 'panks':
-                    // Petal/blossom — floral rosettes (catalog p107)
-                    for (var pki = 0; pki < params.flowers.length; pki++) {
-                        var fl = params.flowers[pki];
-                        wt += motifRosette(x, y, fl[0], fl[1], fl[2], fl[3], fl[4]) * fl[5];
-                    }
-                    wt += motifDiamond(x, y, w / 2, h / 2, params.stemR, params.stemRing) * params.stemWt;
-                    break;
-
-                case 'kishtima':
-                    // Pattern/ornament — all-over field tessellation (catalog p168, p172)
-                    if (params.tileType === 0) {
-                        wt += motifLattice(x, y, params.tileP, params.tileT) * params.tileWt;
-                    } else {
-                        wt += motifHexLattice(x, y, params.tileP, params.tileT) * params.tileWt;
-                    }
-                    // Node accents at intersections
-                    var nodeX = ((x + params.nodeOff) % params.nodeP) < params.nodeSz;
-                    var nodeY = ((y + params.nodeOff) % params.nodeP) < params.nodeSz;
-                    if (nodeX && nodeY) wt += params.nodeWt;
-                    wt += motifDiamond(x, y, w / 2, h / 2, params.fieldR, params.fieldRing) * params.fieldWt;
-                    break;
-
-                case 'kshtir':
-                    // Spindle/whorl — rotational pinwheel (catalog p132)
-                    var kdx = x - w / 2, kdy = y - h / 2;
-                    var kAngle = Math.atan2(kdy, kdx);
-                    var kDist = Math.sqrt(kdx * kdx + kdy * kdy);
-                    // Spiral arms
-                    for (var kai = 0; kai < params.armCount; kai++) {
-                        var armAngle = kAngle - kai * Math.PI * 2 / params.armCount;
-                        var spiralPhase = ((armAngle + kDist * params.spiralTwist) % (Math.PI * 2) + Math.PI * 2) % (Math.PI * 2);
-                        if (spiralPhase < params.armWidth && kDist < params.armLen) {
-                            wt += params.armWt;
-                        }
-                    }
-                    wt += motifMeander(x, y, params.bgP, params.bgW, params.bgD) * params.bgWt;
-                    break;
-
-                case 'narmuny':
-                    // Border/edge — decorative horizontal band strips (catalog p152-155)
-                    var zoneY = y % params.zoneH;
-                    var zoneIdx = Math.floor(y / params.zoneH) % params.zones.length;
-                    var zone = params.zones[zoneIdx];
-                    if (zone.motif === 0) wt += motifChevron(x, y, w, zone.p1, zone.p2) * zone.wt;
-                    else if (zone.motif === 1) wt += motifDiagonal(x, y, zone.p1, zone.p2) * zone.wt;
-                    else if (zone.motif === 2) wt += motifLattice(x, y, zone.p1, zone.p2) * zone.wt;
-                    else if (zone.motif === 3) wt += motifDiamond(x, y, w / 2, y, zone.p1, zone.p2) * zone.wt;
-                    else wt += motifStepped(x, y, zone.p1, zone.p2, 0) * zone.wt;
-                    // Separator lines between zones
-                    if (zoneY < params.sepT) wt += params.sepWt;
-                    break;
-
-                case 'pakshats':
-                    // Interlocking — Greek key meander at multiple scales (catalog p115, p135)
-                    for (var psi = 0; psi < params.layers.length; psi++) {
-                        var ml = params.layers[psi];
-                        wt += motifMeander(x, y, ml[0], ml[1], ml[2]) * ml[3];
-                    }
-                    wt += motifStepped(x, y, params.accentStep, params.accentT, 1) * params.accentWt;
-                    break;
-
-                case 'kolya':
-                    // Corner/angle — triangular meander-filled V-shapes (catalog p162)
-                    var cornerDist = Math.min(x, y, w - 1 - x, h - 1 - y);
-                    var diag1 = Math.abs(x - y);
-                    var diag2 = Math.abs(x - (h - 1 - y));
-                    var inCorner = cornerDist < params.cornerSize;
-                    if (inCorner) {
-                        wt += motifMeander(x, y, params.fillP, params.fillW, params.fillD) * params.cornerWt;
-                        wt += motifChevron(x, y, w, params.chevP, params.chevA) * params.chevWt;
-                    }
-                    // Edge frame
-                    if (cornerDist < params.edgeW) wt += params.edgeWt;
-                    break;
-            }
-            return wt;
-        }
-
-        // ── ENGINE PARAMETER GENERATOR ───────────────────────────────────────────────
-        // Uses PRNG to create parameters for each Mordvinian engine.
-        function generatePatternParams(engine, prng, w, h) {
-            var rfl = prng.rfl, rin = prng.rin;
-            var params = {};
-
-            switch (engine) {
-                case 'bogolan':
-                    params.orientation = rin(0, 1);
-                    params.stripW = Math.floor((params.orientation === 1 ? w : h) / rin(3, 9));
-                    var numMotifs = rin(2, 5);
-                    params.motifs = [];
-                    for (var bi = 0; bi < numMotifs; bi++) {
-                        params.motifs.push({
-                            type: rin(0, 3),
-                            p1: rin(20, 60),
-                            p2: rin(4, 15),
-                            wt: rfl(4, 9)
-                        });
-                    }
-                    params.borderT = rin(2, 8);
-                    params.borderWt = rfl(5, 12);
-                    break;
-
-                case 'kuba':
-                    params.patchSize = rin(20, 80);
-                    // Generate specific meander and stepped params to map tightly
-                    params.mP = rin(10, 30);
-                    params.mT = rin(1, 4);
-                    params.mWt = rfl(3, 8);
-                    params.sP = rin(10, 30);
-                    params.sT = rin(2, 6);
-                    params.sWt = rfl(4, 9);
-                    params.bT = rin(2, 6);
-                    params.bWt = rfl(5, 12);
-                    break;
-
-                case 'wari':
-                    params.patchW = Math.floor(w / rin(3, 6)); // 3 to 6 columns
-                    params.patchH = Math.floor(h / rin(4, 8)); // 4 to 8 rows
-                    params.ringR = rin(10, Math.floor(params.patchW * 0.4));
-                    params.ringT = rin(2, 6);
-                    params.fuzz = rin(6, 16); // frequency of tie-dye bleeding
-                    params.ringWt = rfl(4, 9);
-                    params.step = rin(4, 12);
-                    params.stepT = rin(1, 4);
-                    params.stepWt = rfl(2, 5);
-                    params.gridP = rin(6, 14);
-                    params.gridT = rin(1, 3);
-                    params.gridWt = rfl(2, 5);
-                    params.seamT = rin(1, 4);
-                    params.seamWt = rfl(3, 7);
-                    break;
-
-                case 'navajo':
-                    params.bandH = Math.floor(h / rin(3, 7)); // 3 to 7 big bands
-                    var nbands = rin(3, 7);
-                    params.bands = [];
-                    for (var nbi = 0; nbi < nbands; nbi++) {
-                        params.bands.push({
-                            type: rin(0, 2),
-                            diaR: Math.floor(params.bandH * rfl(0.3, 0.45)),
-                            stepSz: rin(2, 6),
-                            amp: rin(4, 15),
-                            period: rin(20, 50),
-                            wt: rfl(4, 9)
-                        });
-                    }
-                    // At least one diamond band
-                    params.bands[Math.floor(nbands / 2)].type = 0;
-
-                    params.stripeT = rin(2, 8);
-                    params.stripeWt = rfl(3, 8);
-                    break;
-
-                case 'argyle':
-                    params.borderW = Math.floor(w * rfl(0.1, 0.25));
-                    params.scallopR = rin(15, 40);
-                    params.scallopP = rin(30, 80);
-                    params.laceWt = rfl(3, 7);
-
-                    params.latP = rin(30, 80); // large lattice
-                    params.latT = rin(2, 5);
-                    params.latL = rin(2, 4); // 2-4 lines
-                    params.latSpc = params.latT * rin(2, 4);
-                    params.argyleWt = rfl(4, 8);
-
-                    params.dotR = rin(3, 8);
-                    params.dotWt = rfl(2, 6);
-                    break;
-
-                case 'interlace':
-                    params.starP = rin(30, 100); // grid period
-                    params.starR = Math.floor(params.starP * rfl(0.35, 0.45)); // radius of shapes
-                    params.starT = rin(2, 8); // thickness of strapwork
-                    params.starWt = rfl(4, 9);
-                    break;
-
-                case 'seigaiha':
-                    params.R = Math.floor(w * rfl(0.08, 0.25));
-                    params.thickness = rin(1, 4);
-                    params.spacing = params.thickness * rin(2, 5);
-                    params.dotR = rfl(0, params.spacing); // sometimes solid center dot
-                    params.waveWt = rfl(3, 8);
-                    break;
-
-                case 'sermat':
-                    var nc = rin(3, 8);
-                    params.centers = [];
-                    params.weights = [];
-                    var spacingX = w / (Math.ceil(Math.sqrt(nc)) + 1);
-                    var spacingY = h / (Math.ceil(nc / Math.ceil(Math.sqrt(nc))) + 1);
-                    for (var i = 0; i < nc; i++) {
-                        var col = i % Math.ceil(Math.sqrt(nc));
-                        var row = Math.floor(i / Math.ceil(Math.sqrt(nc)));
-                        params.centers.push([
-                            Math.floor(spacingX * (col + 1) + rfl(-spacingX * 0.2, spacingX * 0.2)),
-                            Math.floor(spacingY * (row + 1) + rfl(-spacingY * 0.2, spacingY * 0.2))
-                        ]);
-                        params.weights.push(rfl(3, 8));
-                    }
-                    params.armW = rin(2, 5);
-                    params.armL = rin(4, 12);
-                    params.ringR = rin(8, 25);
-                    params.ringW = rin(1, 4);
-                    params.ringWt = rfl(2, 6);
-                    break;
-
-                case 'tol':
-                    var nl = rin(2, 5);
-                    params.layers = [];
-                    for (var j = 0; j < nl; j++) {
-                        params.layers.push([
-                            rin(6, 30),       // period
-                            rin(2, 8),        // amplitude
-                            rfl(2, 8)         // weight
-                        ]);
-                    }
-                    break;
-
-                case 'mastor':
-                    var nd = rin(2, 5);
-                    params.diamonds = [];
-                    for (var k = 0; k < nd; k++) {
-                        params.diamonds.push([
-                            Math.floor(w / 2 + rfl(-w * 0.2, w * 0.2)),
-                            Math.floor(h / 2 + rfl(-h * 0.2, h * 0.2)),
-                            rin(10, 40),
-                            rin(1, 5),
-                            rfl(3, 8)
-                        ]);
-                    }
-                    params.diagP = rin(4, 16);
-                    params.diagT = rin(1, Math.floor(params.diagP / 2));
-                    params.diagWt = rfl(1, 4);
-                    params.adiagP = rin(4, 16);
-                    params.adiagT = rin(1, Math.floor(params.adiagP / 2));
-                    params.adiagWt = rfl(1, 4);
-                    break;
-
-                case 'virma':
-                    params.borderW = rin(5, 15);
-                    params.step = rin(3, 10);
-                    params.stepT = rin(1, Math.max(2, Math.floor(params.step / 2)));
-                    params.stepWt = rfl(3, 8);
-                    params.step2 = rin(3, 10);
-                    params.stepT2 = rin(1, Math.max(2, Math.floor(params.step2 / 2)));
-                    params.stepWt2 = rfl(2, 6);
-                    params.innerW = rin(5, 15);
-                    params.innerP = rin(8, 20);
-                    params.innerA = rin(2, 6);
-                    params.innerWt = rfl(3, 7);
-                    params.centralR = rin(15, 35);
-                    params.centralRing = rin(2, 6);
-                    params.centralWt = rfl(3, 8);
-                    break;
-
-                case 'ved':
-                    var ns = rin(2, 5);
-                    params.scrolls = [];
-                    for (var m = 0; m < ns; m++) {
-                        params.scrolls.push([
-                            rfl(5, 20),
-                            rfl(0.5, 3),
-                            rfl(3, 8)
-                        ]);
-                    }
-                    params.bandStep = rin(4, 12);
-                    params.bandT = rin(1, Math.max(2, Math.floor(params.bandStep / 2)));
-                    params.bandWt = rfl(1, 4);
-                    break;
-
-                case 'chipaz':
-                    // Sun god — radiating crosses + concentric diamond + lattice glow
-                    var nr = rin(4, 8);
-                    params.rays = [];
-                    for (var ri = 0; ri < nr; ri++) {
-                        var angle = ri * Math.PI * 2 / nr;
-                        var dist = rfl(10, 35);
-                        params.rays.push([
-                            Math.floor(w / 2 + Math.cos(angle) * dist),
-                            Math.floor(h / 2 + Math.sin(angle) * dist * (h / w)),
-                            rin(1, 3),   // armW
-                            rin(3, 10),  // armL
-                            rfl(3, 8)    // weight
-                        ]);
-                    }
-                    params.sunR = rin(12, 30);
-                    params.sunRing = rin(2, 5);
-                    params.sunWt = rfl(3, 8);
-                    params.glowP = rin(6, 16);
-                    params.glowT = rin(1, 3);
-                    params.glowWt = rfl(1, 3);
-                    break;
-
-                case 'kudo':
-                    // House — brick masonry + room partitions
-                    params.brickW = rin(4, 12);
-                    params.brickH = rin(3, 8);
-                    params.brickWt = rfl(2, 5);
-                    var nRooms = rin(3, 8);
-                    params.rooms = [];
-                    for (var ri2 = 0; ri2 < nRooms; ri2++) {
-                        params.rooms.push([
-                            rin(0, w - 10),      // x
-                            rin(0, h - 10),      // y
-                            rin(8, 25),           // width
-                            rin(8, 30),           // height
-                            rfl(2, 7)             // weight
-                        ]);
-                    }
-                    params.frameStep = rin(4, 10);
-                    params.frameT = rin(1, Math.max(2, Math.floor(params.frameStep / 2)));
-                    params.frameWt = rfl(1, 4);
-                    break;
-
-                case 'pulay':
-                    // Waist belt — horizontal bands with varied motifs
-                    params.bandH = rin(8, 25);
-                    var nBands = rin(3, 7);
-                    params.bands = [];
-                    for (var bi = 0; bi < nBands; bi++) {
-                        var btype = rin(0, 4);
-                        params.bands.push({
-                            type: btype,
-                            p1: rin(4, 16),
-                            p2: rin(1, 6),
-                            p3: rin(1, 3),
-                            wt: rfl(3, 8)
-                        });
-                    }
-                    // Duckfoot accents
-                    var nFeet = rin(2, 6);
-                    params.feet = [];
-                    for (var fi = 0; fi < nFeet; fi++) {
-                        params.feet.push([
-                            rin(5, w - 5),   // cx
-                            rin(5, h - 10),  // cy
-                            rfl(3, 10),      // spread
-                            rin(4, 12),      // length
-                            rfl(3, 7)        // weight
-                        ]);
-                    }
-                    break;
-
-                case 'sermat-kudo':
-                    // Pattern-house — blend 2-3 engines
-                    var bases = ['wari', 'navajo', 'argyle', 'interlace', 'seigaiha', 'sermat', 'tol', 'mastor', 'virma', 'ved', 'chipaz', 'kudo', 'pulay', 'kepe', 'panks', 'kishtima', 'kshtir', 'narmuny', 'pakshats', 'kolya', 'bogolan', 'kuba', 'adire', 'kente'];
-                    var nmix = rin(2, 3);
-                    params.mix = [];
-                    var used = {};
-                    for (var q = 0; q < nmix; q++) {
-                        var pick;
-                        do { pick = bases[rin(0, bases.length - 1)]; } while (used[pick]);
-                        used[pick] = true;
-                        var subPrng = makePRNG(prng.rin(1, 999999));
-                        params.mix.push({
-                            engine: pick,
-                            params: generatePatternParams(pick, subPrng, w, h),
-                            weight: rfl(0.3, 1.0)
-                        });
-                    }
-                    break;
-
-                case 'kepe':
-                    // Crown/headdress — sawtooth peaks
-                    params.peakH = rin(8, 20);
-                    params.peakW = rin(6, 16);
-                    params.peakWt = rfl(3, 8);
-                    params.fillStep = rin(3, 8);
-                    params.fillT = rin(1, Math.max(2, Math.floor(params.fillStep / 2)));
-                    params.fillWt = rfl(2, 5);
-                    params.crownR = rin(15, 35);
-                    params.crownRing = rin(2, 5);
-                    params.crownWt = rfl(2, 6);
-                    break;
-
-                case 'panks':
-                    // Petal/blossom — floral rosettes
-                    var nFlowers = rin(3, 7);
-                    params.flowers = [];
-                    for (var pfi = 0; pfi < nFlowers; pfi++) {
-                        params.flowers.push([
-                            rin(10, w - 10),     // cx
-                            rin(10, h - 10),     // cy
-                            rin(8, 25),           // radius
-                            rin(3, 8) * 2,        // petals (even number)
-                            rin(1, 4),            // thickness
-                            rfl(3, 8)             // weight
-                        ]);
-                    }
-                    params.stemR = rin(20, 40);
-                    params.stemRing = rin(2, 5);
-                    params.stemWt = rfl(1, 4);
-                    break;
-
-                case 'kishtima':
-                    // Pattern/ornament — all-over field tessellation
-                    params.tileType = rin(0, 1);
-                    params.tileP = rin(8, 24);
-                    params.tileT = rin(1, 3);
-                    params.tileWt = rfl(3, 7);
-                    params.nodeP = rin(6, 18);
-                    params.nodeSz = rin(1, 3);
-                    params.nodeOff = rin(0, params.nodeP);
-                    params.nodeWt = rfl(2, 6);
-                    params.fieldR = rin(20, 45);
-                    params.fieldRing = rin(2, 5);
-                    params.fieldWt = rfl(1, 3);
-                    break;
-
-                case 'kshtir':
-                    // Spindle/whorl — rotational pinwheel
-                    params.armCount = rin(3, 6);
-                    params.armWidth = rfl(0.3, 1.2);
-                    params.armLen = rfl(w * 0.3, w * 0.6);
-                    params.spiralTwist = rfl(0.02, 0.1);
-                    params.armWt = rfl(4, 9);
-                    params.bgP = rin(10, 24);
-                    params.bgW = rin(1, 3);
-                    params.bgD = rin(1, 2);
-                    params.bgWt = rfl(0.5, 2);
-                    break;
-
-                case 'narmuny':
-                    // Border/edge — decorative horizontal band strips
-                    params.zoneH = rin(6, 18);
-                    var nZones = rin(3, 7);
-                    params.zones = [];
-                    for (var nzi = 0; nzi < nZones; nzi++) {
-                        params.zones.push({
-                            motif: rin(0, 4),
-                            p1: rin(4, 16),
-                            p2: rin(1, 6),
-                            wt: rfl(3, 8)
-                        });
-                    }
-                    params.sepT = rin(1, 3);
-                    params.sepWt = rfl(2, 6);
-                    break;
-
-                case 'pakshats':
-                    // Interlocking — Greek key meander layers
-                    var nLayers = rin(2, 4);
-                    params.layers = [];
-                    for (var pli = 0; pli < nLayers; pli++) {
-                        params.layers.push([
-                            rin(10, 30),   // period
-                            rin(1, 3),     // width
-                            rin(1, 2),     // depth
-                            rfl(3, 8)      // weight
-                        ]);
-                    }
-                    params.accentStep = rin(4, 12);
-                    params.accentT = rin(1, Math.max(2, Math.floor(params.accentStep / 2)));
-                    params.accentWt = rfl(1, 3);
-                    break;
-
-                case 'kolya':
-                    // Corner/angle — triangular fills
-                    params.cornerSize = rin(15, Math.floor(Math.min(w, h) * 0.4));
-                    params.fillP = rin(8, 20);
-                    params.fillW = rin(1, 3);
-                    params.fillD = rin(1, 2);
-                    params.cornerWt = rfl(3, 8);
-                    params.chevP = rin(6, 16);
-                    params.chevA = rin(2, 6);
-                    params.chevWt = rfl(2, 5);
-                    params.edgeW = rin(2, 6);
-                    params.edgeWt = rfl(2, 5);
-                    break;
-
-                case 'adire':
-                    params.cellSize = rin(60, 150);
-                    params.ringR = Math.floor(params.cellSize * rfl(0.2, 0.4));
-                    params.ringT = rin(3, 8);
-                    params.fuzz = rin(10, 30);
-                    params.ringWt = rfl(4, 10);
-                    params.waveP = rin(15, 35);
-                    params.waveT = rfl(0.1, 0.3);
-                    params.waveWt = rfl(3, 7);
-                    params.lP = rin(10, 25);
-                    params.lT = rin(2, 6);
-                    params.lWt = rfl(3, 8);
-                    params.bleed = rin(5, 15);
-                    params.bleedWt = rfl(5, 12);
-                    break;
-
-                case 'seigaiha':
-                    params.period = Math.floor(w * rfl(0.15, 0.4));
-                    params.amp = params.period * rfl(0.15, 0.35);
-                    params.rowSpacing = params.amp * rfl(0.7, 1.4);
-                    params.thickness = rin(1, 3);
-                    params.stripeSpacing = params.thickness * rin(3, 7);
-                    params.waveWt = rfl(3, 8);
-                    break;
-
-                case 'kente':
-                    params.stripW = Math.floor(w / rin(6, 15)); // Narrow, numerous strips
-                    params.blockH = Math.floor(params.stripW * rfl(1.0, 2.5)); // Rectangular sequence blocks
-                    params.seamW = rin(2, 6);
-                    params.seamWt = rfl(5, 12);
-                    params.cP = rin(6, 14); // dense checkered
-                    params.cT = rin(3, 7);
-                    params.cWt = rfl(4, 9);
-                    params.wP = rin(4, 10); // dense weft
-                    params.wT = rin(2, 5);
-                    params.wWt = rfl(3, 8);
-                    params.sP = rin(8, 20); // steps
-                    params.sT = rin(2, 6);
-                    params.sWt = rfl(3, 8);
-                    params.bWt = rfl(6, 12); // solid
-                    break;
-            }
-            return params;
-        }
 
-        // ── LARKSPUR ENGINE ──────────────────────────────────────────────────────────────
+        // ── LARKSPUR ENGINE (Worker Version) ──────────────────────────────────────────
         // Grid is ALWAYS 78 wide × 148 tall (portrait).
-        // Banner mode = same grid, SVG scaled to fit width via CSS.
-        // This keeps the lns/pls/rts data valid and the seed search tractable.
         function renderLarkspur(svgId, id, traits) {
-            var svgNS = "http://www.w3.org/2000/svg";
-            var dc = document;
-            var svg = dc.getElementById(svgId);
-            if (!svg) return { flowers: 0 };
-
-            id = Math.min(Math.max(id | 0, 1), 3);
-
-
-
-            // Clear
-            while (svg.firstChild) svg.removeChild(svg.firstChild);
-
-            // Grid is always portrait 78×148
-            var w = 78, h = 148, size = 16, rSize = 14;
-
-            // Palette
-            var cls = PALETTES[traits.chromes] || PALETTES.panar;
-            function rc(t) {
-                for (var i = 0; i < cls.length; i++) {
-                    if (t < cls[i].r) return cls[i];
-                    t -= cls[i].r;
-                }
-            }
-
-
-
-            // Drawing groups
-            var gEl = dc.createElementNS(svgNS, "g");
-
-            // ── Hybrid flower inversion ───────────────────────────────────────────────
-            // Symmetry-aware base: flowers in mirrored zones face opposite directions
-            // 12% deterministic scatter: position-hash deviation for organic variation
-            function shouldFlipFlower(fx, fy) {
-                var cx = w / 2, cy = h / 2;
-                var baseFlip = false;
-                switch (traits.symmetry) {
-                    case 'none':
-                        baseFlip = false;
-                        break;
-                    case 'vertical':
-                        baseFlip = fx >= cx;
-                        break;
-                    case 'horizontal':
-                        baseFlip = fy >= cy;
-                        break;
-                    case 'both':
-                        baseFlip = (fx >= cx) !== (fy >= cy);
-                        break;
-                    case 'diagonal':
-                        baseFlip = (fx / w) > (fy / h);
-                        break;
-                    case 'rotational':
-                        baseFlip = (fx >= cx) !== (fy >= cy);
-                        break;
-                    case 'quad':
-                        baseFlip = (fx >= cx) !== (fy >= cy);
-                        break;
-                    case 'kaleidoscope':
-                        var angle = Math.atan2(fy - cy, fx - cx);
-                        var sector = Math.floor(((angle + Math.PI) / (2 * Math.PI)) * 8);
-                        baseFlip = sector % 2 === 1;
-                        break;
-                    case 'glide':
-                        baseFlip = Math.floor(fy / (h / 8)) % 2 === 1;
-                        break;
-                    case 'radial':
-                        var rdx = fx - cx, rdy = fy - cy;
-                        var dist = Math.sqrt(rdx * rdx + rdy * rdy);
-                        var ring = Math.floor(dist / (Math.max(w, h) / 8));
-                        baseFlip = ring % 2 === 1;
-                        break;
-                }
-                // 12% deterministic scatter — position hash, no PRNG consumption
-                var scatter = ((fx * 7919 + fy * 6271) % 100);
-                if (scatter < 12) baseFlip = !baseFlip;
-                return baseFlip;
-            }
-
-            // Draw a filled larkspur flower — inline path, direct fill, no symbol needed
-            function adr(x, y, col, flip) {
-                var px = (x + 4) * size + 1;
-                var py = (y + 4) * size + 1;
-                var sc = rSize / 20;
-                var path = dc.createElementNS(svgNS, "path");
-                var xform = "translate(" + px + "," + py + ") scale(" + sc + ")";
-                if (flip) xform += " rotate(180,10,10)";
-                path.setAttribute("transform", xform);
-                path.setAttribute("d", FLOWER_PATH);
-                path.setAttribute("fill", col);
-                gEl.appendChild(path);
-            }
-
-
-
-            // Non-euclidean warp
-            function warp(px, py) {
-                if (traits.space !== 'noneuclidean') return [px, py];
-                var cx = w / 2, cy = h / 2;
-                var dx = (px - cx) / cx, dy = (py - cy) / cy;
-                var r = Math.sqrt(dx * dx + dy * dy);
-                var sc = r < 0.95 ? 1 / (1 - r * 0.35) : 4;
-                return [cx + dx * sc * cx * 0.68, cy + dy * sc * cy * 0.68];
-            }
-
-            function irl(c) { c.run++; if (c.prev) irl(c.prev); }
-
-            // ── Seed search — max 2000 attempts to prevent hang ──────────────────────
-            var cells = [], grid = [], cell, a;
-            // Base seed: RENDER_SEED drives the pattern shape, id offsets each variant
-            var seed = RENDER_SEED + (id - 1) * 37;
-            var prng = makePRNG(seed);
-            var rfl = prng.rfl, rin = prng.rin;
-            var attempts = 0, MAX_ATTEMPTS = 2000;
-
-            // ── Generate motif-based pattern parameters ──────────────────────────────
-            var patPRNG = makePRNG(RENDER_SEED + id * 1009);
-            var patternType = traits.engine || 'mastor';
-            var motifParams = generatePatternParams(patternType, patPRNG, w, h);
-
-            while (attempts++ < MAX_ATTEMPTS) {
-                cells = [];
-                grid = [];
-                for (var xi = 0; xi < w; xi++) {
-                    grid[xi] = [];
-                    for (var yi = 0; yi < h; yi++)
-                        grid[xi][yi] = { x: xi, y: yi, col: null, c: null, run: 0, prev: null };
-                }
-
-                // Secondary pole attractors (subtle warp influence)
-                var pls = [];
-                for (var pi = 0; pi < 3; pi++) {
-                    var o = rin(0, 1);
-                    pls.push([rfl(0, o ? h : w), o, rfl(8, 25)]);
-                }
-
-                // ── Two-pass weight normalization ──────────────────────────
-                // Pass 1: compute raw weights + find range
-                var rawWt = [];
-                var wMin = Infinity, wMax = -Infinity;
-                for (var cy = 0; cy < h; cy++) {
-                    for (var cx = 0; cx < w; cx++) {
-                        var wp = warp(cx, cy);
-                        var p = [wp[0], wp[1]], d;
-                        var qq = [cx + 16, cy + 16];
-                        for (var pli = 0; pli < pls.length; pli++) {
-                            var pl = pls[pli];
-                            d = (Math.abs(p[pl[1]] - pl[0]) + 1) / pl[2];
-                            if (d < 1) { p[0] = d * p[0] + (1 - d) * qq[0]; p[1] = d * p[1] + (1 - d) * qq[1]; }
-                        }
-                        var cw = calcMotifWeight(Math.round(p[0]), Math.round(p[1]), w, h, patternType, motifParams);
-                        rawWt.push(cw);
-                        if (cw < wMin) wMin = cw;
-                        if (cw > wMax) wMax = cw;
-                    }
-                }
-
-                // Pass 2: normalize to 0-12 buckets using actual range
-                var wRange = wMax - wMin;
-                if (wRange < 0.001) wRange = 1; // flat field fallback
-                var ri2 = 0;
-                for (var cy2 = 0; cy2 < h; cy2++) {
-                    for (var cx2 = 0; cx2 < w; cx2++) {
-                        var norm = (rawWt[ri2++] - wMin) / wRange; // 0..1
-                        var c = Math.floor(norm * 12.99);          // 0..12
-                        if (c < 10) grid[cx2][cy2].col = rc(c);
+            return new Promise(function (resolve, reject) {
+                var svgNS = "http://www.w3.org/2000/svg";
+                var dc = document;
+                var svg = dc.getElementById(svgId);
+                if (!svg) return resolve({ flowers: 0 });
+
+                id = Math.min(Math.max(id | 0, 1), 3);
+                var w = 78, h = 148, size = 16, rSize = 14;
+
+                // Clear
+                while (svg.firstChild) svg.removeChild(svg.firstChild);
+
+                var svgW = (w + 8) * size;
+                var svgH = (h + 8) * size;
+                svg.setAttribute("width", svgW);
+                svg.setAttribute("height", svgH);
+                svg.setAttribute("viewBox", "0 0 " + svgW + " " + svgH);
+
+                var bg = dc.createElementNS(svgNS, "rect");
+                bg.setAttribute("width", svgW); bg.setAttribute("height", svgH);
+                bg.setAttribute("fill", "transparent");
+                svg.appendChild(bg);
+
+                var gEl = dc.createElementNS(svgNS, "g");
+                svg.appendChild(gEl);
+
+                // Setup the worker
+                var worker = new Worker('js/larkspur-worker.js');
+
+                worker.onmessage = function (e) {
+                    var data = e.data;
+                    var flowers = data.flowers; // Array of {x, y, c, flip}
+
+                    // Draw flowers using returned data
+                    for (var i = 0; i < flowers.length; i++) {
+                        var f = flowers[i];
+                        var px = (f.x + 4) * size + 1;
+                        var py = (f.y + 4) * size + 1;
+                        var sc = rSize / 20;
+                        var path = dc.createElementNS(svgNS, "path");
+                        var xform = "translate(" + px + "," + py + ") scale(" + sc + ")";
+                        if (f.flip) xform += " rotate(180,10,10)";
+                        path.setAttribute("transform", xform);
+                        path.setAttribute("d", FLOWER_PATH);
+                        path.setAttribute("fill", f.c);
+                        gEl.appendChild(path);
                     }
-                }
-
 
-                // Symmetry
-                var sym = traits.symmetry;
-                if (sym === 'horizontal' || sym === 'both') {
-                    for (var sy = 0; sy < h; sy++)
-                        for (var sx = 0; sx < Math.floor(w / 2); sx++) {
-                            var sxx = w - 1 - sx;
-                            grid[sxx][sy] = { x: sxx, y: sy, col: grid[sx][sy].col, c: null, run: 0, prev: null };
-                        }
-                }
-                if (sym === 'vertical' || sym === 'both') {
-                    for (var sy2 = 0; sy2 < Math.floor(h / 2); sy2++)
-                        for (var sx2 = 0; sx2 < w; sx2++) {
-                            var syy = h - 1 - sy2;
-                            grid[sx2][syy] = { x: sx2, y: syy, col: grid[sx2][sy2].col, c: null, run: 0, prev: null };
-                        }
-                }
-                if (sym === 'diagonal') {
-                    for (var dy2 = 0; dy2 < h; dy2++)
-                        for (var dx2 = 0; dx2 < w; dx2++) {
-                            var mx = Math.min(Math.floor(dx2 * h / w), h - 1);
-                            var my = Math.min(Math.floor(dy2 * w / h), w - 1);
-                            grid[dx2][dy2].col = grid[my][mx].col;
-                        }
-                }
-                // Rotational: 180° point symmetry around center
-                if (sym === 'rotational') {
-                    for (var ry2 = 0; ry2 < h; ry2++)
-                        for (var rx2 = 0; rx2 < w; rx2++) {
-                            var rrx = w - 1 - rx2, rry = h - 1 - ry2;
-                            if (ry2 * w + rx2 > rry * w + rrx)
-                                grid[rx2][ry2] = { x: rx2, y: ry2, col: grid[rrx][rry].col, c: null, run: 0, prev: null };
-                        }
-                }
-                // Quad: 4-fold rotation (both axes + 180°)
-                if (sym === 'quad') {
-                    var hw2 = Math.floor(w / 2), hh2 = Math.floor(h / 2);
-                    for (var qy = 0; qy < hh2; qy++)
-                        for (var qx = 0; qx < hw2; qx++) {
-                            var col_q = grid[qx][qy].col;
-                            grid[w - 1 - qx][qy] = { x: w - 1 - qx, y: qy, col: col_q, c: null, run: 0, prev: null };
-                            grid[qx][h - 1 - qy] = { x: qx, y: h - 1 - qy, col: col_q, c: null, run: 0, prev: null };
-                            grid[w - 1 - qx][h - 1 - qy] = { x: w - 1 - qx, y: h - 1 - qy, col: col_q, c: null, run: 0, prev: null };
-                        }
-                }
-                // Kaleidoscope: 8-fold (quad + diagonal reflection)
-                if (sym === 'kaleidoscope') {
-                    var hw3 = Math.floor(w / 2), hh3 = Math.floor(h / 2);
-                    // First: quad symmetry
-                    for (var ky = 0; ky < hh3; ky++)
-                        for (var kx = 0; kx < hw3; kx++) {
-                            var col_k = grid[kx][ky].col;
-                            grid[w - 1 - kx][ky] = { x: w - 1 - kx, y: ky, col: col_k, c: null, run: 0, prev: null };
-                            grid[kx][h - 1 - ky] = { x: kx, y: h - 1 - ky, col: col_k, c: null, run: 0, prev: null };
-                            grid[w - 1 - kx][h - 1 - ky] = { x: w - 1 - kx, y: h - 1 - ky, col: col_k, c: null, run: 0, prev: null };
-                        }
-                    // Then: diagonal reflection within each quadrant
-                    for (var ky2 = 0; ky2 < hh3; ky2++)
-                        for (var kx2 = 0; kx2 < hw3; kx2++) {
-                            var dkx = Math.min(Math.floor(kx2 * hh3 / hw3), hh3 - 1);
-                            var dky = Math.min(Math.floor(ky2 * hw3 / hh3), hw3 - 1);
-                            if (kx2 > dky) grid[kx2][ky2].col = grid[dky][dkx].col;
-                        }
-                }
-                // Glide: horizontal mirror + half-period vertical shift (classic band pattern)
-                if (sym === 'glide') {
-                    var halfH = Math.floor(h / 2);
-                    for (var gy = 0; gy < h; gy++)
-                        for (var gx = 0; gx < Math.floor(w / 2); gx++) {
-                            var gShiftY = (gy + halfH) % h;
-                            grid[w - 1 - gx][gShiftY] = { x: w - 1 - gx, y: gShiftY, col: grid[gx][gy].col, c: null, run: 0, prev: null };
-                        }
-                }
-                // Radial: concentric ring mirroring from center
-                if (sym === 'radial') {
-                    var rcx = w / 2, rcy = h / 2;
-                    for (var ray = 0; ray < h; ray++)
-                        for (var rax = 0; rax < w; rax++) {
-                            var raDist = Math.sqrt((rax - rcx) * (rax - rcx) + (ray - rcy) * (ray - rcy));
-                            var raAngle = Math.atan2(ray - rcy, rax - rcx);
-                            // Mirror to first quadrant
-                            var mAngle = Math.abs(raAngle);
-                            if (mAngle > Math.PI / 2) mAngle = Math.PI - mAngle;
-                            var srcX = Math.min(Math.max(Math.round(rcx + Math.cos(mAngle) * raDist), 0), w - 1);
-                            var srcY = Math.min(Math.max(Math.round(rcy + Math.sin(mAngle) * raDist), 0), h - 1);
-                            if (srcX !== rax || srcY !== ray)
-                                grid[rax][ray] = { x: rax, y: ray, col: grid[srcX][srcY].col, c: null, run: 0, prev: null };
-                        }
-                }
+                    worker.terminate();
+                    resolve({ flowers: data.flowerCount, seed: data.seed, attempts: data.attempts });
+                };
 
-                // Collect cells
-                a = 0;
-                var b = 1;
-                for (var ry = 0; ry < h; ry++) {
-                    var prev = null;
-                    for (var rx = 0; rx < w; rx++) {
-                        cell = grid[rx][ry];
-                        if (cell.col && !cell.col.v) {
-                            a++;
-                            cells.push(cell);
-                            if (prev !== null && prev.col === cell.col) {
-                                cell.prev = prev; cell.run = prev.run;
-                            }
-                            irl(cell);
-                            prev = cell;
-                        } else { b++; prev = null; }
-                    }
-                }
+                worker.onerror = function (err) {
+                    console.error("Worker error in panel " + id + ":", err.message);
+                    worker.terminate();
+                    reject(err);
+                };
 
-                cells.sort(function (a, b) {
-                    return (b.run - a.run) || (Math.abs(h / 2 - a.y) - Math.abs(h / 2 - b.y));
+                // Dispatch payload
+                worker.postMessage({
+                    id: id,
+                    traits: traits,
+                    seed: RENDER_SEED,
+                    w: w,
+                    h: h,
+                    size: size,
+                    rSize: rSize
                 });
-
-
-
-                seed++;
-                prng = makePRNG(seed); rfl = prng.rfl; rin = prng.rin;
-                cells = [];
-            }
-
-
-
-            // SVG dimensions — always portrait
-            var svgW = (w + 8) * size;
-            var svgH = (h + 8) * size;
-            svg.setAttribute("width", svgW);
-            svg.setAttribute("height", svgH);
-            svg.setAttribute("viewBox", "0 0 " + svgW + " " + svgH);
-
-            // Background
-            var bg = dc.createElementNS(svgNS, "rect");
-            bg.setAttribute("width", svgW); bg.setAttribute("height", svgH);
-            bg.setAttribute("fill", "transparent");
-            svg.appendChild(bg);
-            svg.appendChild(gEl);
-
-            // Render
-            for (var fy = 0; fy < h; fy++) {
-                for (var fx = 0; fx < w; fx++) {
-                    cell = grid[fx][fy];
-
-                    var dx = cell.x, dy = cell.y;
-                    if (traits.space === 'noneuclidean') {
-                        var p = warp(cell.x, cell.y);
-                        dx = p[0];
-                        dy = p[1];
-                    }
-
-                    if (cell.col) {
-                        var flip = shouldFlipFlower(fx, fy);
-                        adr(dx, dy, cell.col.c, flip);
-                    }
-
-                }
-            }
-
-            return { flowers: cells.length, seed: seed, attempts: attempts };
+            });
         }
 
 
diff --git a/larkspur.reb b/larkspur.reb
new file mode 100644
index 0000000..28a54ed
Binary files /dev/null and b/larkspur.reb differ
diff --git a/larkspur_canvas_white.png b/larkspur_canvas_white.png
new file mode 100644
index 0000000..c627792
Binary files /dev/null and b/larkspur_canvas_white.png differ
diff --git a/match_pens.py b/match_pens.py
new file mode 100644
index 0000000..cd981c4
--- /dev/null
+++ b/match_pens.py
@@ -0,0 +1,152 @@
+import math
+import re
+
+# Faber-Castell Pitt Artist Pens (India Ink, Archival, 60 Colors)
+PITT_PENS = [
+    {"name": "White", "code": "101", "hex": "#FFFFFF"},
+    {"name": "Cream", "code": "102", "hex": "#FDF2C8"},
+    {"name": "Light Yellow Glaze", "code": "104", "hex": "#FBF57E"},
+    {"name": "Cadmium Yellow", "code": "107", "hex": "#FFED00"},
+    {"name": "Dark Cadmium Yellow", "code": "108", "hex": "#FFCC00"},
+    {"name": "Dark Chrome Yellow", "code": "109", "hex": "#FDB913"},
+    {"name": "Phthalo Blue", "code": "110", "hex": "#004B87"},
+    {"name": "Leaf Green", "code": "112", "hex": "#6EB23B"},
+    {"name": "Orange Glaze", "code": "113", "hex": "#F07F23"},
+    {"name": "Pale Geranium Lake", "code": "114", "hex": "#E24F44"},
+    {"name": "Dark Cadmium Orange", "code": "115", "hex": "#E35A22"},
+    {"name": "Scarlet Red", "code": "118", "hex": "#D12D36"},
+    {"name": "Ultramarine", "code": "120", "hex": "#304192"},
+    {"name": "Pale Carmine", "code": "124", "hex": "#CA3E5F"},
+    {"name": "Middle Purple Pink", "code": "125", "hex": "#D85189"},
+    {"name": "Permanent Carmine", "code": "126", "hex": "#BB2845"},
+    {"name": "Pink Carmine", "code": "127", "hex": "#D23768"},
+    {"name": "Light Purple Pink", "code": "128", "hex": "#E67F99"},
+    {"name": "Pink Madder Lake", "code": "129", "hex": "#CA567B"},
+    {"name": "Medium Flesh", "code": "131", "hex": "#E9A28E"},
+    {"name": "Light Flesh", "code": "132", "hex": "#F4BCA3"},
+    {"name": "Magenta", "code": "133", "hex": "#A83669"},
+    {"name": "Crimson", "code": "134", "hex": "#A2284C"},
+    {"name": "Purple Violet", "code": "136", "hex": "#623162"},
+    {"name": "Sky Blue", "code": "146", "hex": "#64A6D5"},
+    {"name": "Warm Grey V", "code": "274", "hex": "#504E4A"},
+    {"name": "Warm Grey IV", "code": "273", "hex": "#6C6B64"},
+    {"name": "Warm Grey III", "code": "272", "hex": "#89887F"},
+    {"name": "Cold Grey I", "code": "230", "hex": "#D9DBDA"},
+    {"name": "Cold Grey III", "code": "232", "hex": "#99A0A6"},
+    {"name": "Cold Grey IV", "code": "233", "hex": "#767A7D"},
+    {"name": "Cold Grey VI", "code": "235", "hex": "#4C4F51"},
+    {"name": "Black", "code": "199", "hex": "#000000"},
+    {"name": "Nougat", "code": "278", "hex": "#584435"},
+    {"name": "Dark Sepia", "code": "175", "hex": "#3A332B"},
+    {"name": "Sanguine", "code": "188", "hex": "#983626"},
+    {"name": "Terracotta", "code": "186", "hex": "#BB5835"},
+    {"name": "Cinnamon", "code": "189", "hex": "#CF8460"},
+    {"name": "Green Gold", "code": "268", "hex": "#9BA829"},
+    {"name": "May Green", "code": "170", "hex": "#9CC347"},
+    {"name": "Earth Green", "code": "172", "hex": "#597E52"},
+    {"name": "Olive Green Yellowish", "code": "173", "hex": "#7A823B"},
+    {"name": "Chromium Green Opaque", "code": "174", "hex": "#4A6E4F"},
+    {"name": "Dark Phthalo Green", "code": "264", "hex": "#005540"},
+    {"name": "Deep Scarlet Red", "code": "219", "hex": "#BF1931"},
+    {"name": "Dark Red", "code": "225", "hex": "#881E31"},
+    {"name": "Alizarin Crimson", "code": "226", "hex": "#A11438"},
+    {"name": "Dark Indigo", "code": "157", "hex": "#193551"},
+    {"name": "Dark Naples Ochre", "code": "184", "hex": "#EABC52"},
+    {"name": "Raw Umber", "code": "180", "hex": "#86603D"},
+    {"name": "Burnt Sienna", "code": "283", "hex": "#A04A2F"},
+    {"name": "Light Phthalo Green", "code": "162", "hex": "#27A472"},
+    {"name": "Cobalt Green", "code": "156", "hex": "#008688"},
+    {"name": "Cobalt Turquoise", "code": "153", "hex": "#0083A8"},
+    {"name": "Light Indigo", "code": "145", "hex": "#5D6782"}
+]
+
+def hex_to_rgb(hex_code):
+    hex_code = hex_code.lstrip('#')
+    return tuple(int(hex_code[i:i+2], 16) for i in (0, 2, 4))
+
+def color_distance(rgb1, rgb2):
+    return math.sqrt(sum((a - b) ** 2 for a, b in zip(rgb1, rgb2)))
+
+def get_closest_pen(target_hex):
+    target_rgb = hex_to_rgb(target_hex)
+    closest_pen = None
+    min_dist = float('inf')
+    
+    for pen in PITT_PENS:
+        pen_rgb = hex_to_rgb(pen["hex"])
+        dist = color_distance(target_rgb, pen_rgb)
+        if dist < min_dist:
+            min_dist = dist
+            closest_pen = pen
+            
+    return closest_pen
+
+def process_palettes(html_file):
+    with open(html_file, 'r', encoding='utf-8') as f:
+        content = f.read()
+        
+    palettes_text = re.search(r'var PALETTES = (\{.*?\});', content, re.DOTALL).group(1)
+    
+    palettes = {}
+    current_palette = None
+    lines = palettes_text.split('\n')
+    for line in lines:
+        m_start = re.search(r'([a-z]+):\s*\[', line)
+        if m_start:
+            current_palette = m_start.group(1)
+            palettes[current_palette] = []
+        elif current_palette:
+            m_color = re.search(r'{[^}]*c:\s*"([^"]+)"', line)
+            if m_color:
+                palettes[current_palette].append(m_color.group(1))
+
+    mapped_palettes = {}
+    for name, hexes in palettes.items():
+        mapped_palettes[name] = []
+        for h in hexes:
+            pen = get_closest_pen(h)
+            mapped_palettes[name].append({
+                "original_hex": h,
+                "pen_name": pen["name"],
+                "pen_code": pen["code"],
+                "pen_hex": pen["hex"]
+            })
+            
+    return mapped_palettes
+
+if __name__ == "__main__":
+    mapped = process_palettes("larkspur-quine.html")
+    
+    md_content = "# Axidraw Pen Mappings\n\n"
+    md_content += "This document provides a mapping of all generative palette colors to **Faber-Castell Pitt Artist Pens**.\n"
+    md_content += "Faber-Castell Pitt Artist Pens use high-quality, archival India Ink, which is perfect for plotter art.\n\n"
+    
+    md_content += "## Complete Pen List (Palette Subset)\n"
+    seen_pens = set()
+    for pal in mapped.values():
+        for color in pal:
+            seen_pens.add(f"- {color['pen_code']} - {color['pen_name']}")
+    md_content += "\n".join(sorted(seen_pens)) + "\n\n"
+    
+    md_content += "## Palettes\n\n"
+    for pal_name, colors in mapped.items():
+        md_content += f"### {pal_name.capitalize()}\n"
+        for c in colors:
+            md_content += f"- Original `{c['original_hex']}` ➔ **{c['pen_name']}** ({c['pen_code']})\n"
+        md_content += "\n"
+        
+    with open("library/axidraw-pens.md", "w") as f:
+        f.write(md_content)
+        
+    js_dict = "var AXIDRAW_PENS = {\n"
+    for pal_name, colors in mapped.items():
+        js_dict += f"    {pal_name}: [\n"
+        for c in colors:
+            js_dict += f"        {{ orig: \"{c['original_hex']}\", pen: \"{c['pen_name']} (Pitt {c['pen_code']})\", hex: \"{c['pen_hex']}\" }},\n"
+        js_dict += "    ],\n"
+    js_dict += "};"
+    
+    with open("js.txt", "w") as f:
+        f.write(js_dict)
+    
+    print("Done generating pens.")
diff --git a/splice.js b/splice.js
new file mode 100644
index 0000000..fe6ab2b
--- /dev/null
+++ b/splice.js
@@ -0,0 +1,28 @@
+const fs = require('fs');
+
+let lines = fs.readFileSync('larkspur.html', 'utf8').split('\n');
+
+// Arrays are 0-indexed, so line N is lines[N-1]
+// We delete from bottom to top to preserve indices of earlier targets
+
+// 4. Delete PRNG, motifs, calcMotifWeight, generatePatternParams (Lines 1129-2200)
+lines.splice(1128, 2200 - 1129 + 1);
+
+// 3. Delete CHROME_DESCS and ENGINE_DESCS (Lines 999-1025)
+lines.splice(998, 1025 - 999 + 1);
+
+// 2. Delete SYMMETRY_OPTS through CANVAS_OPTS (Lines 947-952)
+lines.splice(946, 952 - 947 + 1);
+
+// 1. Delete PALETTES and PEN_MAP (Lines 448-937)
+lines.splice(447, 937 - 448 + 1);
+
+// Inject script tags right before the main <script> tag (Line 389 originally, meaning index 388)
+lines.splice(388, 0,
+    '    <script src="js/larkspur-core.js"></script>',
+    '    <script src="js/larkspur-motifs.js"></script>',
+    '    <script src="js/larkspur-engines.js"></script>'
+);
+
+fs.writeFileSync('larkspur.html', lines.join('\n'));
+console.log('Successfully spliced larkspur.html');

commit 8539b879431e1e06059698fbfd84e3b8aac900be
Author: artistdbjohnson <fakedoingstuff@gmail.com>
Date:   Sun Feb 22 01:05:04 2026 +0000

    style: replace Seigaiha overlapping concentric circles with continuous geometric sine wave ribbons

diff --git a/larkspur.html b/larkspur.html
index 009b75a..8f6815a 100644
--- a/larkspur.html
+++ b/larkspur.html
@@ -1349,43 +1349,31 @@
             return (onCross || onX || onDia || onSq) ? 1 : 0;
         }
 
-        // 21. Seigaiha: Japanese continuous overlapping concentric ocean waves
-        function motifSeigaiha(x, y, radius, spacing, thickness, dotR) {
-            var R = radius;
-            var approxRow = Math.floor(y / (R / 2));
-            var foundDist = -1;
-
-            // Search rows from highest y (drawn on top) backwards
-            for (var rOff = +2; rOff >= -3; rOff--) {
-                var row = approxRow + rOff;
-                if (row < 0) continue;
-                var cy = row * (R / 2);
-
-                var isEven = row % 2 === 0;
-                var xOff = isEven ? R : 0;
-
-                var approxCol = Math.floor((x - xOff) / (2 * R));
-                for (var cOff = 0; cOff <= 1; cOff++) {
-                    var col = approxCol + cOff;
-                    var cx = xOff + col * (2 * R);
-
-                    var dx = x - cx;
-                    var dy = y - cy;
-                    var dist = Math.sqrt(dx * dx + dy * dy);
-                    // Match full circle; Z-order determines visibility
-                    if (dist <= R) {
-                        foundDist = dist;
-                        break;
-                    }
+        // 21. Seigaiha: Japanese continuous overlapping sine waves (vectors)
+        function motifSeigaiha(x, y, period, amp, rowSpacing, stripeSpacing, thickness) {
+            var approxRow = Math.floor(y / rowSpacing);
+            var foundDy = -1;
+
+            // Search rows from bottom-up (higher y drawn on top of lower y)
+            for (var ro = 2; ro >= -2; ro--) {
+                var r = approxRow + ro;
+                var phaseOff = (r % 2 === 0) ? 0 : (period / 2); // Stagger alternate rows
+
+                // Base Y for the wave crest (crest points up, so lower Y)
+                var waveY = r * rowSpacing - amp * Math.cos((x + phaseOff) / period * Math.PI * 2);
+
+                if (y >= waveY) {
+                    // Inside this wave body
+                    foundDy = y - waveY;
+                    break;
                 }
-                if (foundDist >= 0) break;
             }
 
-            if (foundDist < 0) return 0;
-            if (dotR > 0 && foundDist < dotR) return 1;
+            if (foundDy < 0) return 0;
 
-            var phase = foundDist % spacing;
-            return Math.abs(phase) < thickness ? 1 : 0;
+            // Draw regular internal stripes parallel to the wave
+            var phaseDist = foundDy % stripeSpacing;
+            return (phaseDist < thickness || (stripeSpacing - phaseDist) < thickness) ? 1 : 0;
         }
 
 
@@ -1418,8 +1406,8 @@
                     break;
 
                 case 'seigaiha':
-                    // Japanese Waves: Overlapping concentric arches
-                    wt += motifSeigaiha(x, y, params.R, params.spacing, params.thickness, params.dotR) * params.waveWt;
+                    // Japanese Waves: Overlapping continuous sine vectors
+                    wt += motifSeigaiha(x, y, params.period, params.amp, params.rowSpacing, params.stripeSpacing, params.thickness) * params.waveWt;
                     break;
 
                 case 'kente':
@@ -2183,10 +2171,11 @@
                     break;
 
                 case 'seigaiha':
-                    params.R = Math.floor(w * rfl(0.08, 0.25));
-                    params.thickness = rin(1, 4);
-                    params.spacing = params.thickness * rin(2, 5);
-                    params.dotR = rfl(0, params.spacing); // sometimes solid center dot
+                    params.period = Math.floor(w * rfl(0.15, 0.4));
+                    params.amp = params.period * rfl(0.15, 0.35);
+                    params.rowSpacing = params.amp * rfl(0.7, 1.4);
+                    params.thickness = rin(1, 3);
+                    params.stripeSpacing = params.thickness * rin(3, 7);
                     params.waveWt = rfl(3, 8);
                     break;
 

commit ac9f0a2a1481937f2620152a455ef34e92655ca4
Author: artistdbjohnson <fakedoingstuff@gmail.com>
Date:   Sun Feb 22 00:20:37 2026 +0000

    fix: rewrite motifSeigaiha math to compute true radial z-order overlap

diff --git a/larkspur.html b/larkspur.html
index 8bdfe29..009b75a 100644
--- a/larkspur.html
+++ b/larkspur.html
@@ -1372,8 +1372,8 @@
                     var dx = x - cx;
                     var dy = y - cy;
                     var dist = Math.sqrt(dx * dx + dy * dy);
-                    // Match upper semicircle only
-                    if (dist <= R && dy <= 0) {
+                    // Match full circle; Z-order determines visibility
+                    if (dist <= R) {
                         foundDist = dist;
                         break;
                     }
@@ -1417,6 +1417,11 @@
                     }
                     break;
 
+                case 'seigaiha':
+                    // Japanese Waves: Overlapping concentric arches
+                    wt += motifSeigaiha(x, y, params.R, params.spacing, params.thickness, params.dotR) * params.waveWt;
+                    break;
+
                 case 'kente':
                     // Asante strip-weave: staggered vertical narrow bands with dense checkerboards/blocks
                     var bandIdx = Math.floor(x / params.stripW);
@@ -2177,6 +2182,14 @@
                     params.bleedWt = rfl(5, 12);
                     break;
 
+                case 'seigaiha':
+                    params.R = Math.floor(w * rfl(0.08, 0.25));
+                    params.thickness = rin(1, 4);
+                    params.spacing = params.thickness * rin(2, 5);
+                    params.dotR = rfl(0, params.spacing); // sometimes solid center dot
+                    params.waveWt = rfl(3, 8);
+                    break;
+
                 case 'kente':
                     params.stripW = Math.floor(w / rin(6, 15)); // Narrow, numerous strips
                     params.blockH = Math.floor(params.stripW * rfl(1.0, 2.5)); // Rectangular sequence blocks

commit 89e8657e21e7f18a44e9364f64cb044196b0e947
Author: artistdbjohnson <fakedoingstuff@gmail.com>
Date:   Sun Feb 22 00:09:38 2026 +0000

    feat: implement Adire and Kente generator engines

diff --git a/larkspur.html b/larkspur.html
index 736d0ce..8bdfe29 100644
--- a/larkspur.html
+++ b/larkspur.html
@@ -948,7 +948,7 @@
 
         var SPACE_OPTS = ['euclidean', 'noneuclidean'];
         var CHROME_OPTS = ['random', 'kokkos', 'murex', 'tukmo', 'rutsya', 'zahav', 'karkom', 'viride', 'gan', 'tarshish', 'tekhelet', 'yam', 'porphyra', 'shoshan', 'pepel', 'panar', 'katan', 'kaspi', 'barzel', 'wari', 'navajo', 'kinjiki', 'shibui', 'seigaiha', 'bogolan', 'adire', 'kuba', 'kente'];
-        var ENGINE_OPTS = ['random', 'argyle', 'bogolan', 'chipaz', 'interlace', 'kepe', 'kishtima', 'kolya', 'kshtir', 'kuba', 'kudo', 'mastor', 'narmuny', 'navajo', 'pakshats', 'panks', 'pulay', 'seigaiha', 'sermat', 'sermat-kudo', 'tol', 'ved', 'virma', 'wari'];
+        var ENGINE_OPTS = ['random', 'adama', 'adire', 'argyle', 'bogolan', 'chipaz', 'interlace', 'kente', 'kepe', 'kishtima', 'kolya', 'kshtir', 'kuba', 'kudo', 'mastor', 'narmuny', 'navajo', 'pakshats', 'panks', 'pulay', 'seigaiha', 'sermat', 'sermat-kudo', 'tol', 'ved', 'virma', 'wari'];
         var CANVAS_OPTS = ['black', 'tan', 'olive', 'white'];
 
         var currentLayout = 'panel';
@@ -1013,8 +1013,9 @@
         };
 
         var ENGINE_DESCS = {
-            'random': 'Random', 'argyle': 'Lace & Lattice', 'bogolan': 'Mudcloth Strips',
-            'chipaz': 'Sunburst', 'interlace': 'Islamic Strapwork', 'kepe': 'Crown Peaks',
+            'random': 'Random', 'adama': 'Earth / Southwest', 'adire': 'Indigo Resist Dye',
+            'argyle': 'Lace & Lattice', 'bogolan': 'Mudcloth Strips', 'chipaz': 'Sunburst',
+            'interlace': 'Islamic Strapwork', 'kente': 'Staggered Weave', 'kepe': 'Crown Peaks',
             'kishtima': 'Tessellation', 'kolya': 'Corner Meander', 'kshtir': 'Rotational Pinwheel',
             'kuba': 'Raffia Patches', 'kudo': 'Brick Masonry', 'mastor': 'Diamonds & Diagonals',
             'narmuny': 'Decorative Strips', 'navajo': 'Stepped Bands', 'pakshats': 'Interlocking Meander',
@@ -1393,6 +1394,63 @@
         function calcMotifWeight(x, y, w, h, engine, params) {
             var wt = 0;
             switch (engine) {
+                case 'adire':
+                    // Yoruba indigo resist dye: organic, tied circles, stenciled bleeding grids
+                    var adireType = (Math.floor(x / params.cellSize) + Math.floor(y / params.cellSize)) % 3;
+
+                    if (adireType === 0) {
+                        // organic tie-dye sunburst circles
+                        var cx = Math.floor(x / params.cellSize) * params.cellSize + params.cellSize / 2;
+                        var cy = Math.floor(y / params.cellSize) * params.cellSize + params.cellSize / 2;
+                        wt += motifTieDyeRing(x, y, cx, cy, params.ringR, params.ringT, params.fuzz) * params.ringWt;
+                    } else if (adireType === 1) {
+                        // freehand cascading waves/whorls (using a warped sine)
+                        var wave = Math.sin((x + Math.sin(y / 20) * 15) / params.waveP);
+                        if (Math.abs(wave) < params.waveT) wt += params.waveWt;
+                    } else {
+                        // sloppy stenciled grid (imperfect)
+                        wt += motifLattice(x + Math.sin(y / 10) * 5, y + Math.cos(x / 10) * 5, params.lP, params.lT) * params.lWt;
+                    }
+                    // intense border bleed
+                    if ((x % params.cellSize < params.bleed) || (y % params.cellSize < params.bleed)) {
+                        wt += params.bleedWt * (Math.random() * 0.5 + 0.5);
+                    }
+                    break;
+
+                case 'kente':
+                    // Asante strip-weave: staggered vertical narrow bands with dense checkerboards/blocks
+                    var bandIdx = Math.floor(x / params.stripW);
+                    var localX = x % params.stripW;
+
+                    // stagger the blocks vertically based on the band
+                    var staggeredY = y + (bandIdx % 2 === 0 ? 0 : params.blockH / 2);
+                    var blockIdx = Math.floor(staggeredY / params.blockH);
+                    var localY = staggeredY % params.blockH;
+
+                    var motifType = (bandIdx * 7 + blockIdx * 11) % 4; // pseudo-random deterministic choice per block
+
+                    // Heavy visible stitched seam between vertical weaves
+                    if (localX < params.seamW || localX > params.stripW - params.seamW) {
+                        wt += params.seamWt;
+                    } else {
+                        if (motifType === 0) {
+                            // tightly packed checkered
+                            wt += motifLattice(localX, localY, params.cP, params.cT) * params.cWt;
+                        } else if (motifType === 1) {
+                            // dense parallel weft lines
+                            if (localY % params.wP < params.wT) wt += params.wWt;
+                        } else if (motifType === 2) {
+                            // stepped block steps
+                            wt += motifStepped(localX, localY, params.sP, params.sT, 0) * params.sWt;
+                        } else {
+                            // solid block
+                            if (Math.abs(localX - params.stripW / 2) < params.stripW * 0.4 && Math.abs(localY - params.blockH / 2) < params.blockH * 0.4) {
+                                wt += params.bWt;
+                            }
+                        }
+                    }
+                    break;
+
                 case 'bogolan':
                     // Mudcloth - distinct horizontal or vertical strips containing geometric repetitions
                     var isVert = params.orientation === 1;
@@ -1978,7 +2036,7 @@
 
                 case 'sermat-kudo':
                     // Pattern-house — blend 2-3 engines
-                    var bases = ['wari', 'navajo', 'argyle', 'interlace', 'seigaiha', 'sermat', 'tol', 'mastor', 'virma', 'ved', 'chipaz', 'kudo', 'pulay', 'kepe', 'panks', 'kishtima', 'kshtir', 'narmuny', 'pakshats', 'kolya', 'bogolan', 'kuba'];
+                    var bases = ['wari', 'navajo', 'argyle', 'interlace', 'seigaiha', 'sermat', 'tol', 'mastor', 'virma', 'ved', 'chipaz', 'kudo', 'pulay', 'kepe', 'panks', 'kishtima', 'kshtir', 'narmuny', 'pakshats', 'kolya', 'bogolan', 'kuba', 'adire', 'kente'];
                     var nmix = rin(2, 3);
                     params.mix = [];
                     var used = {};
@@ -2102,6 +2160,39 @@
                     params.edgeW = rin(2, 6);
                     params.edgeWt = rfl(2, 5);
                     break;
+
+                case 'adire':
+                    params.cellSize = rin(60, 150);
+                    params.ringR = Math.floor(params.cellSize * rfl(0.2, 0.4));
+                    params.ringT = rin(3, 8);
+                    params.fuzz = rin(10, 30);
+                    params.ringWt = rfl(4, 10);
+                    params.waveP = rin(15, 35);
+                    params.waveT = rfl(0.1, 0.3);
+                    params.waveWt = rfl(3, 7);
+                    params.lP = rin(10, 25);
+                    params.lT = rin(2, 6);
+                    params.lWt = rfl(3, 8);
+                    params.bleed = rin(5, 15);
+                    params.bleedWt = rfl(5, 12);
+                    break;
+
+                case 'kente':
+                    params.stripW = Math.floor(w / rin(6, 15)); // Narrow, numerous strips
+                    params.blockH = Math.floor(params.stripW * rfl(1.0, 2.5)); // Rectangular sequence blocks
+                    params.seamW = rin(2, 6);
+                    params.seamWt = rfl(5, 12);
+                    params.cP = rin(6, 14); // dense checkered
+                    params.cT = rin(3, 7);
+                    params.cWt = rfl(4, 9);
+                    params.wP = rin(4, 10); // dense weft
+                    params.wT = rin(2, 5);
+                    params.wWt = rfl(3, 8);
+                    params.sP = rin(8, 20); // steps
+                    params.sT = rin(2, 6);
+                    params.sWt = rfl(3, 8);
+                    params.bWt = rfl(6, 12); // solid
+                    break;
             }
             return params;
         }

commit ef461ae6c1c7de8f2b0d5f9cdb1aaa32bb6a7411
Author: artistdbjohnson <fakedoingstuff@gmail.com>
Date:   Sat Feb 21 23:55:16 2026 +0000

    feat: implement African Textile palettes and engines (bogolan, kuba)

diff --git a/larkspur.html b/larkspur.html
index 7fcf337..736d0ce 100644
--- a/larkspur.html
+++ b/larkspur.html
@@ -838,6 +838,37 @@
                 { c: "#A5BA93", r: 1 },       // Byakuroku / Whitish green accent
                 { c: "#FFDDCA", r: 1 },       // Shironeri / Unbleached silk
                 { c: "#FFFFFF", r: 1, v: 1 }  // Pure White (Larkspur foam accent)
+            ],
+
+            // ── African Textiles ──────────────────────────────────────────────────────
+
+            bogolan: [  // Bamana Mudcloth
+                { c: "#272013", r: 3 },       // Dark Mud
+                { c: "#a9a599", r: 2 },       // Bleached Cotton
+                { c: "#8c7c57", r: 2 },       // Ochre Mud
+                { c: "#4b3f23", r: 2 },       // Wet Earth
+                { c: "#392f1b", r: 3 }        // Deep Brown
+            ],
+            adire: [  // Yoruba Indigo Resist Dye
+                { c: "#1b1c1d", r: 3 },       // Deep Indigo Black
+                { c: "#333f44", r: 3 },       // Dark Slate Blue
+                { c: "#4a5b61", r: 2 },       // Faded Indigo
+                { c: "#6f7471", r: 2 },       // Greyed Blue
+                { c: "#928d82", r: 1, v: 1 }  // White Resist Dye Accent
+            ],
+            kuba: [  // Ngongo Raffia
+                { c: "#aaa59d", r: 2 },       // Natural Light Raffia
+                { c: "#816a5b", r: 2 },       // Warm Woven Tan
+                { c: "#713417", r: 2 },       // Terracotta Dye
+                { c: "#502716", r: 2 },       // Russet Brown
+                { c: "#311a12", r: 3 }        // Dark Woven Brown
+            ],
+            kente: [  // Asante / Ewe Woven Colors
+                { c: "#272018", r: 3 },       // Dark Woven Bark
+                { c: "#a29a8b", r: 2 },       // Light Woven Thread
+                { c: "#8b4513", r: 2, v: 1 }, // Vibrant Rust/Ochre Accent
+                { c: "#683c1c", r: 2 },       // Warm Earth Red
+                { c: "#4b351b", r: 2 }        // Deep Brown
             ]
         };
 
@@ -916,8 +947,8 @@
         var SYMMETRY_OPTS = ['random', 'none', 'vertical', 'horizontal', 'both', 'diagonal', 'rotational', 'quad', 'kaleidoscope', 'glide', 'radial'];
 
         var SPACE_OPTS = ['euclidean', 'noneuclidean'];
-        var CHROME_OPTS = ['random', 'kokkos', 'murex', 'tukmo', 'rutsya', 'zahav', 'karkom', 'viride', 'gan', 'tarshish', 'tekhelet', 'yam', 'porphyra', 'shoshan', 'pepel', 'panar', 'katan', 'kaspi', 'barzel', 'wari', 'navajo', 'kinjiki', 'shibui', 'seigaiha'];
-        var ENGINE_OPTS = ['random', 'argyle', 'chipaz', 'interlace', 'kepe', 'kishtima', 'kolya', 'kshtir', 'kudo', 'mastor', 'narmuny', 'navajo', 'pakshats', 'panks', 'pulay', 'seigaiha', 'sermat', 'sermat-kudo', 'tol', 'ved', 'virma', 'wari'];
+        var CHROME_OPTS = ['random', 'kokkos', 'murex', 'tukmo', 'rutsya', 'zahav', 'karkom', 'viride', 'gan', 'tarshish', 'tekhelet', 'yam', 'porphyra', 'shoshan', 'pepel', 'panar', 'katan', 'kaspi', 'barzel', 'wari', 'navajo', 'kinjiki', 'shibui', 'seigaiha', 'bogolan', 'adire', 'kuba', 'kente'];
+        var ENGINE_OPTS = ['random', 'argyle', 'bogolan', 'chipaz', 'interlace', 'kepe', 'kishtima', 'kolya', 'kshtir', 'kuba', 'kudo', 'mastor', 'narmuny', 'navajo', 'pakshats', 'panks', 'pulay', 'seigaiha', 'sermat', 'sermat-kudo', 'tol', 'ved', 'virma', 'wari'];
         var CANVAS_OPTS = ['black', 'tan', 'olive', 'white'];
 
         var currentLayout = 'panel';
@@ -977,18 +1008,19 @@
             'simcha': 'Joy / Festival', 'tarshish': 'Aqua / Beryl', 'tekhelet': 'Azure Blue',
             'tsemer': 'Soft Wool', 'tukmo': 'Brick / Earthen', 'viride': 'Green Earth / Forest',
             'wari': 'Andean Patchwork', 'yam': 'Sea / Navy', 'zahav': 'Gold / Luxe',
-            'kinjiki': 'Imperial Reds & Purples', 'shibui': 'Astringent / Earthy', 'seigaiha': 'The Sea & Waves'
+            'kinjiki': 'Imperial Reds & Purples', 'shibui': 'Astringent / Earthy', 'seigaiha': 'The Sea & Waves',
+            'bogolan': 'Mali Mudcloth', 'adire': 'Yoruba Indigo', 'kuba': 'Ngongo Raffia', 'kente': 'Asante Woven'
         };
 
         var ENGINE_DESCS = {
-            'random': 'Random', 'argyle': 'Lace & Lattice', 'chipaz': 'Sunburst',
-            'interlace': 'Islamic Strapwork', 'kepe': 'Crown Peaks', 'kishtima': 'Tessellation',
-            'kolya': 'Corner Meander', 'kshtir': 'Rotational Pinwheel', 'kudo': 'Brick Masonry',
-            'mastor': 'Diamonds & Diagonals', 'narmuny': 'Decorative Strips', 'navajo': 'Stepped Bands',
-            'pakshats': 'Interlocking Meander', 'panks': 'Floral Rosettes', 'pulay': 'Varied Grid Bands',
-            'seigaiha': 'Japanese Waves', 'sermat': 'Tiled Crosses', 'sermat-kudo': 'Blended Patterns',
-            'tol': 'Chevron Flames', 'ved': 'Interlocking S-Curves', 'virma': 'Stepped Forest Frame',
-            'wari': 'Patchwork Checks'
+            'random': 'Random', 'argyle': 'Lace & Lattice', 'bogolan': 'Mudcloth Strips',
+            'chipaz': 'Sunburst', 'interlace': 'Islamic Strapwork', 'kepe': 'Crown Peaks',
+            'kishtima': 'Tessellation', 'kolya': 'Corner Meander', 'kshtir': 'Rotational Pinwheel',
+            'kuba': 'Raffia Patches', 'kudo': 'Brick Masonry', 'mastor': 'Diamonds & Diagonals',
+            'narmuny': 'Decorative Strips', 'navajo': 'Stepped Bands', 'pakshats': 'Interlocking Meander',
+            'panks': 'Floral Rosettes', 'pulay': 'Varied Grid Bands', 'seigaiha': 'Japanese Waves',
+            'sermat': 'Tiled Crosses', 'sermat-kudo': 'Blended Patterns', 'tol': 'Chevron Flames',
+            'ved': 'Interlocking S-Curves', 'virma': 'Stepped Forest Frame', 'wari': 'Patchwork Checks'
         };
 
         function populateSelect(id, opts, val) {
@@ -1361,6 +1393,54 @@
         function calcMotifWeight(x, y, w, h, engine, params) {
             var wt = 0;
             switch (engine) {
+                case 'bogolan':
+                    // Mudcloth - distinct horizontal or vertical strips containing geometric repetitions
+                    var isVert = params.orientation === 1;
+                    var primaryCoord = isVert ? x : y;
+                    var secondaryCoord = isVert ? y : x;
+
+                    var stripIdx = Math.floor(primaryCoord / params.stripW);
+                    var stripPhase = primaryCoord % params.stripW;
+
+                    var motifType = stripIdx % params.motifs.length;
+                    var mo = params.motifs[motifType];
+
+                    if (stripPhase < params.borderT || stripPhase > params.stripW - params.borderT) {
+                        wt += params.borderWt;
+                    } else {
+                        if (mo.type === 0) {
+                            wt += motifChevron(secondaryCoord, stripPhase, params.stripW, mo.p1, mo.p2) * mo.wt;
+                        } else if (mo.type === 1) {
+                            var dcx = (secondaryCoord % mo.p1) - mo.p1 / 2;
+                            var dcy = stripPhase - params.stripW / 2;
+                            if (Math.abs(dcx) + Math.abs(dcy) < mo.p2) wt += mo.wt;
+                        } else if (mo.type === 2) {
+                            var ccx = (secondaryCoord % mo.p1) - mo.p1 / 2;
+                            var ccy = stripPhase - params.stripW / 2;
+                            if (Math.abs(ccx) < mo.p2 || Math.abs(ccy) < mo.p2) wt += mo.wt;
+                        } else {
+                            if ((secondaryCoord % mo.p1) < mo.p2) wt += mo.wt;
+                        }
+                    }
+                    break;
+
+                case 'kuba':
+                    // Raffia weave - dense, irregular orthogonal patches
+                    var pcx = Math.floor(x / params.patchSize);
+                    var pcy = Math.floor(y / params.patchSize);
+                    var pType = (pcx * 13 + pcy * 29) % 5;
+                    var ix = x % params.patchSize;
+                    var iy = y % params.patchSize;
+
+                    if (pType === 0) wt += motifMeander(x, y, params.mP, params.mT, 1) * params.mWt;
+                    else if (pType === 1) wt += motifMeander(y, x, params.mP, params.mT, 1) * params.mWt;
+                    else if (pType === 2) wt += motifStepped(x, y, params.sP, params.sT, 0) * params.sWt;
+                    else if (pType === 3) wt += motifStepped(x, y, params.sP, params.sT, 1) * params.sWt;
+                    else wt += motifLattice(x, y, params.mP, params.mT) * params.mWt * 0.7;
+
+                    if (ix < params.bT || iy < params.bT) wt += params.bWt;
+                    break;
+
                 case 'wari':
                     // Patchwork checks + tie-dye rings
                     var px = Math.floor(x / params.patchW);
@@ -1638,6 +1718,36 @@
             var params = {};
 
             switch (engine) {
+                case 'bogolan':
+                    params.orientation = rin(0, 1);
+                    params.stripW = Math.floor((params.orientation === 1 ? w : h) / rin(3, 9));
+                    var numMotifs = rin(2, 5);
+                    params.motifs = [];
+                    for (var bi = 0; bi < numMotifs; bi++) {
+                        params.motifs.push({
+                            type: rin(0, 3),
+                            p1: rin(20, 60),
+                            p2: rin(4, 15),
+                            wt: rfl(4, 9)
+                        });
+                    }
+                    params.borderT = rin(2, 8);
+                    params.borderWt = rfl(5, 12);
+                    break;
+
+                case 'kuba':
+                    params.patchSize = rin(20, 80);
+                    // Generate specific meander and stepped params to map tightly
+                    params.mP = rin(10, 30);
+                    params.mT = rin(1, 4);
+                    params.mWt = rfl(3, 8);
+                    params.sP = rin(10, 30);
+                    params.sT = rin(2, 6);
+                    params.sWt = rfl(4, 9);
+                    params.bT = rin(2, 6);
+                    params.bWt = rfl(5, 12);
+                    break;
+
                 case 'wari':
                     params.patchW = Math.floor(w / rin(3, 6)); // 3 to 6 columns
                     params.patchH = Math.floor(h / rin(4, 8)); // 4 to 8 rows
@@ -1868,7 +1978,7 @@
 
                 case 'sermat-kudo':
                     // Pattern-house — blend 2-3 engines
-                    var bases = ['wari', 'navajo', 'argyle', 'interlace', 'seigaiha', 'sermat', 'tol', 'mastor', 'virma', 'ved', 'chipaz', 'kudo', 'pulay', 'kepe', 'panks', 'kishtima', 'kshtir', 'narmuny', 'pakshats', 'kolya'];
+                    var bases = ['wari', 'navajo', 'argyle', 'interlace', 'seigaiha', 'sermat', 'tol', 'mastor', 'virma', 'ved', 'chipaz', 'kudo', 'pulay', 'kepe', 'panks', 'kishtima', 'kshtir', 'narmuny', 'pakshats', 'kolya', 'bogolan', 'kuba'];
                     var nmix = rin(2, 3);
                     params.mix = [];
                     var used = {};

commit 546a913dcd9adc9a42b7e9311d30ae6fc4532a4b
Author: artistdbjohnson <fakedoingstuff@gmail.com>
Date:   Sat Feb 21 23:38:26 2026 +0000

    style: add crisp white larkspur accents to seigaiha and shibui palettes

diff --git a/larkspur.html b/larkspur.html
index 5a4dee5..7fcf337 100644
--- a/larkspur.html
+++ b/larkspur.html
@@ -826,16 +826,18 @@
                 { c: "#656255", r: 2 },       // Rikyu-nezumi / Greyish dark green
                 { c: "#A13D2D", r: 2 },       // Edocha / Red-brown
                 { c: "#593A27", r: 2 },       // Susutake / Weathered bamboo
-                { c: "#8B7D3A", r: 1, v: 1 }, // Koke / Moss accent
-                { c: "#B9A193", r: 2 }        // Shironezumi / White mouse
+                { c: "#8B7D3A", r: 1 },       // Koke / Moss accent
+                { c: "#B9A193", r: 2 },       // Shironezumi / White mouse
+                { c: "#F8F8F8", r: 1, v: 1 }  // Bright White (Larkspur accent)
             ],
             seigaiha: [  // The Sea & Waves
                 { c: "#003171", r: 3 },       // Konjo / Prussian blue
                 { c: "#1B294B", r: 3 },       // Rurikon / Lapis dark blue
                 { c: "#48929B", r: 2 },       // Asagi / Light blue
                 { c: "#5D8CAE", r: 2 },       // Gunjo / Ultramarine
-                { c: "#A5BA93", r: 1, v: 1 }, // Byakuroku / Whitish green accent
-                { c: "#FFDDCA", r: 1 }        // Shironeri / Unbleached silk
+                { c: "#A5BA93", r: 1 },       // Byakuroku / Whitish green accent
+                { c: "#FFDDCA", r: 1 },       // Shironeri / Unbleached silk
+                { c: "#FFFFFF", r: 1, v: 1 }  // Pure White (Larkspur foam accent)
             ]
         };
 

commit 6048cfa4ac99f76ca96ce7ad0c02b8e2ad99ea29
Author: artistdbjohnson <fakedoingstuff@gmail.com>
Date:   Sat Feb 21 23:36:00 2026 +0000

    style: boost purple in kinjiki palette and remove drop shadow in light theme

diff --git a/larkspur.html b/larkspur.html
index 02de44c..5a4dee5 100644
--- a/larkspur.html
+++ b/larkspur.html
@@ -23,6 +23,7 @@
             --dim: rgba(222, 228, 215, 0.35);
             --canvas-bg: #151515;
             --body-bg: #100e13;
+            --shadow: drop-shadow(0 0 10px rgba(0, 0, 0, 0.5));
         }
 
         [data-theme="light"] {
@@ -34,6 +35,7 @@
             --dim: rgba(0, 0, 0, 0.5);
             --canvas-bg: #ffffff;
             --body-bg: #ffffff;
+            --shadow: none;
         }
 
         html,
@@ -185,7 +187,7 @@
             background-size: contain;
             background-position: center;
             background-repeat: no-repeat;
-            filter: drop-shadow(0 0 10px rgba(0, 0, 0, 0.5));
+            filter: var(--shadow);
             position: relative;
             cursor: grab;
             transform-origin: center center;
@@ -811,11 +813,11 @@
             // ── Japanese Traditional Palettes ─────────────────────────────────────────
 
             kinjiki: [  // Imperial Reds & Purples (Forbidden Colors)
-                { c: "#FF4E20", r: 2 },       // Otan / Orange
-                { c: "#C91F37", r: 3 },       // Karakurenai / Crimson
-                { c: "#7E2639", r: 2 },       // Su'o / Sappanwood
+                { c: "#FF4E20", r: 1 },       // Otan / Orange
+                { c: "#C91F37", r: 2 },       // Karakurenai / Crimson
+                { c: "#7E2639", r: 1 },       // Su'o / Sappanwood
                 { c: "#2B2028", r: 3 },       // Shikon / Blue-violet
-                { c: "#5D3F6A", r: 2 },       // Kikyo / Bellflower
+                { c: "#5D3F6A", r: 4 },       // Kikyo / Bellflower (Boosted)
                 { c: "#DB5A6B", r: 1, v: 1 }, // Kobai / Red plum accent
                 { c: "#F5E6D3", r: 1 }        // Cream contrast
             ],

commit 0fc7dffc6ef7b4791f4118367d7d215a3d566808
Author: artistdbjohnson <fakedoingstuff@gmail.com>
Date:   Sat Feb 21 23:17:00 2026 +0000

    feat: add Japanese Waves engine and traditional color palettes

diff --git a/larkspur.html b/larkspur.html
index 0ad6f2b..02de44c 100644
--- a/larkspur.html
+++ b/larkspur.html
@@ -806,6 +806,34 @@
                 { c: "#C8E0A0", r: 2 },       // lime
                 { c: "#387050", r: 2 },       // dark green
                 { c: "#203828", r: 3 }        // near-black
+            ],
+
+            // ── Japanese Traditional Palettes ─────────────────────────────────────────
+
+            kinjiki: [  // Imperial Reds & Purples (Forbidden Colors)
+                { c: "#FF4E20", r: 2 },       // Otan / Orange
+                { c: "#C91F37", r: 3 },       // Karakurenai / Crimson
+                { c: "#7E2639", r: 2 },       // Su'o / Sappanwood
+                { c: "#2B2028", r: 3 },       // Shikon / Blue-violet
+                { c: "#5D3F6A", r: 2 },       // Kikyo / Bellflower
+                { c: "#DB5A6B", r: 1, v: 1 }, // Kobai / Red plum accent
+                { c: "#F5E6D3", r: 1 }        // Cream contrast
+            ],
+            shibui: [  // Earthy & Astringent (Permissible Colors)
+                { c: "#264348", r: 3 },       // Ai-iro / Indigo
+                { c: "#656255", r: 2 },       // Rikyu-nezumi / Greyish dark green
+                { c: "#A13D2D", r: 2 },       // Edocha / Red-brown
+                { c: "#593A27", r: 2 },       // Susutake / Weathered bamboo
+                { c: "#8B7D3A", r: 1, v: 1 }, // Koke / Moss accent
+                { c: "#B9A193", r: 2 }        // Shironezumi / White mouse
+            ],
+            seigaiha: [  // The Sea & Waves
+                { c: "#003171", r: 3 },       // Konjo / Prussian blue
+                { c: "#1B294B", r: 3 },       // Rurikon / Lapis dark blue
+                { c: "#48929B", r: 2 },       // Asagi / Light blue
+                { c: "#5D8CAE", r: 2 },       // Gunjo / Ultramarine
+                { c: "#A5BA93", r: 1, v: 1 }, // Byakuroku / Whitish green accent
+                { c: "#FFDDCA", r: 1 }        // Shironeri / Unbleached silk
             ]
         };
 
@@ -884,8 +912,8 @@
         var SYMMETRY_OPTS = ['random', 'none', 'vertical', 'horizontal', 'both', 'diagonal', 'rotational', 'quad', 'kaleidoscope', 'glide', 'radial'];
 
         var SPACE_OPTS = ['euclidean', 'noneuclidean'];
-        var CHROME_OPTS = ['random', 'kokkos', 'murex', 'tukmo', 'rutsya', 'zahav', 'karkom', 'viride', 'gan', 'tarshish', 'tekhelet', 'yam', 'porphyra', 'shoshan', 'pepel', 'panar', 'katan', 'kaspi', 'barzel', 'wari', 'navajo'];
-        var ENGINE_OPTS = ['random', 'argyle', 'chipaz', 'interlace', 'kepe', 'kishtima', 'kolya', 'kshtir', 'kudo', 'mastor', 'narmuny', 'navajo', 'pakshats', 'panks', 'pulay', 'sermat', 'sermat-kudo', 'tol', 'ved', 'virma', 'wari'];
+        var CHROME_OPTS = ['random', 'kokkos', 'murex', 'tukmo', 'rutsya', 'zahav', 'karkom', 'viride', 'gan', 'tarshish', 'tekhelet', 'yam', 'porphyra', 'shoshan', 'pepel', 'panar', 'katan', 'kaspi', 'barzel', 'wari', 'navajo', 'kinjiki', 'shibui', 'seigaiha'];
+        var ENGINE_OPTS = ['random', 'argyle', 'chipaz', 'interlace', 'kepe', 'kishtima', 'kolya', 'kshtir', 'kudo', 'mastor', 'narmuny', 'navajo', 'pakshats', 'panks', 'pulay', 'seigaiha', 'sermat', 'sermat-kudo', 'tol', 'ved', 'virma', 'wari'];
         var CANVAS_OPTS = ['black', 'tan', 'olive', 'white'];
 
         var currentLayout = 'panel';
@@ -944,7 +972,8 @@
             'rutsya': 'Rust', 'shahar': 'Dawn / Salmon', 'shemen': 'Olive Oil', 'shoshan': 'Lily / Rose',
             'simcha': 'Joy / Festival', 'tarshish': 'Aqua / Beryl', 'tekhelet': 'Azure Blue',
             'tsemer': 'Soft Wool', 'tukmo': 'Brick / Earthen', 'viride': 'Green Earth / Forest',
-            'wari': 'Andean Patchwork', 'yam': 'Sea / Navy', 'zahav': 'Gold / Luxe'
+            'wari': 'Andean Patchwork', 'yam': 'Sea / Navy', 'zahav': 'Gold / Luxe',
+            'kinjiki': 'Imperial Reds & Purples', 'shibui': 'Astringent / Earthy', 'seigaiha': 'The Sea & Waves'
         };
 
         var ENGINE_DESCS = {
@@ -953,8 +982,9 @@
             'kolya': 'Corner Meander', 'kshtir': 'Rotational Pinwheel', 'kudo': 'Brick Masonry',
             'mastor': 'Diamonds & Diagonals', 'narmuny': 'Decorative Strips', 'navajo': 'Stepped Bands',
             'pakshats': 'Interlocking Meander', 'panks': 'Floral Rosettes', 'pulay': 'Varied Grid Bands',
-            'sermat': 'Tiled Crosses', 'sermat-kudo': 'Blended Patterns', 'tol': 'Chevron Flames',
-            'ved': 'Interlocking S-Curves', 'virma': 'Stepped Forest Frame', 'wari': 'Patchwork Checks'
+            'seigaiha': 'Japanese Waves', 'sermat': 'Tiled Crosses', 'sermat-kudo': 'Blended Patterns',
+            'tol': 'Chevron Flames', 'ved': 'Interlocking S-Curves', 'virma': 'Stepped Forest Frame',
+            'wari': 'Patchwork Checks'
         };
 
         function populateSelect(id, opts, val) {
@@ -1282,6 +1312,45 @@
             return (onCross || onX || onDia || onSq) ? 1 : 0;
         }
 
+        // 21. Seigaiha: Japanese continuous overlapping concentric ocean waves
+        function motifSeigaiha(x, y, radius, spacing, thickness, dotR) {
+            var R = radius;
+            var approxRow = Math.floor(y / (R / 2));
+            var foundDist = -1;
+
+            // Search rows from highest y (drawn on top) backwards
+            for (var rOff = +2; rOff >= -3; rOff--) {
+                var row = approxRow + rOff;
+                if (row < 0) continue;
+                var cy = row * (R / 2);
+
+                var isEven = row % 2 === 0;
+                var xOff = isEven ? R : 0;
+
+                var approxCol = Math.floor((x - xOff) / (2 * R));
+                for (var cOff = 0; cOff <= 1; cOff++) {
+                    var col = approxCol + cOff;
+                    var cx = xOff + col * (2 * R);
+
+                    var dx = x - cx;
+                    var dy = y - cy;
+                    var dist = Math.sqrt(dx * dx + dy * dy);
+                    // Match upper semicircle only
+                    if (dist <= R && dy <= 0) {
+                        foundDist = dist;
+                        break;
+                    }
+                }
+                if (foundDist >= 0) break;
+            }
+
+            if (foundDist < 0) return 0;
+            if (dotR > 0 && foundDist < dotR) return 1;
+
+            var phase = foundDist % spacing;
+            return Math.abs(phase) < thickness ? 1 : 0;
+        }
+
 
         // ── ENGINE WEIGHT CALCULATOR ─────────────────────────────────────────────────
         // Returns total motif weight for cell (x,y) based on engine type + params.
@@ -1365,6 +1434,11 @@
                     wt += motifStarGrid(x, y, params.starP, params.starR, params.starT) * params.starWt;
                     break;
 
+                case 'seigaiha':
+                    // Japanese concentric overlapping waves
+                    wt += motifSeigaiha(x, y, params.R, params.spacing, params.thickness, params.dotR) * params.waveWt;
+                    break;
+
                 case 'sermat':
                     // Tiled crosses — family identity marks
                     for (var ci = 0; ci < params.centers.length; ci++) {
@@ -1621,6 +1695,14 @@
                     params.starWt = rfl(4, 9);
                     break;
 
+                case 'seigaiha':
+                    params.R = Math.floor(w * rfl(0.08, 0.25));
+                    params.thickness = rin(1, 4);
+                    params.spacing = params.thickness * rin(2, 5);
+                    params.dotR = rfl(0, params.spacing); // sometimes solid center dot
+                    params.waveWt = rfl(3, 8);
+                    break;
+
                 case 'sermat':
                     var nc = rin(3, 8);
                     params.centers = [];
@@ -1782,7 +1864,7 @@
 
                 case 'sermat-kudo':
                     // Pattern-house — blend 2-3 engines
-                    var bases = ['wari', 'navajo', 'argyle', 'interlace', 'sermat', 'tol', 'mastor', 'virma', 'ved', 'chipaz', 'kudo', 'pulay', 'kepe', 'panks', 'kishtima', 'kshtir', 'narmuny', 'pakshats', 'kolya'];
+                    var bases = ['wari', 'navajo', 'argyle', 'interlace', 'seigaiha', 'sermat', 'tol', 'mastor', 'virma', 'ved', 'chipaz', 'kudo', 'pulay', 'kepe', 'panks', 'kishtima', 'kshtir', 'narmuny', 'pakshats', 'kolya'];
                     var nmix = rin(2, 3);
                     params.mix = [];
                     var used = {};

commit 46230763cb5c4d5c92581ff254d3ab03036afaf9
Author: artistdbjohnson <fakedoingstuff@gmail.com>
Date:   Sat Feb 21 17:03:42 2026 +0000

    feat: distill chromes dropdown to primary color palettes and add descriptions

diff --git a/larkspur.html b/larkspur.html
index 30a5e1e..0ad6f2b 100644
--- a/larkspur.html
+++ b/larkspur.html
@@ -884,7 +884,7 @@
         var SYMMETRY_OPTS = ['random', 'none', 'vertical', 'horizontal', 'both', 'diagonal', 'rotational', 'quad', 'kaleidoscope', 'glide', 'radial'];
 
         var SPACE_OPTS = ['euclidean', 'noneuclidean'];
-        var CHROME_OPTS = ['random', 'adama', 'afarsek', 'alon', 'argaman', 'bahir', 'barzel', 'erev', 'gan', 'gehelet', 'karkom', 'kaspi', 'katan', 'kelme', 'kokkos', 'madder', 'murex', 'nana', 'navajo', 'neshef', 'panar', 'pepel', 'porphyra', 'ruach', 'rutsya', 'shahar', 'shemen', 'shoshan', 'simcha', 'tarshish', 'tekhelet', 'tsemer', 'tukmo', 'viride', 'wari', 'yam', 'zahav'];
+        var CHROME_OPTS = ['random', 'kokkos', 'murex', 'tukmo', 'rutsya', 'zahav', 'karkom', 'viride', 'gan', 'tarshish', 'tekhelet', 'yam', 'porphyra', 'shoshan', 'pepel', 'panar', 'katan', 'kaspi', 'barzel', 'wari', 'navajo'];
         var ENGINE_OPTS = ['random', 'argyle', 'chipaz', 'interlace', 'kepe', 'kishtima', 'kolya', 'kshtir', 'kudo', 'mastor', 'narmuny', 'navajo', 'pakshats', 'panks', 'pulay', 'sermat', 'sermat-kudo', 'tol', 'ved', 'virma', 'wari'];
         var CANVAS_OPTS = ['black', 'tan', 'olive', 'white'];
 
@@ -933,12 +933,44 @@
             renderAll();
         }
 
+        var CHROME_DESCS = {
+            'random': 'Random', 'adama': 'Earth / Southwest', 'afarsek': 'Peach', 'alon': 'Oak',
+            'argaman': 'Crimson', 'bahir': 'Luminous', 'barzel': 'Iron', 'erev': 'Evening',
+            'gan': 'Garden / Green', 'gehelet': 'Ember / Umber', 'karkom': 'Saffron / Amber',
+            'kaspi': 'Silver / Slate', 'katan': 'Linen / Cream', 'kelme': 'Ink & Parchment',
+            'kokkos': 'Scarlet', 'madder': 'Madder Root', 'murex': 'Midnight', 'nana': 'Mint',
+            'navajo': 'Southwest Rug', 'neshef': 'Twilight', 'panar': 'Hemp / Parchment',
+            'pepel': 'Ash / White Mono', 'porphyra': 'Purple Shell / Coral', 'ruach': 'Wind / Breeze',
+            'rutsya': 'Rust', 'shahar': 'Dawn / Salmon', 'shemen': 'Olive Oil', 'shoshan': 'Lily / Rose',
+            'simcha': 'Joy / Festival', 'tarshish': 'Aqua / Beryl', 'tekhelet': 'Azure Blue',
+            'tsemer': 'Soft Wool', 'tukmo': 'Brick / Earthen', 'viride': 'Green Earth / Forest',
+            'wari': 'Andean Patchwork', 'yam': 'Sea / Navy', 'zahav': 'Gold / Luxe'
+        };
+
+        var ENGINE_DESCS = {
+            'random': 'Random', 'argyle': 'Lace & Lattice', 'chipaz': 'Sunburst',
+            'interlace': 'Islamic Strapwork', 'kepe': 'Crown Peaks', 'kishtima': 'Tessellation',
+            'kolya': 'Corner Meander', 'kshtir': 'Rotational Pinwheel', 'kudo': 'Brick Masonry',
+            'mastor': 'Diamonds & Diagonals', 'narmuny': 'Decorative Strips', 'navajo': 'Stepped Bands',
+            'pakshats': 'Interlocking Meander', 'panks': 'Floral Rosettes', 'pulay': 'Varied Grid Bands',
+            'sermat': 'Tiled Crosses', 'sermat-kudo': 'Blended Patterns', 'tol': 'Chevron Flames',
+            'ved': 'Interlocking S-Curves', 'virma': 'Stepped Forest Frame', 'wari': 'Patchwork Checks'
+        };
+
         function populateSelect(id, opts, val) {
             var sel = document.getElementById(id);
             if (sel.options.length === 0) {
                 for (var i = 0; i < opts.length; i++) {
                     var o = document.createElement('option');
-                    o.value = opts[i]; o.textContent = opts[i];
+                    var key = opts[i];
+                    var desc = "";
+                    if (id === 't-chrome' && CHROME_DESCS[key] && key !== 'random') {
+                        desc = " [" + CHROME_DESCS[key] + "]";
+                    } else if (id === 't-engine' && ENGINE_DESCS[key] && key !== 'random') {
+                        desc = " [" + ENGINE_DESCS[key] + "]";
+                    }
+                    o.value = key;
+                    o.textContent = key + desc;
                     sel.appendChild(o);
                 }
             }

commit d5c36864475a971abf4c73280fee63dc2e138545
Author: artistdbjohnson <fakedoingstuff@gmail.com>
Date:   Sat Feb 21 16:18:01 2026 +0000

    fix: skip texture XHR when protocol is file:// to prevent CORS console spam

diff --git a/larkspur.html b/larkspur.html
index edf6baf..30a5e1e 100644
--- a/larkspur.html
+++ b/larkspur.html
@@ -2600,6 +2600,14 @@
 
         var _alertedFileProtocol = false;
         function preloadTexture(path, callback) {
+            if (window.location.protocol === 'file:') {
+                if (!_alertedFileProtocol) {
+                    _alertedFileProtocol = true;
+                    console.warn('Larkspur Warning: Canvas textures cannot be exported directly from file:// due to browser security restrictions. Export will use solid color backgrounds. Generation will continue.');
+                }
+                return; // skip XHR to avoid console spam
+            }
+
             var xhr = new XMLHttpRequest();
             xhr.open('GET', path, true);
             xhr.responseType = 'blob';
@@ -2612,10 +2620,6 @@
             };
             xhr.onerror = function () {
                 console.error('[Larkspur] Failed to load texture: ' + path);
-                if (window.location.protocol === 'file:' && !_alertedFileProtocol) {
-                    _alertedFileProtocol = true;
-                    console.warn('Larkspur Warning: Canvas textures cannot be exported directly from file:// due to browser security restrictions, but generation will continue.');
-                }
             };
             xhr.send();
         }

commit e80d91d8330ddecfe430b54e56a15264ad8b8a68
Author: artistdbjohnson <fakedoingstuff@gmail.com>
Date:   Sat Feb 21 14:34:32 2026 +0000

    fix: disable auto-render, remove texture export alert, disable UI until render

diff --git a/larkspur.html b/larkspur.html
index eebfa74..edf6baf 100644
--- a/larkspur.html
+++ b/larkspur.html
@@ -347,28 +347,30 @@
             <button id="btn-theme" onclick="toggleTheme()" title="Toggle Light/Dark Mode">☀︎</button>
             <button id="btn-randomize" onclick="randomize()">&#9632; Randomize</button>
             <button id="btn-regen" onclick="regenerate()">&#8635; Regenerate</button>
-            <button id="btn-panel" class="active" onclick="setLayout('panel')">Panel 9:16</button>
-            <button id="btn-banner" onclick="setLayout('banner')">Banner 16:9</button>
-            <button onclick="saveSVG()" title="Download SVG">&#8595; SVG</button>
-            <button onclick="savePNG()" title="Download PNG">&#8595; PNG</button>
-            <button onclick="saveTriptych()" title="Download Triptych (3 Panels)">&#8595; TRIPTYCH</button>
-            <button onclick="saveDST()" title="Download DST (Embroidery)">&#8595; DST</button>
-            <button onclick="savePlotterSVG()" title="Download SVG for Plotter (Inkscape Layers)">&#8595; PLOT</button>
+            <button id="btn-panel" class="active" onclick="setLayout('panel')" disabled>Panel 9:16</button>
+            <button id="btn-banner" onclick="setLayout('banner')" disabled>Banner 16:9</button>
+            <button id="btn-out-svg" onclick="saveSVG()" title="Download SVG" disabled>&#8595; SVG</button>
+            <button id="btn-out-png" onclick="savePNG()" title="Download PNG" disabled>&#8595; PNG</button>
+            <button id="btn-out-triptych" onclick="saveTriptych()" title="Download Triptych (3 Panels)" disabled>&#8595;
+                TRIPTYCH</button>
+            <button id="btn-out-dst" onclick="saveDST()" title="Download DST (Embroidery)" disabled>&#8595; DST</button>
+            <button id="btn-out-plot" onclick="savePlotterSVG()" title="Download SVG for Plotter (Inkscape Layers)"
+                disabled>&#8595; PLOT</button>
         </div>
     </div>
 
     <div id="traitbar">
         <div class="trait"><span class="trait-label">Symmetry</span><select class="trait-select" id="t-sym"
-                onchange="TRAITS.symmetry=this.value"></select></div>
+                onchange="TRAITS.symmetry=this.value" disabled></select></div>
 
         <div class="trait"><span class="trait-label">Space</span><select class="trait-select" id="t-space"
-                onchange="TRAITS.space=this.value"></select></div>
+                onchange="TRAITS.space=this.value" disabled></select></div>
         <div class="trait"><span class="trait-label">Chromes</span><select class="trait-select" id="t-chrome"
-                onchange="TRAITS.chromes=this.value"></select></div>
+                onchange="TRAITS.chromes=this.value" disabled></select></div>
         <div class="trait"><span class="trait-label">Engine</span><select class="trait-select" id="t-engine"
-                onchange="TRAITS.engine=this.value"></select></div>
+                onchange="TRAITS.engine=this.value" disabled></select></div>
         <div class="trait"><span class="trait-label">Canvas</span><select class="trait-select" id="t-canvas"
-                onchange="TRAITS.canvas=this.value; updateCanvasTexture()"></select></div>
+                onchange="TRAITS.canvas=this.value; updateCanvasTexture()" disabled></select></div>
     </div>
 
     <!-- Always 3 panels — quantity is always 3 -->
@@ -907,8 +909,8 @@
                 btnPanel.classList.remove('active');
                 btnBanner.classList.add('active');
             }
-
-            renderAll();
+            // Auto-render disabled: only render on randomize/regenerate
+            // renderAll();
         }
 
         function randomize() {
@@ -1007,6 +1009,21 @@
 
                 // Start chain
                 renderNext();
+
+                // Enable UI elements now that we have rendered
+                document.getElementById('btn-panel').disabled = false;
+                document.getElementById('btn-banner').disabled = false;
+                document.getElementById('btn-out-svg').disabled = false;
+                document.getElementById('btn-out-png').disabled = false;
+                document.getElementById('btn-out-triptych').disabled = false;
+                document.getElementById('btn-out-dst').disabled = false;
+                document.getElementById('btn-out-plot').disabled = false;
+
+                document.getElementById('t-sym').disabled = false;
+                document.getElementById('t-space').disabled = false;
+                document.getElementById('t-chrome').disabled = false;
+                document.getElementById('t-engine').disabled = false;
+                document.getElementById('t-canvas').disabled = false;
             }, 10);
         }
 
@@ -2597,7 +2614,7 @@
                 console.error('[Larkspur] Failed to load texture: ' + path);
                 if (window.location.protocol === 'file:' && !_alertedFileProtocol) {
                     _alertedFileProtocol = true;
-                    alert('Larkspur Error: Canvas textures cannot be exported when opening the HTML file directly (file://) due to browser security restrictions.\n\nPlease run a local web server (e.g., using "python serve.py") to export canvas backgrounds correctly.');
+                    console.warn('Larkspur Warning: Canvas textures cannot be exported directly from file:// due to browser security restrictions, but generation will continue.');
                 }
             };
             xhr.send();
@@ -2910,7 +2927,7 @@
         })();
 
         // ── Boot ──────────────────────────────────────────────────────────────────────
-        setTimeout(renderAll, 100);
+        // Initial render disabled per user request: setTimeout(renderAll, 100);
     </script>
 </body>
 
